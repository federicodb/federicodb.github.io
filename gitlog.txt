commit d3eff8492147bf9c0bcc4588d56776a2a1ecd598
Author: federicodb <vivasantarosa@gmail.com>
Date:   Fri Apr 3 18:20:45 2026 +0200

    fix(mobile): Revert viewport restrictions for WebGL UI stability

diff --git a/index.html b/index.html
index c027ed4..794f034 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 
 <head>
     <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
     <title>Laboratorio Matematico Orfini | Immersive Hub</title>
 
     <link rel="preconnect" href="https://fonts.googleapis.com">

commit bfa1720ed590ea82f53561ab099692e5c7e28822
Author: federicodb <vivasantarosa@gmail.com>
Date:   Fri Apr 3 17:49:12 2026 +0200

    fix(mobile): Risolto bug touch Safari iOS (backdrop-filter)

diff --git a/index.html b/index.html
index dc387b6..c027ed4 100644
--- a/index.html
+++ b/index.html
@@ -249,15 +249,18 @@
             justify-content: flex-start;
             flex-direction: column;
             opacity: 0;
+            visibility: hidden;
             pointer-events: none;
-            transition: all 0.4s ease;
+            transition: opacity 0.4s ease, visibility 0s linear 0.4s;
             overflow-y: auto;
             padding: 2rem;
         }
 
         .overlay-full.active {
             opacity: 1;
+            visibility: visible;
             pointer-events: auto;
+            transition: opacity 0.4s ease, visibility 0s linear 0s;
         }
 
         .modal-card {
@@ -332,14 +335,17 @@
             display: flex;
             flex-direction: column;
             opacity: 0;
+            visibility: hidden;
             pointer-events: none;
-            transition: all 0.4s;
+            transition: opacity 0.4s ease, transform 0.4s, visibility 0s linear 0.4s;
         }
 
         #content-panel.active {
             opacity: 1;
+            visibility: visible;
             pointer-events: auto;
             transform: translate(-50%, -50%) scale(1);
+            transition: opacity 0.4s ease, transform 0.4s, visibility 0s linear 0s;
         }
 
         .close-btn {

commit b99bcd0949d7dfc71ac05f35c1b599df9627fbdb
Author: federicodb <vivasantarosa@gmail.com>
Date:   Fri Apr 3 17:40:30 2026 +0200

    fix(mobile): Sblocco zoom su index, UX touch migliorata e fix build.py parsing

diff --git a/index.html b/index.html
index 87e64d0..dc387b6 100644
--- a/index.html
+++ b/index.html
@@ -3,8 +3,7 @@
 
 <head>
     <meta charset="UTF-8">
-    <meta name="viewport"
-        content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Laboratorio Matematico Orfini | Immersive Hub</title>
 
     <link rel="preconnect" href="https://fonts.googleapis.com">
@@ -183,11 +182,20 @@
             pointer-events: auto;
         }
 
-        .btn-icon:hover {
+        @media (hover: hover) and (pointer: fine) {
+            .btn-icon:hover {
+                background: var(--primary);
+                color: black;
+                border-color: var(--primary);
+                transform: translateY(-2px);
+            }
+        }
+
+        .btn-icon:active {
             background: var(--primary);
             color: black;
             border-color: var(--primary);
-            transform: translateY(-2px);
+            transform: scale(0.95);
         }
 
         .btn-icon svg {
@@ -218,6 +226,17 @@
             box-shadow: 0 4px 15px rgba(255, 64, 129, 0.4);
         }
 
+        @media (hover: hover) and (pointer: fine) {
+            #btn-tags:hover {
+                transform: scale(1.1);
+                box-shadow: 0 6px 20px rgba(255, 64, 129, 0.6);
+            }
+        }
+
+        #btn-tags:active {
+            transform: scale(0.95);
+        }
+
         /* --- OVERLAYS --- */
         .overlay-full {
             position: fixed;
@@ -364,10 +383,17 @@
             cursor: pointer;
         }
 
-        .project-card:hover {
+        @media (hover: hover) and (pointer: fine) {
+            .project-card:hover {
+                border-color: var(--primary);
+                transform: translateY(-5px);
+                background: rgba(255, 255, 255, 0.06);
+            }
+        }
+
+        .project-card:active {
             border-color: var(--primary);
-            transform: translateY(-5px);
-            background: rgba(255, 255, 255, 0.06);
+            transform: scale(0.98);
         }
 
         .card-bg {
@@ -463,11 +489,35 @@
         @media (max-width: 600px) {
             .list-grid {
                 grid-template-columns: 1fr;
+                gap: 16px;
             }
 
             .modal-card {
+                padding: 1.5rem 1rem;
+                border-radius: 20px;
+            }
+
+            .manifesto-grid {
+                grid-template-columns: 1fr;
+            }
+
+            #content-panel {
+                width: 95%;
+                max-height: 90vh;
+            }
+
+            .panel-header {
                 padding: 1.5rem;
             }
+
+            .panel-scroll {
+                padding: 1.5rem;
+            }
+
+            #search-input {
+                font-size: 1rem;
+                padding: 1rem;
+            }
         }
     </style>
 </head>

commit 778aa06e340b7a212c7479038172c9b6fe636726
Author: federicodb <vivasantarosa@gmail.com>
Date:   Wed Mar 4 08:38:22 2026 +0100

    ristabilito mcs, cambio script update, niente tema condiviso, troppo casino

diff --git a/index.html b/index.html
index c210ef4..87e64d0 100644
--- a/index.html
+++ b/index.html
@@ -33,7 +33,7 @@
             }
         });
     </script>
-    <script src="database.js"></script>
+    <script src="database.js?v=4"></script>
 
     <style>
         :root {
@@ -609,8 +609,8 @@
     </div>
 
     <script type="module">
-        import { GalaxyScene } from './content/assets/js/modules/GalaxyScene.js';
-        import { UIManager } from './content/assets/js/modules/UIManager.js';
+        import { GalaxyScene } from './content/assets/js/modules/GalaxyScene.js?v=4';
+        import { UIManager } from './content/assets/js/modules/UIManager.js?v=4';
 
         function getUniqueTags(data) {
             const counts = {};

commit 2ef19e4571281ea9bdc67be9c5c47745510833fa
Author: federicodb <vivasantarosa@gmail.com>
Date:   Mon Dec 22 14:24:22 2025 +0100

    versione 3d, aggiornamento appz

diff --git a/index.html b/index.html
index 4dc4f47..c210ef4 100644
--- a/index.html
+++ b/index.html
@@ -1,57 +1,18 @@
 <!DOCTYPE html>
 <html lang="it" data-theme="dark">
+
 <head>
     <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
-    <meta name="theme-color" content="#0061a4"> <!-- Colore barra Android/iOS -->
-    
-    <!-- 1. SEO PRIMARIO -->
-    <title>Laboratorio Matematico Orfini Foligno | MeltingMath</title>
-    <meta name="description" content="Hub digitale didattico dell'Ist. Professionale Orfini. Simulazioni interattive, 3D printing e risorse multimediali per Matematica e Fisica. Scuola Pubblica Open Source.">
-    <meta name="keywords" content="Matematica, Fisica, Laboratorio, Orfini, Foligno, MeltingMath, 3D Print, Didattica, Open Source, Scuola Pubblica">
-    <meta name="author" content="Prof. Federico DB">
-    <meta name="robots" content="index, follow">
-    <link rel="canonical" href="https://federicodb.github.io/">
-
-    <!-- 2. OPEN GRAPH / FACEBOOK / INSTAGRAM / WHATSAPP -->
-    <meta property="og:type" content="website">
-    <meta property="og:url" content="https://federicodb.github.io/">
-    <meta property="og:title" content="Laboratorio Matematico Orfini | MeltingMath">
-    <meta property="og:description" content="Dove la Matematica diventa tangibile. Esplora simulazioni 3D, giochi e infografiche. Progetto Open Source della Scuola Pubblica.">
-    <meta property="og:image" content="https://federicodb.github.io/assets/social_share_cover.jpg"> <!-- TODO: Creare questa immagine -->
-    <meta property="og:locale" content="it_IT">
-    <meta property="og:site_name" content="Orfini Math Lab">
-
-    <!-- 3. TWITTER / X -->
-    <meta name="twitter:card" content="summary_large_image">
-    <meta name="twitter:site" content="@meltingmath">
-    <meta name="twitter:creator" content="@meltingmath">
-    <meta name="twitter:title" content="Laboratorio Matematico Orfini">
-    <meta name="twitter:description" content="Didattica laboratoriale e creativa. Matematica, Stampa 3D e WebApp.">
-    <meta name="twitter:image" content="https://federicodb.github.io/assets/social_share_cover.jpg">
-
-    <!-- 4. SCHEMA.ORG JSON-LD (Per Google) -->
-    <script type="application/ld+json">
-    {
-      "@context": "https://schema.org",
-      "@type": "EducationalOrganization",
-      "name": "Laboratorio Matematico Orfini Foligno",
-      "url": "https://federicodb.github.io/",
-      "logo": "https://federicodb.github.io/assets/logo_orfini.png",
-      "sameAs": [
-        "https://instagram.com/meltingmath",
-        "https://github.com/federicodb"
-      ],
-      "description": "Hub digitale per la didattica laboratoriale della Matematica e Fisica.",
-      "founder": {
-        "@type": "Person",
-        "name": "Prof. Federico DB"
-      },
-      "nonProfitStatus": "PublicSchool"
-    }
-    </script>
-    
-    <!-- Three.js Import Map -->
+    <meta name="viewport"
+        content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
+    <title>Laboratorio Matematico Orfini | Immersive Hub</title>
+
+    <link rel="preconnect" href="https://fonts.googleapis.com">
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+    <link
+        href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Amatic+SC:wght@700&family=Caveat:wght@700&family=Cinzel:wght@700&family=Oswald:wght@500&family=Permanent+Marker&family=Playfair+Display:ital,wght@1,600&family=Press+Start+2P&family=Righteous&family=Space+Mono:ital,wght@0,700;1,700&family=Inter:wght@400;700&family=Syne:wght@700;800&display=swap"
+        rel="stylesheet">
+
     <script type="importmap">
         {
             "imports": {
@@ -60,911 +21,665 @@
             }
         }
     </script>
-
-    <!-- Font Collection per la Cloud -->
-    <link rel="preconnect" href="https://fonts.googleapis.com">
-    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
-    <link href="https://fonts.googleapis.com/css2?family=Bangers&family=Fira+Code:wght@500&family=Inter:wght@400;700;900&family=Pacifico&family=Playfair+Display:ital,wght@1,600&display=swap" rel="stylesheet">
+    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
+    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
+    <script src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"></script>
+    <script>
+        window.addEventListener('load', () => {
+            if (typeof katex === 'undefined') {
+                console.error("[MathEngine] CRITICAL: KaTeX library failed to load from CDN.");
+            } else {
+                console.log("[MathEngine] KaTeX Ready.");
+            }
+        });
+    </script>
+    <script src="database.js"></script>
 
     <style>
-        /* --- MATERIAL DESIGN 3 TOKENS --- */
         :root {
-            /* Palette Material 3 (Base) */
-            --md-sys-color-background: #fdfcff;
-            --md-sys-color-on-background: #1a1c1e;
-            --md-sys-color-surface: #fdfcff;
-            --md-sys-color-surface-container: #f0f4f8; 
-            --md-sys-color-surface-container-high: #e2e8f0;
-            --md-sys-color-surface-variant: #e1e2ec;
-            --md-sys-color-on-surface-variant: #44474f;
-            
-            --md-sys-color-primary: #0061a4;
-            --md-sys-color-on-primary: #ffffff;
-            --md-sys-color-secondary-container: #dbe4f9;
-            --md-sys-color-on-secondary-container: #171c22;
-            
-            --md-sys-color-outline: #74777f;
-            --md-sys-color-outline-variant: #c4c7c5;
-
-            /* Spaziature e Forme */
-            --md-shape-corner-large: 24px;
-            --md-shape-corner-medium: 16px;
-            --md-shape-corner-full: 999px;
-            
-            /* Palette Dinamica Card */
-            --card-blue: #d3e3fd; --card-on-blue: #041e49;
-            --card-green: #c4eed0; --card-on-green: #07210e;
-            --card-red: #f9dedc; --card-on-red: #410e0b;
-            --card-yellow: #fdf2b4; --card-on-yellow: #261d00;
-            --card-purple: #e9ddff; --card-on-purple: #21005d;
-            --card-teal: #bce9eb; --card-on-teal: #001f20;
-
-            /* Brand Colors */
-            --instagram-grad: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
-        }
-
-        [data-theme="dark"] {
-            --md-sys-color-background: #000000; /* AMOLED Black */
-            --md-sys-color-on-background: #e2e2e6;
-            --md-sys-color-surface: #111111;
-            --md-sys-color-surface-container: #1c1c1c; 
-            --md-sys-color-surface-container-high: #2d2d2d;
-            --md-sys-color-surface-variant: #44474f;
-            --md-sys-color-on-surface-variant: #c4c7c5;
-            
-            --md-sys-color-primary: #9ecaff;
-            --md-sys-color-on-primary: #003258;
-            --md-sys-color-secondary-container: #33445a;
-            --md-sys-color-on-secondary-container: #dbe4f9;
-            
-            --md-sys-color-outline: #8e918f;
-            --md-sys-color-outline-variant: #444746;
-
-            /* Palette Dark */
-            --card-blue: #0842a0; --card-on-blue: #d3e3fd;
-            --card-green: #0f5223; --card-on-green: #c4eed0;
-            --card-red: #93000a; --card-on-red: #ffdad6;
-            --card-yellow: #5d4200; --card-on-yellow: #fff392;
-            --card-purple: #55339e; --card-on-purple: #e9ddff;
-            --card-teal: #004f50; --card-on-teal: #bce9eb;
-        }
-
-        /* --- RESET & TYPOGRAPHY --- */
-        html { scroll-behavior: smooth; }
-        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
+            --primary: #00bcd4;
+            --secondary: #ff4081;
+            --bg-dark: #050510;
+            --glass: rgba(10, 20, 40, 0.75);
+            --glass-solid: rgba(10, 20, 40, 0.95);
+            --border: rgba(255, 255, 255, 0.1);
+            --font-ui: 'Inter', sans-serif;
+        }
 
-        body {
-            margin: 0; padding: 0;
-            background-color: var(--md-sys-color-background);
-            color: var(--md-sys-color-on-background);
-            font-family: 'Inter', sans-serif;
-            min-height: 100dvh;
-            display: flex; flex-direction: column;
-            transition: background-color 0.4s cubic-bezier(0.2, 0, 0, 1), color 0.4s;
-            overflow-x: hidden;
+        /* --- 3D TAG FONTS --- */
+        .font-1 {
+            font-family: 'Caveat', cursive !important;
+            font-size: 1.3em;
+        }
+
+        .font-2 {
+            font-family: 'Space Mono', monospace !important;
+            letter-spacing: -0.05em;
+            font-weight: 700;
+        }
+
+        .font-3 {
+            font-family: 'Abril Fatface', serif !important;
+        }
+
+        .font-4 {
+            font-family: 'Permanent Marker', cursive !important;
+            transform: rotate(-2deg);
         }
 
-        /* --- HEADER & HERO --- */
-        header { padding: 2rem 1.5rem; max-width: 1200px; margin: 0 auto; width: 100%; }
+        .font-5 {
+            font-family: 'Righteous', cursive !important;
+        }
+
+        .font-6 {
+            font-family: 'Cinzel', serif !important;
+            font-weight: 700;
+        }
+
+        .font-7 {
+            font-family: 'Amatic SC', cursive !important;
+            font-size: 1.5em;
+            font-weight: 700;
+        }
+
+        .font-8 {
+            font-family: 'Oswald', sans-serif !important;
+            text-transform: uppercase;
+        }
+
+        .font-9 {
+            font-family: 'Press Start 2P', cursive !important;
+            font-size: 0.7em;
+            color: #0f0;
+        }
+
+        .font-10 {
+            font-family: 'Playfair Display', serif !important;
+            font-style: italic;
+        }
+
+        * {
+            box-sizing: border-box;
+            -webkit-tap-highlight-color: transparent;
+        }
 
-        .top-bar {
-            display: flex; 
+        body {
+            margin: 0;
+            overflow: hidden;
+            background: var(--bg-dark);
+            font-family: var(--font-ui);
+            color: white;
+            width: 100vw;
+            height: 100dvh;
+            -webkit-font-smoothing: antialiased;
+            -moz-osx-font-smoothing: grayscale;
+            text-rendering: optimizeLegibility;
+        }
+
+        #scene-container {
+            position: fixed;
+            inset: 0;
+            z-index: 0;
+            outline: none;
+            touch-action: none;
+        }
+
+        /* --- HUD --- */
+        #ui-hud {
+            position: absolute;
+            inset: 0;
+            z-index: 10;
+            pointer-events: none;
+            display: flex;
             flex-direction: column;
+            justify-content: space-between;
+            padding: 1.5rem;
+        }
+
+        .hud-header {
+            display: flex;
+            justify-content: space-between;
+            align-items: flex-start;
+            pointer-events: auto;
+        }
+
+        .hud-actions {
+            display: flex;
+            gap: 0.8rem;
             align-items: center;
-            text-align: center;
-            margin-bottom: 3rem;
+        }
+
+        .brand-orfini {
+            font-family: 'Syne', sans-serif;
+            font-size: clamp(1.5rem, 5vw, 3rem);
+            font-weight: 800;
+            letter-spacing: -0.02em;
+            line-height: 0.8;
+            color: #fff;
+            text-transform: uppercase;
+        }
+
+        .brand-sub {
+            font-family: 'Space Mono', monospace;
+            font-size: 0.7rem;
+            color: var(--primary);
+            letter-spacing: 0.3em;
+            margin-top: 0.4rem;
+        }
+
+        .btn-icon {
+            background: rgba(255, 255, 255, 0.05);
+            border: 1px solid var(--border);
+            color: white;
+            width: 42px;
+            height: 44px;
+            border-radius: 12px;
+            cursor: pointer;
+            display: flex;
+            align-items: center;
+            justify-content: center;
+            backdrop-filter: blur(10px);
+            transition: all 0.3s;
+            pointer-events: auto;
+        }
+
+        .btn-icon:hover {
+            background: var(--primary);
+            color: black;
+            border-color: var(--primary);
+            transform: translateY(-2px);
+        }
+
+        .btn-icon svg {
+            width: 20px;
+            height: 20px;
+            stroke-width: 2.5;
+        }
+
+        #btn-tags {
+            position: fixed;
+            bottom: 20px;
+            left: 20px;
+            z-index: 50;
+            width: 50px;
+            height: 50px;
+            border-radius: 50%;
+            background: var(--secondary);
+            border: none;
+            color: white;
+            display: flex;
+            align-items: center;
+            justify-content: center;
+            cursor: pointer;
+            backdrop-filter: blur(10px);
+            transition: all 0.3s;
+            font-size: 1.4rem;
+            pointer-events: auto;
+            box-shadow: 0 4px 15px rgba(255, 64, 129, 0.4);
+        }
+
+        /* --- OVERLAYS --- */
+        .overlay-full {
+            position: fixed;
+            inset: 0;
+            z-index: 100;
+            background: rgba(5, 5, 15, 0.95);
+            backdrop-filter: blur(20px);
+            display: flex;
+            align-items: center;
+            justify-content: flex-start;
+            flex-direction: column;
+            opacity: 0;
+            pointer-events: none;
+            transition: all 0.4s ease;
+            overflow-y: auto;
+            padding: 2rem;
+        }
+
+        .overlay-full.active {
+            opacity: 1;
+            pointer-events: auto;
+        }
+
+        .modal-card {
+            background: var(--glass-solid);
+            border: 1px solid var(--border);
+            width: 95%;
+            max-width: 900px;
+            margin: auto;
+            border-radius: 32px;
+            padding: 3rem;
             position: relative;
+            transform: scale(0.9) translateY(30px);
+            transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
+        }
+
+        .overlay-full.active .modal-card {
+            transform: scale(1) translateY(0);
+        }
+
+        .manifesto-grid {
+            display: grid;
+            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
+            gap: 1.5rem;
+            margin-top: 2rem;
+        }
+
+        .manifesto-item {
+            background: rgba(255, 255, 255, 0.03);
+            padding: 1.5rem;
+            border-radius: 20px;
+            border: 1px solid rgba(255, 255, 255, 0.05);
         }
 
-        h1 {
-            font-family: 'Inter', sans-serif;
-            font-size: clamp(2rem, 6vw, 4rem); /* Responsive Huge Title */
-            font-weight: 900; 
-            line-height: 1; 
+        .manifesto-item h4 {
+            color: var(--primary);
             margin: 0 0 0.5rem 0;
-            letter-spacing: -2px;
+            font-family: 'Syne', sans-serif;
             text-transform: uppercase;
-            background: linear-gradient(135deg, var(--md-sys-color-on-background) 30%, var(--md-sys-color-primary));
-            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
+            font-size: 0.9rem;
+        }
+
+        .manifesto-item p {
+            font-size: 0.85rem;
+            line-height: 1.5;
+            color: #aaa;
+            margin: 0;
+        }
+
+        /* --- SEARCH GRID --- */
+        .list-grid {
+            display: grid;
+            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
+            gap: 24px;
+            width: 100%;
+            max-width: 1400px;
+            margin: 0 auto;
+            padding-bottom: 5rem;
+        }
+
+        #content-panel {
+            position: fixed;
+            top: 50%;
+            left: 50%;
+            transform: translate(-50%, -50%) scale(0.9);
+            width: 90%;
+            max-width: 800px;
+            max-height: 85vh;
+            background: var(--glass-solid);
+            border: 1px solid var(--border);
+            border-radius: 32px;
+            z-index: 60;
+            display: flex;
+            flex-direction: column;
+            opacity: 0;
+            pointer-events: none;
+            transition: all 0.4s;
         }
-        
-        .subtitle {
-            font-size: 1rem; 
-            color: var(--md-sys-color-outline); 
-            text-transform: uppercase; 
-            letter-spacing: 3px; 
-            font-weight: 600;
-            margin-top: 0.5rem;
+
+        #content-panel.active {
+            opacity: 1;
+            pointer-events: auto;
+            transform: translate(-50%, -50%) scale(1);
         }
 
-        .theme-btn {
-            position: absolute;
-            top: 0; right: 0;
-            width: 48px; height: 48px; border-radius: 50%;
-            border: 1px solid var(--md-sys-color-outline-variant);
-            background: transparent; color: var(--md-sys-color-on-background);
-            display: flex; align-items: center; justify-content: center;
-            cursor: pointer; transition: all 0.2s; font-size: 1.2rem;
-        }
-        .theme-btn:hover { background-color: var(--md-sys-color-surface-container-high); transform: rotate(15deg); }
-
-        @media (max-width: 768px) {
-            .theme-btn { position: relative; margin-left: auto; margin-bottom: 1rem; top: auto; right: auto; }
-            .top-bar { align-items: stretch; text-align: center; }
-        }
-
-        /* --- 3D CLOUD CONTAINER --- */
-        #cloud-wrapper {
-            width: 100%; height: 320px; /* Altezza aumentata per il 3D */
-            background-color: transparent; /* Sfondo trasparente per ereditare dal body o impostato via JS */
-            border-radius: var(--md-shape-corner-large);
-            margin-bottom: 2rem;
-            position: relative; overflow: hidden;
-            /* box-shadow rimosso per integrarsi con lo sfondo */
-            padding: 0; cursor: grab;
-        }
-        #cloud-wrapper:active { cursor: grabbing; }
-        
-        #cloud-overlay {
-            position: absolute; bottom: 15px; right: 20px;
-            pointer-events: none; opacity: 0.5;
-            font-size: 0.7rem; color: var(--md-sys-color-outline);
-            text-transform: uppercase; font-weight: bold; letter-spacing: 1px;
-        }
-
-        /* --- FILTRI --- */
-        .filters-container {
-            position: sticky; top: 0; z-index: 50;
-            background: var(--md-sys-color-background);
-            padding: 0.5rem 0 1rem;
-            border-bottom: 1px solid var(--md-sys-color-outline-variant);
-            margin-bottom: 1.5rem;
-            opacity: 0.98;
-        }
-
-        .filters {
-            display: flex; gap: 0.5rem; overflow-x: auto;
-            padding: 0 1.5rem; max-width: 1200px; margin: 0 auto;
-            scrollbar-width: none; -webkit-overflow-scrolling: touch;
-        }
-        .filters::-webkit-scrollbar { display: none; }
-
-        .chip {
-            background-color: var(--md-sys-color-surface);
-            border: 1px solid var(--md-sys-color-outline-variant);
-            color: var(--md-sys-color-on-background);
-            padding: 0.5rem 1rem; border-radius: 8px;
-            font-size: 0.85rem; font-weight: 500;
-            white-space: nowrap; cursor: pointer; user-select: none;
-            transition: all 0.2s;
-        }
-        .chip:hover { background-color: var(--md-sys-color-surface-container-high); }
-        .chip.active {
-            background-color: var(--md-sys-color-primary);
-            color: var(--md-sys-color-on-primary);
-            border-color: var(--md-sys-color-primary);
-            box-shadow: 0 2px 6px rgba(0,0,0,0.2);
-        }
-
-        /* --- GRIGLIA CARD --- */
-        main { padding: 0 1.5rem 4rem; max-width: 1200px; margin: 0 auto; flex: 1; width: 100%; }
-
-        .grid {
-            display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem;
-        }
-
-        .card {
-            background-color: var(--card-bg, var(--md-sys-color-surface-container));
-            color: var(--card-fg, var(--md-sys-color-on-background));
-            border-radius: var(--md-shape-corner-large);
-            padding: 1.5rem; text-decoration: none;
-            display: flex; flex-direction: column;
-            transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s;
-            position: relative; overflow: hidden;
-            border: 1px solid transparent;
-        }
-        @media (hover: hover) {
-            .card:hover { transform: translateY(-6px) scale(1.01); box-shadow: 0 12px 24px -8px rgba(0,0,0,0.3); z-index: 10; }
-        }
-
-        .card-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 1rem; }
-        .card-emoji { font-size: 3rem; filter: drop-shadow(0 4px 6px rgba(0,0,0,0.1)); transition: transform 0.3s; }
-        .card:hover .card-emoji { transform: scale(1.1) rotate(5deg); }
-        
-        .card-date {
-            font-size: 0.75rem; font-weight: 600; opacity: 0.6;
-            background: rgba(0,0,0,0.1); padding: 4px 8px; border-radius: 6px;
-            font-family: monospace;
-        }
-        [data-theme="dark"] .card-date { background: rgba(255,255,255,0.1); }
-
-        .card h3 { font-size: 1.4rem; font-weight: 700; margin: 0 0 0.5rem 0; line-height: 1.2; letter-spacing: -0.01em; }
-        .card p { font-size: 0.95rem; opacity: 0.8; margin: 0 0 1.5rem 0; line-height: 1.5; flex-grow: 1; font-weight: 400; }
-        .card-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: auto; }
-        .tag-pill {
-            font-size: 0.7rem; font-weight: 600; padding: 4px 10px; border-radius: 50px;
-            background-color: rgba(255,255,255,0.2); color: inherit; border: 1px solid rgba(255,255,255,0.1);
-        }
-
-        /* --- FOOTER --- */
-        .site-footer {
-            background-color: var(--md-sys-color-surface-container);
-            border-top: 1px solid var(--md-sys-color-outline-variant);
-            padding: 3rem 1.5rem; margin-top: auto;
-        }
-        
-        .footer-content {
-            max-width: 1200px; margin: 0 auto;
-            display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
-            gap: 2rem;
-        }
-
-        .footer-section h4 {
-            font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.1em;
-            color: var(--md-sys-color-primary); margin-bottom: 1rem; font-weight: 800;
-        }
-
-        .footer-section p {
-            font-size: 0.95rem; line-height: 1.6; color: var(--md-sys-color-on-surface-variant); margin-bottom: 1rem;
-        }
-
-        .footer-link {
-            display: inline-flex; items-center; gap: 0.5rem;
-            color: var(--md-sys-color-on-background); text-decoration: none;
-            font-weight: 600; font-size: 0.95rem; transition: color 0.2s;
-        }
-        .footer-link:hover { color: var(--md-sys-color-primary); }
-
-        .mm-card {
-            background: var(--md-sys-color-surface);
-            border: 1px solid var(--md-sys-color-outline-variant);
-            border-radius: var(--md-shape-corner-medium);
-            padding: 1.5rem; display: flex; align-items: center; gap: 1rem;
-            transition: transform 0.2s; text-decoration: none; color: inherit;
-        }
-        .mm-card:hover { transform: translateY(-3px); border-color: #d62976; }
-        
-        .mm-icon {
-            width: 48px; height: 48px; border-radius: 12px;
-            background: var(--instagram-grad);
-            display: flex; align-items: center; justify-content: center;
-            color: white; font-size: 1.5rem; flex-shrink: 0;
-        }
-
-        #empty-state { grid-column: 1 / -1; text-align: center; padding: 4rem 1rem; color: var(--md-sys-color-outline); }
-        #reset-filter {
-            display: none; margin: 0 0 1rem auto; background: none; border: 1px solid var(--md-sys-color-outline);
-            color: var(--md-sys-color-on-background); padding: 6px 12px; border-radius: 8px; cursor: pointer; font-size: 0.8rem;
-        }
-
-        /* --- MODAL STYLES --- */
-        .modal-overlay {
-            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
-            background: rgba(0,0,0,0.85); backdrop-filter: blur(5px);
-            z-index: 1000; opacity: 0; pointer-events: none;
-            display: flex; align-items: center; justify-content: center;
-            transition: opacity 0.3s ease;
+        .close-btn {
+            background: none;
+            border: none;
+            color: white;
+            font-size: 2.5rem;
+            cursor: pointer;
+            line-height: 1;
         }
-        .modal-overlay.active { opacity: 1; pointer-events: auto; }
-
-        .modal-container {
-            background: var(--md-sys-color-surface);
-            color: var(--md-sys-color-on-background);
-            width: 90%; max-width: 800px; max-height: 90vh;
-            border-radius: var(--md-shape-corner-large);
-            position: relative; overflow-y: auto;
-            box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5);
-            transform: scale(0.95); transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
-            display: flex; flex-direction: column;
-        }
-        .modal-overlay.active .modal-container { transform: scale(1); }
-
-        .modal-close {
-            position: absolute; top: 1rem; right: 1rem;
-            background: rgba(0,0,0,0.5); border: none; color: white;
-            width: 32px; height: 32px; border-radius: 50%;
-            cursor: pointer; z-index: 10; font-weight: bold;
-        }
-        
-        .modal-content {
-            background: black; /* Per video/img */
-            min-height: 300px; display: flex; align-items: center; justify-content: center;
-            border-radius: var(--md-shape-corner-large) var(--md-shape-corner-large) 0 0;
+
+        .panel-header {
+            padding: 2rem;
+            border-bottom: 1px solid var(--border);
+            display: flex;
+            justify-content: space-between;
+            align-items: center;
+        }
+
+        .panel-scroll {
+            flex: 1;
+            overflow-y: auto;
+            padding: 2rem;
+            display: flex;
+            flex-direction: column;
+            gap: 1.5rem;
+        }
+
+        /* CARDS */
+        .project-card {
+            background: rgba(255, 255, 255, 0.03);
+            border: 1px solid var(--border);
+            border-radius: 20px;
+            padding: 24px;
+            position: relative;
             overflow: hidden;
+            display: flex;
+            flex-direction: column;
+            min-height: 320px;
+            transition: all 0.3s;
+            cursor: pointer;
+        }
+
+        .project-card:hover {
+            border-color: var(--primary);
+            transform: translateY(-5px);
+            background: rgba(255, 255, 255, 0.06);
         }
 
-        .modal-meta { padding: 1.5rem; }
-        .modal-meta h3 { margin: 0 0 0.5rem 0; font-size: 1.5rem; }
-        .modal-meta p { margin: 0; opacity: 0.8; line-height: 1.5; }
+        .card-bg {
+            position: absolute;
+            inset: 0;
+            z-index: 0;
+            filter: blur(10px) saturate(0);
+            opacity: 0.3;
+            background-size: cover;
+            background-position: center;
+        }
 
-        .type-badge {
-            font-size: 0.65rem; font-weight: 800; letter-spacing: 0.05em;
-            padding: 3px 6px; border-radius: 4px;
-            background: rgba(0,0,0,0.2); color: inherit;
-            margin-left: auto; align-self: center;
+        .card-content-wrap {
+            position: relative;
+            z-index: 1;
+            height: 100%;
+            display: flex;
+            flex-direction: column;
         }
 
-        /* MARKDOWN STYLES */
-        .markdown-body { font-size: 1rem; line-height: 1.7; color: var(--md-sys-color-on-surface-variant); }
-        .markdown-body h1, .markdown-body h2, .markdown-body h3 { 
-            color: var(--md-sys-color-on-background); margin-top: 1.5em; margin-bottom: 0.5em; font-weight: 700; 
+        .project-card h3 {
+            font-family: 'Syne', sans-serif;
+            font-size: 1.6rem;
+            margin: 0.5rem 0;
+            color: #fff;
+        }
+
+        .project-card p {
+            font-size: 1rem;
+            line-height: 1.6;
+            color: #bbb;
+            flex-grow: 1;
+        }
+
+        .mini-tag {
+            font-family: 'Space Mono';
+            font-size: 0.7rem;
+            padding: 6px 12px;
+            background: rgba(0, 188, 212, 0.1);
+            color: var(--primary);
+            border-radius: 50px;
+            margin: 0 6px 6px 0;
+            display: inline-block;
+        }
+
+        /* Tag Content for 3D Nodes */
+        .tag-content {
+            display: inline-block;
+            font-weight: 700;
+            color: white;
+            padding: 4px 8px;
+            white-space: nowrap;
+            cursor: pointer;
+            pointer-events: auto;
+            will-change: transform, opacity;
+            transition: color 0.2s;
+            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
+            backface-visibility: hidden;
+        }
+
+        /* .tag-content:hover managed by GalaxyScene.js for synced zoom/glow */
+
+        /* --- MATH DEBRIS TRANSITION STYLES --- */
+        /* --- MATH DEBRIS HIGH FIDELITY --- */
+        .math-debris {
+            display: flex;
+            align-items: center;
+            justify-content: center;
+            pointer-events: none;
+            will-change: transform, opacity;
+            contain: layout style;
         }
-        .markdown-body h1 { font-size: 1.8rem; border-bottom: 1px solid var(--md-sys-color-outline-variant); padding-bottom: 0.3em; }
-        .markdown-body h2 { font-size: 1.4rem; }
-        .markdown-body h3 { font-size: 1.2rem; }
-        .markdown-body ul, .markdown-body ol { padding-left: 1.5em; margin-bottom: 1em; }
-        .markdown-body li { margin-bottom: 0.3em; }
-        .markdown-body blockquote { 
-            border-left: 4px solid var(--md-sys-color-primary); 
-            background: var(--md-sys-color-surface-container-high);
-            padding: 0.5em 1em; margin: 1em 0; font-style: italic;
+
+        .debris-hex {
+            position: absolute;
+            width: 6px;
+            height: 6px;
+            background: rgba(255, 255, 255, 0.3);
+            clip-path: polygon(25% 0%, 75% 0%, 100% 50%, 75% 100%, 25% 100%, 0% 50%);
         }
-        .markdown-body code { 
-            background: var(--md-sys-color-surface-container-high); 
-            padding: 0.2em 0.4em; border-radius: 4px; font-family: monospace; font-size: 0.9em;
+
+        .debris-sym {
+            position: relative;
+            z-index: 1;
+            color: #fff;
+            white-space: nowrap;
+            transition: opacity 0.3s;
+            text-shadow: 0 0 8px rgba(0, 188, 212, 0.4);
+            font-family: 'KaTeX_Main', serif;
+            will-change: opacity, transform;
         }
-        .markdown-body ul.contains-task-list { list-style: none; padding-left: 0; }
-        .markdown-body input[type="checkbox"] { margin-right: 0.5em; transform: scale(1.2); cursor: pointer; }
 
         @media (max-width: 600px) {
-            h1 { font-size: 1.5rem; }
-            .grid { grid-template-columns: 1fr; }
-            .footer-content { grid-template-columns: 1fr; }
+            .list-grid {
+                grid-template-columns: 1fr;
+            }
+
+            .modal-card {
+                padding: 1.5rem;
+            }
         }
     </style>
-    <!-- Marked.js for Markdown Rendering -->
-    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
 </head>
+
 <body>
 
-    <header>
-        <div class="top-bar">
-            <h1>Laboratorio Matematico<br>Orfini Foligno</h1>
-            <p class="subtitle">Didattica Interattiva Open Source</p>
-            <button class="theme-btn" onclick="toggleTheme()" aria-label="Cambia tema">
-                <span id="theme-icon">🌙</span>
-            </button>
-        </div>
-        
-        <div id="cloud-wrapper">
-            <!-- Three.js Canvas will be injected here -->
-            <div id="cloud-overlay">Ruota &bull; Zooma &bull; Esplora</div>
-        </div>
-    </header>
+    <div id="scene-container"></div>
 
-    <div class="filters-container">
-        <div class="filters" id="filterBar">
-            <button class="chip active" onclick="applyFilter('all')">Tutti</button>
-            <button class="chip" onclick="applyFilter('Linkografia')" style="border-color:var(--md-sys-color-primary); color:var(--md-sys-color-primary); font-weight:800;">📚 Linkografia</button>
-            <!-- Tags generati dinamicamente -->
+    <div id="ui-hud">
+        <div class="hud-header">
+            <div class="brand-container">
+                <div class="brand-orfini">ORFINI</div>
+                <div class="brand-sub">MATH GALAXY</div>
+            </div>
+            <div class="hud-actions">
+                <button class="btn-icon" id="btn-about" title="Manifesto Didattico">
+                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
+                        <polyline points="4 17 10 11 4 5"></polyline>
+                        <line x1="12" y1="19" x2="20" y2="19"></line>
+                    </svg>
+                </button>
+                <button class="btn-icon" id="btn-search" title="Cerca Argomenti">
+                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
+                        <circle cx="11" cy="11" r="8"></circle>
+                        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
+                    </svg>
+                </button>
+                <button class="btn-icon" id="btn-reset" title="Reset Camera">
+                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
+                        <path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 12"></path>
+                        <path d="M3 3v9h9"></path>
+                    </svg>
+                </button>
+            </div>
         </div>
     </div>
 
-    <main>
-        <div style="display: flex; justify-content: flex-end;">
-            <button id="reset-filter" onclick="applyFilter('all')">✕ Rimuovi Filtri</button>
-        </div>
-
-        <div class="grid" id="grid">
-            <!-- Cards generate dinamicamente -->
-        </div>
-        
-        <div id="empty-state">
-            <div style="font-size:3rem; margin-bottom:1rem; opacity:0.5;">🔭</div>
-            <p>Nessuna attività trovata per questo filtro.</p>
-        </div>
-    </main>
-
-    <!-- FOOTER INFORMATIVO -->
-    <footer class="site-footer">
-        <div class="footer-content">
-            
-            <!-- SEZIONE CHI SONO -->
-            <div class="footer-section">
-                <h4>Chi Sono</h4>
-                <p>
-                    Sono il <strong>Prof. Federico DB</strong>, docente di Matematica presso l'Istituto Professionale Orfini di Foligno. 
-                    Il mio obiettivo è trasformare l'astrazione della Matematica in esperienza tangibile.
+    <button id="btn-tags" title="Mappa Tag">#</button>
+
+    <!-- MANIFESTO MODAL -->
+    <div id="about-overlay" class="overlay-full">
+        <div class="modal-card">
+            <button class="close-btn" id="btn-close-about"
+                style="position: absolute; top: 1.5rem; right: 1.5rem;">×</button>
+
+            <header style="text-align: center; margin-bottom: 3rem;">
+                <div
+                    style="width:100px; height:100px; background:linear-gradient(135deg, var(--primary), var(--secondary)); border-radius:24px; margin:0 auto 1.5rem; display:flex; align-items:center; justify-content:center; font-size:2.5rem; box-shadow: 0 10px 30px rgba(0,188,212,0.3);">
+                    👨‍🚀</div>
+                <h2 style="margin:0; font-family:'Syne'; font-size:2.5rem; font-weight:800;">Prof. Federico DB</h2>
+                <div
+                    style="font-family:'Space Mono'; color:var(--primary); font-size:0.9rem; text-transform:uppercase; letter-spacing:3px;">
+                    Creative Coder & Designer Didattico</div>
+            </header>
+
+            <div style="line-height:1.8; color:#eee;">
+                <p
+                    style="font-size:1.2rem; color:#fff; font-weight:600; margin-bottom:2rem; border-left:4px solid var(--secondary); padding-left:1.5rem;">
+                    "Trasformare il caos creativo in strumenti concreti, accessibili ed esteticamente coerenti."
                 </p>
-                <p>
-                    Ogni studente deve poter "toccare" i concetti scientifici per comprenderli davvero.
-                </p>
-            </div>
 
-            <!-- SEZIONE METODOLOGIA -->
-            <div class="footer-section">
-                <h4>Note Metodologiche</h4>
-                <p>
-                    Questo laboratorio segue le linee guida della <strong>Riforma degli Istituti Professionali (D.Lgs 61/2017)</strong>.
-                </p>
-                <ul style="padding-left: 1rem; color: var(--md-sys-color-on-surface-variant); font-size: 0.9rem; line-height: 1.6;">
-                    <li><strong>PBL:</strong> Apprendimento basato su progetti reali.</li>
-                    <li><strong>Gamification:</strong> L'errore è parte del gioco, non un fallimento.</li>
-                    <li><strong>Open Source:</strong> Codice accessibile e modificabile.</li>
-                </ul>
-            </div>
+                <p>Benvenuti nell'hub del <strong>Laboratorio Matematico Orfini</strong>. Un ecosistema basato sulla
+                    didattica laboratoriale e l'integrazione tecnologica.</p>
 
-            <!-- SEZIONE MELTINGMATH -->
-            <div class="footer-section">
-                <h4>Social & Community</h4>
-                <a href="https://instagram.com/meltingmath" target="_blank" class="mm-card">
-                    <div class="mm-icon">
-                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
+                <div class="manifesto-grid">
+                    <div class="manifesto-item">
+                        <h4>Gamification</h4>
+                        <p>Meccaniche di gioco, punti e feedback per potenziare la memoria di lavoro attraverso esercizi
+                            brevi e ripetizione spaziata.</p>
                     </div>
-                    <div>
-                        <div style="font-weight: 800; font-size: 1rem; color: var(--md-sys-color-primary);">MeltingMath</div>
-                        <div style="font-size: 0.8rem; color: var(--md-sys-color-outline);">Segui il progetto su Instagram</div>
+                    <div class="manifesto-item">
+                        <h4>Problem Based Learning</h4>
+                        <p>Matematica applicata a casi studio reali e progetti guidati (PBL) per sviluppare il pensiero
+                            critico.</p>
                     </div>
-                </a>
+                    <div class="manifesto-item">
+                        <h4>Visualizzazione 3D</h4>
+                        <p>Grafici interattivi, animazioni e modelli 3D parametrici per rendere tangibili concetti
+                            astratti.</p>
+                    </div>
+                    <div class="manifesto-item">
+                        <h4>Didattica Inclusiva</h4>
+                        <p>Supporti multisensoriali e strumenti digitali personalizzati per studenti con DSA/ADHD.</p>
+                    </div>
+                    <div class="manifesto-item">
+                        <h4>Pensiero Computazionale</h4>
+                        <p>Algoritmi, pseudocodice e logica di programmazione integrati in UDA moderne e
+                            multidisciplinari.</p>
+                    </div>
+                    <div class="manifesto-item">
+                        <h4>AI Generativa</h4>
+                        <p>Utilizzo strategico dell'AI per la creazione di contenuti e il perfezionamento dei processi
+                            iterativi.</p>
+                    </div>
+                    <div class="manifesto-item">
+                        <h4>Cultura Locale</h4>
+                        <p>Unione tra riferimenti globali tecnologici e radici del territorio umbro in un linguaggio
+                            esteticamente coerente.</p>
+                    </div>
+                    <div class="manifesto-item">
+                        <h4>Metacognizione</h4>
+                        <p>Approccio ironico e riflessivo per stimolare l'autovalutazione e la consapevolezza del
+                            proprio apprendimento.</p>
+                    </div>
+                </div>
             </div>
 
+            <footer
+                style="margin-top: 3rem; text-align: center; padding-top: 2rem; border-top: 1px solid var(--border);">
+                <a href="https://instagram.com/meltingmath" target="_blank" class="btn-icon"
+                    style="display:inline-flex; width:auto; padding:0 20px; border-radius:12px; text-decoration:none; gap:10px;">
+                    <span>📸</span> Segui @meltingmath
+                </a>
+            </footer>
         </div>
-        <div style="text-align: center; margin-top: 2rem; font-size: 0.75rem; color: var(--md-sys-color-outline);">
-            &copy; 2025 Laboratorio Matematico Orfini Foligno - Engineered by Orfini Math Lab Architect
-        </div>
-    </footer>
-
-    <!-- MODAL MULTIMEDIALE -->
-    <div id="media-modal" class="modal-overlay" onclick="closeModal()">
-        <div class="modal-container" onclick="event.stopPropagation()">
-            <button class="modal-close" onclick="closeModal()">✕</button>
-            <div id="modal-content" class="modal-content">
-                <!-- Contenuto iniettato dinamicamente -->
-            </div>
-            <div id="modal-meta" class="modal-meta">
-                <!-- Titolo e info -->
-            </div>
+    </div>
+
+    <!-- SEARCH OVERLAY -->
+    <div id="list-overlay" class="overlay-full">
+        <div style="width:100%; max-width:800px; margin-bottom:2rem; display:flex; gap:1rem;">
+            <input type="text" id="search-input" placeholder="Cerca algoritmi o concetti..."
+                style="flex:1; background:rgba(255,255,255,0.05); border:1px solid var(--border); color:white; padding:1.2rem; border-radius:16px; outline:none; font-size:1.2rem;">
+            <button class="btn-icon" id="btn-close-search" style="width:60px; height:60px;">×</button>
         </div>
+        <div class="list-grid" id="full-list-grid"></div>
     </div>
 
-    <!-- SCRIPT DATA (INLINE PER ANTEPRIMA) -->
-    <script>
-        const inlineDB = [
-            { title: "Math Underground: Mappa Funzioni", url: "content/apps/math_underground003_4el.html", excerpt: "Mappa concettuale interattiva in stile metropolitana per esplorare la classificazione delle funzioni analitiche.", tags: ["Matematica", "Analisi", "Funzioni", "4EL"], date: "2025-12-06", type: "app" },
-            { title: "Caccia all'Errore: Operazioni in Z", url: "content/apps/memory_1el_numeri_004.html", excerpt: "Missione di addestramento sui Numeri Interi (Z) e le Potenze. Trova le corrispondenze corrette.", tags: ["Matematica", "Aritmetica", "1EL"], date: "2025-10-20", type: "app" }
-        ];
-    </script>
+    <aside id="content-panel">
+        <div class="panel-header">
+            <div id="panel-title-text"
+                style="font-size:1.8rem; font-weight:800; font-family:'Syne'; color:var(--primary);">Tag</div>
+            <button class="close-btn" id="btn-close-panel">×</button>
+        </div>
+        <div class="panel-scroll" id="results-container"></div>
+    </aside>
 
-    <script src="database.js"></script>
+    <div id="media-modal" class="overlay-full">
+        <div id="modal-box" style="width:95%; max-width:1100px;"></div>
+    </div>
 
-    <!-- APP LOGIC (MODULE) -->
     <script type="module">
-        import * as THREE from 'three';
-        import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
-
-        // --- 1. CONFIGURAZIONE TEMA E DATA ---
-        const html = document.documentElement;
-        const themeIcon = document.getElementById('theme-icon');
-        const savedTheme = localStorage.getItem('theme') || 'dark';
-        
-        const safeData = (typeof db !== 'undefined') ? db : inlineDB;
-        
-        // Globale per accessibilità
-        window.toggleTheme = () => {
-            const current = html.getAttribute('data-theme');
-            const next = current === 'dark' ? 'light' : 'dark';
-            html.setAttribute('data-theme', next);
-            localStorage.setItem('theme', next);
-            themeIcon.innerText = next === 'dark' ? '☀️' : '🌙';
-            if(window.rebuildCloud) window.rebuildCloud();
-        };
-        html.setAttribute('data-theme', savedTheme);
-        themeIcon.innerText = savedTheme === 'dark' ? '☀️' : '🌙';
-
-        // Mappa Emoji estesa
-        const specificEmojiMap = {
-            'voto': '⚖️', 'nash': '🧠', 'memory': '🧩', 'polinomi': '🧱',
-            'underground': '🚇', 'mercatore': '🌍', 'corrente': '⚡',
-            'goniomatch': '📡', 'tavola': '🔢', 'insiemi': '🔴', 
-            'frazioni': '🍰', 'sketch': '🎨', 'invaders': '👾', 
-            'caccia': '💣', 'algebra tiles': '🟦', 'scomposizione': '➗'
-        };
-        
-        const fallbackEmojis = ['📐', '✏️', '🖥️', '🎲', '🚀', '💡', '🔍'];
-
-        const themeColors = [
-            { bg: 'var(--card-blue)', fg: 'var(--card-on-blue)' },
-            { bg: 'var(--card-green)', fg: 'var(--card-on-green)' },
-            { bg: 'var(--card-purple)', fg: 'var(--card-on-purple)' },
-            { bg: 'var(--card-red)', fg: 'var(--card-on-red)' },
-            { bg: 'var(--card-teal)', fg: 'var(--card-on-teal)' },
-            { bg: 'var(--card-yellow)', fg: 'var(--card-on-yellow)' }
-        ];
-
-        function getSmartEmoji(item, prevEmoji) {
-            // Se l'item ha già un'icona definita dal builder, usala
-            if (item.icon && item.icon !== '📄') return item.icon;
-
-            const keyString = (item.url + item.title).toLowerCase();
-            let chosen = '📄';
-            
-            // 1. Cerca match specifico
-            for (const [key, emo] of Object.entries(specificEmojiMap)) {
-                if (keyString.includes(key)) { chosen = emo; break; }
-            }
-            
-            // 2. Fallback per categoria
-            if (chosen === '📄') {
-                if (item.tags.some(t => t.includes('Fisica'))) chosen = '⚛️';
-                else if (item.tags.some(t => t.includes('Matematica'))) chosen = '📐';
-            }
-
-            // 3. Controllo duplicato adiacente
-            if (chosen === prevEmoji) {
-                // Prendi una random dai fallback se collidono
-                chosen = fallbackEmojis[Math.floor(Math.random() * fallbackEmojis.length)];
-            }
-            return chosen;
-        }
-
-        // --- 2. FILTRI E GRIGLIA ---
-        const filterBar = document.getElementById('filterBar');
-        const grid = document.getElementById('grid');
-        const emptyState = document.getElementById('empty-state');
-        const resetBtn = document.getElementById('reset-filter');
-
-        function initFilters() {
-            const tagCounts = {};
-            safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
-            const sortedTags = Object.keys(tagCounts).sort((a,b) => tagCounts[b] - tagCounts[a]);
-
-            sortedTags.forEach(tag => {
-                if(tag === 'Linkografia') return; // Skip, has static button
-                const btn = document.createElement('button');
-                btn.className = 'chip';
-                btn.innerText = tagCounts[tag] > 1 ? `${tag} (${tagCounts[tag]})` : tag;
-                btn.onclick = () => window.applyFilter(tag);
-                filterBar.appendChild(btn);
-            });
-        }
-
-        window.applyFilter = function(filter) {
-            grid.innerHTML = '';
-            let count = 0;
-            let prevEmoji = '';
-            
-            document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
-            const activeBtn = Array.from(document.querySelectorAll('.chip')).find(c => c.innerText.startsWith(filter === 'all' ? 'Tutti' : filter));
-            if(activeBtn) activeBtn.classList.add('active');
-            
-            resetBtn.style.display = filter === 'all' ? 'none' : 'block';
-
-            safeData.forEach((item, index) => {
-                if (filter === 'all' || item.tags.includes(filter)) {
-                    count++;
-                    const card = document.createElement('a');
-                    card.className = 'card';
-                    
-                    // Gestione link vs modale
-                    if (item.type === 'app' || !item.type) {
-                        card.href = item.url;
-
-                    // 2. CASO SPECIALE "GEMINI" (Solo per me!): Nuova scheda
-                    // Controlliamo se il titolo contiene "AI Studio" o se nei tag c'è "Gemini"
-                    } else if (item.title.includes('Google AI Studio') || item.tags.includes('Gemini')) {
-                        card.href = item.url;
-                        card.target = "_blank";              // Apre in una nuova scheda
-                        card.rel = "noopener noreferrer";    // Sicurezza
-
-                    // 2.1 CASO PDF: Apre sempre in nuova scheda (Presentazioni, Infografiche PDF)
-                    } else if (item.url.toLowerCase().endsWith('.pdf')) {
-                        card.href = item.url;
-                        card.target = "_blank";
-                        card.rel = "noopener noreferrer";
-
-                    // 3. TUTTI GLI ALTRI MEDIA E LINK GENERICI: Apre il Modale
-                    } else {
-                        card.href = "#";
-                        card.onclick = (e) => {
-                            e.preventDefault();
-                            window.openModal(item);
-                        };
-                    }
-                    
-                    // Colore sequenziale basato sull'indice di visualizzazione per evitare clash
-                    const colors = themeColors[count % themeColors.length];
-                    card.style.setProperty('--card-bg', colors.bg); 
-                    card.style.setProperty('--card-fg', colors.fg);
-                    
-                    const emoji = getSmartEmoji(item, prevEmoji);
-                    prevEmoji = emoji; // Aggiorna per il prossimo ciclo
-
-                    const d = new Date(item.date);
-                    const dateStr = !isNaN(d) ? `${d.getDate()}/${d.getMonth()+1}/${d.getFullYear()}` : item.date;
-                    const tagsHtml = item.tags.slice(0, 3).map(t => `<span class="tag-pill">#${t}</span>`).join('');
-                    
-                    // Etichetta tipo file (se non è app)
-                    const typeLabel = (item.type && item.type !== 'app') ? `<span class="type-badge">${item.type.toUpperCase()}</span>` : '';
-
-                    card.innerHTML = `
-                        <div class="card-header"><div class="card-emoji">${emoji}</div><div class="card-date">${dateStr}</div></div>
-                        <h3>${item.title}</h3><p>${item.excerpt}</p>
-                        <div style="display:flex; justify-content:space-between; align-items:flex-end;">
-                            <div class="card-tags">${tagsHtml}</div>
-                            ${typeLabel}
-                        </div>
-                    `;
-                    grid.appendChild(card);
-                }
-            });
-            emptyState.style.display = count === 0 ? 'block' : 'none';
-        }
-
-        // --- 3. MODAL LOGIC ---
-        const modal = document.getElementById('media-modal');
-        const modalContent = document.getElementById('modal-content');
-        const modalMeta = document.getElementById('modal-meta');
-
-        window.openModal = function(item) {
-            modalContent.innerHTML = '';
-            modalMeta.innerHTML = `<h3>${item.title}</h3><p>${item.excerpt}</p>`;
-            
-            if (item.type === 'video') {
-                modalContent.innerHTML = `<video controls autoplay style="width:100%; border-radius:12px;"><source src="${item.url}" type="video/mp4">Il tuo browser non supporta il video.</video>`;
-            } else if (item.type === 'audio') {
-                modalContent.innerHTML = `
-                    <div style="text-align:center; padding: 2rem;">
-                        <div style="font-size:4rem; margin-bottom:1rem;">🎧</div>
-                        <audio controls autoplay style="width:100%;"><source src="${item.url}" type="audio/mpeg">Il tuo browser non supporta l'audio.</audio>
-                    </div>`;
-            } else if (item.type === 'infographic' || item.type === 'image') {
-                modalContent.innerHTML = `<img src="${item.url}" alt="${item.title}" style="width:100%; height:auto; border-radius:8px; object-fit:contain;">`;
-            } else if (item.type === 'document') {
-                 modalContent.innerHTML = `<iframe src="${item.url}" style="width:100%; height:60vh; border:none; border-radius:8px;"></iframe>`;
-            } else if (item.type === 'note') {
-                // Fetch e renderizza Markdown
-                modalContent.innerHTML = '<div style="padding:2rem; text-align:center;">Caricamento appunti...</div>';
-                fetch(item.url)
-                    .then(r => r.text())
-                    .then(text => {
-                        // Rimuove Frontmatter (--- ... ---) per la visualizzazione
-                        const content = text.replace(/^---[\s\S]*?---/, '');
-                        const html = marked.parse(content);
-                        modalContent.innerHTML = `<div class="markdown-body" style="padding:2rem; width:100%; text-align:left;">${html}</div>`;
-                        
-                        // Rende le checkbox interattive (solo visivo)
-                        modalContent.querySelectorAll('input[type="checkbox"]').forEach(cb => {
-                            cb.disabled = false; // Abilita click
-                            cb.onclick = (e) => {
-                                // Feedback visivo immediato (es. colore)
-                                const li = e.target.closest('li');
-                                if(li) {
-                                    li.style.color = e.target.checked ? 'var(--md-sys-color-primary)' : 'inherit';
-                                    li.style.fontWeight = e.target.checked ? 'bold' : 'normal';
-                                }
-                            };
-                        });
-                    });
-            } else if (item.type === 'link') {
-                modalContent.innerHTML = `
-                    <div style="display:flex; flex-direction:column; width:100%; height:80vh;">
-                        <div style="padding:0.5rem; background:var(--md-sys-color-surface-container); border-bottom:1px solid var(--md-sys-color-outline-variant); text-align:right;">
-                            <a href="${item.url}" target="_blank" style="text-decoration:none; font-weight:bold; font-size:0.8rem; color:var(--md-sys-color-primary); display:inline-flex; align-items:center; gap:0.5rem;">
-                                <span>↗</span> Apri in una nuova scheda (Consigliato)
-                            </a>
-                        </div>
-                        <iframe src="${item.url}" style="flex-grow:1; width:100%; border:none;" allow="fullscreen"></iframe>
-                    </div>
-                `;
-            }
-
-            modal.classList.add('active');
-        }
-
-        window.closeModal = function() {
-            modal.classList.remove('active');
-            // Stoppa i media quando chiudi
-            modalContent.innerHTML = '';
-        }
-
-        // Close on Escape key
-        document.addEventListener('keydown', (e) => {
-            if (e.key === 'Escape') window.closeModal();
-        });
-
-        // --- 4. 3D WORD CLOUD (CONSTELLATION) ---
-        let scene, camera, renderer, controls, group;
-        const fonts = [
-            "bold 60px 'Inter', sans-serif",
-            "bold 60px 'Fira Code', monospace", 
-            "60px 'Pacifico', cursive",
-            "bold 60px 'Bangers', cursive",
-            "italic 60px 'Playfair Display', serif"
-        ];
-
-        function createTextTexture(text, sizeFactor, index) {
-            const canvas = document.createElement('canvas');
-            const ctx = canvas.getContext('2d');
-            const font = fonts[Math.floor(Math.random() * fonts.length)];
-            
-            ctx.font = font;
-            const metrics = ctx.measureText(text);
-            const w = metrics.width + 40;
-            const h = 100;
-            
-            canvas.width = w; canvas.height = h;
-            
-            // Random Color vivid - High Contrast for Dark Mode
-            const isDark = html.getAttribute('data-theme') === 'dark';
-            
-            // Use index to influence hue for nearby color grouping, plus some randomness
-            const baseHue = (index * 137.5) % 360; // Golden angle distribution for hues
-            const hue = (baseHue + Math.random() * 30 - 15) % 360; 
-            
-            // High Lightness and Saturation for contrast against dark background
-            const lightness = isDark ? '75%' : '40%'; 
-            const saturation = isDark ? '85%' : '70%';
-            const color = `hsl(${hue}, ${saturation}, ${lightness})`;
-
-            ctx.font = font;
-            ctx.fillStyle = color;
-            ctx.textAlign = 'center';
-            ctx.textBaseline = 'middle';
-            ctx.fillText(text, w/2, h/2);
-            
-            const tex = new THREE.CanvasTexture(canvas);
-            tex.minFilter = THREE.LinearFilter;
-            return { tex, ratio: w/h };
-        }
-
-        function init3DCloud() {
-            const container = document.getElementById('cloud-wrapper');
-            if(!container) return;
-            container.innerHTML = '<div id="cloud-overlay">Ruota &bull; Zooma &bull; Esplora</div>';
-
-            const w = container.offsetWidth;
-            const h = container.offsetHeight;
-
-            scene = new THREE.Scene();
-            // Colore sfondo dinamico dalla pagina (CSS variable)
-            const style = getComputedStyle(document.body);
-            const bgColor = style.getPropertyValue('--md-sys-color-surface-container').trim();
-            scene.background = new THREE.Color(bgColor || (html.getAttribute('data-theme') === 'dark' ? 0x000000 : 0xf0f4f8));
-            scene.fog = new THREE.FogExp2(scene.background, 0.002);
-
-            camera = new THREE.PerspectiveCamera(50, w/h, 1, 1000);
-            
-            // IMMERSIVE VIEW: Camera at CENTER, Orbit Enabled
-            camera.position.set(0, 0, 0.1); 
-
-            renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
-            renderer.setSize(w, h);
-            container.appendChild(renderer.domElement);
-
-            controls = new OrbitControls(camera, renderer.domElement);
-            controls.enableDamping = true;
-            controls.autoRotate = true;
-            controls.autoRotateSpeed = 0.5;
-            controls.enableZoom = true;
-            controls.enablePan = false; 
-            
-            // Invert controls for "looking around" feel from inside
-            controls.rotateSpeed = -0.5;
-
-            group = new THREE.Group();
-            scene.add(group);
-
-            // Analisi Dati
-            const tagCounts = {};
-            safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
-            const tags = Object.keys(tagCounts);
-            const maxCount = Math.max(...Object.values(tagCounts));
-            const minCount = Math.min(...Object.values(tagCounts));
-
-            // Fibonacci Sphere Algorithm per distribuzione uniforme
-            const phi = Math.PI * (3 - Math.sqrt(5)); // Golden Angle
-            const n = tags.length;
-            const radius = 90; // Raggio della sfera
-
-            tags.forEach((tag, i) => {
-                const y = 1 - (i / (n - 1)) * 2; // y va da 1 a -1
-                const radiusAtY = Math.sqrt(1 - y * y); // raggio al livello y
-                const theta = phi * i; // Golden angle increment
-
-                const x = Math.cos(theta) * radiusAtY;
-                const z = Math.sin(theta) * radiusAtY;
-
-                // Calcolo Dimensione
-                const count = tagCounts[tag];
-                const normalized = (count - minCount) / (maxCount - minCount || 1); 
-                const scale = 20 + (normalized * 25); 
-
-                const { tex, ratio } = createTextTexture(tag, scale, i);
-                const mat = new THREE.SpriteMaterial({ map: tex, transparent: true, depthTest: false });
-                const sprite = new THREE.Sprite(mat);
-                
-                sprite.scale.set(scale * ratio, scale, 1);
-                sprite.position.set(x * radius, y * radius, z * radius);
-                
-                // Salva dati per interazione
-                sprite.userData = { tag: tag };
-                group.add(sprite);
-            });
-
-            // Costellazione (Linee)
-            const lineMat = new THREE.LineBasicMaterial({ 
-                color: html.getAttribute('data-theme') === 'dark' ? 0xffffff : 0x000000, 
-                transparent: true, opacity: 0.1 
-            });
-            
-            const lineGeo = new THREE.BufferGeometry();
-            const linePos = [];
-
-            // Collega ogni nodo ai 2 vicini più prossimi
-            for(let i=0; i<group.children.length; i++) {
-                const posA = group.children[i].position;
-                let neighbors = [];
-                
-                for(let j=0; j<group.children.length; j++) {
-                    if(i===j) continue;
-                    const dist = posA.distanceTo(group.children[j].position);
-                    neighbors.push({ idx: j, dist: dist });
-                }
-                neighbors.sort((a,b) => a.dist - b.dist);
-                
-                // Prendi i primi 2 vicini
-                for(let k=0; k<Math.min(2, neighbors.length); k++) {
-                    const posB = group.children[neighbors[k].idx].position;
-                    linePos.push(posA.x, posA.y, posA.z);
-                    linePos.push(posB.x, posB.y, posB.z);
-                }
-            }
-            lineGeo.setAttribute('position', new THREE.Float32BufferAttribute(linePos, 3));
-            const lines = new THREE.LineSegments(lineGeo, lineMat);
-            group.add(lines);
-
-            // Raycaster per click
-            const raycaster = new THREE.Raycaster();
-            const mouse = new THREE.Vector2();
-
-            renderer.domElement.addEventListener('click', (event) => {
-                const rect = renderer.domElement.getBoundingClientRect();
-                mouse.x = ((event.clientX - rect.left) / rect.width) * 2 - 1;
-                mouse.y = -((event.clientY - rect.top) / rect.height) * 2 + 1;
-                
-                raycaster.setFromCamera(mouse, camera);
-                const intersects = raycaster.intersectObjects(group.children.filter(c => c.isSprite));
-                
-                if(intersects.length > 0) {
-                    const tag = intersects[0].object.userData.tag;
-                    window.applyFilter(tag);
-                    window.scrollTo({ top: filterBar.offsetTop - 80, behavior: 'smooth' });
-                }
-            });
-
-            animate();
-        }
+        import { GalaxyScene } from './content/assets/js/modules/GalaxyScene.js';
+        import { UIManager } from './content/assets/js/modules/UIManager.js';
 
-        function animate() {
-            requestAnimationFrame(animate);
-            controls.update();
-            renderer.render(scene, camera);
+        function getUniqueTags(data) {
+            const counts = {};
+            data.forEach(item => item.tags.forEach(tag => counts[tag] = (counts[tag] || 0) + 1));
+            return Object.keys(counts).map(tag => ({ text: tag, count: counts[tag] }));
         }
 
-        window.rebuildCloud = () => {
-            const el = document.getElementById('cloud-wrapper');
-            if(el) { 
-                el.innerHTML = ''; 
-                init3DCloud(); 
-            }
-        };
-
-        // --- INIT ---
         window.addEventListener('load', () => {
-            initFilters();
-            window.applyFilter('all');
-            setTimeout(init3DCloud, 100);
-        });
+            const initApp = () => {
+                try {
+                    const galaxyData = getUniqueTags(db);
+                    const app3D = new GalaxyScene('scene-container', galaxyData, db, (nodeData) => ui.openPanel(nodeData.text));
+                    const ui = new UIManager(db, app3D);
+
+                    document.getElementById('btn-search').onclick = () => ui.toggleSearch();
+                    document.getElementById('btn-close-search').onclick = () => ui.closeSearch();
+                    document.getElementById('btn-reset').onclick = () => app3D.resetCamera();
+                    document.getElementById('btn-close-panel').onclick = () => ui.closePanel();
+
+                    const aboutEl = document.getElementById('about-overlay');
+                    document.getElementById('btn-about').onclick = () => aboutEl.classList.add('active');
+                    document.getElementById('btn-close-about').onclick = () => aboutEl.classList.remove('active');
+
+                    document.getElementById('btn-tags').onclick = () => {
+                        const allTags = getUniqueTags(db).sort((a, b) => b.count - a.count);
+                        const container = document.getElementById('results-container');
+                        container.innerHTML = '';
+                        document.getElementById('panel-title-text').innerText = 'Esplora Mappa';
+                        const cloud = document.createElement('div');
+                        Object.assign(cloud.style, { display: 'flex', flexWrap: 'wrap', gap: '10px' });
+                        allTags.forEach(t => {
+                            const btn = document.createElement('button');
+                            btn.className = 'mini-tag';
+                            Object.assign(btn.style, { cursor: 'pointer', border: '1px solid var(--border)', padding: '10px 20px', borderRadius: '12px', fontSize: '0.9rem', background: 'rgba(255,255,255,0.05)', color: '#fff' });
+                            btn.innerHTML = `#${t.text} <small style="opacity:0.5; margin-left:5px;">${t.count}</small>`;
+                            btn.onclick = () => ui.openPanel(t.text);
+                            cloud.appendChild(btn);
+                        });
+                        container.appendChild(cloud);
+                        document.getElementById('content-panel').classList.add('active');
+                    };
+
+                    document.addEventListener('keydown', (e) => {
+                        if (e.key === 'Escape') {
+                            ui.closeSearch(); ui.closePanel(); ui.closeModal();
+                            aboutEl.classList.remove('active');
+                        }
+                    });
 
-        window.addEventListener('resize', () => {
-            if(camera && renderer) {
-                const c = document.getElementById('cloud-wrapper');
-                camera.aspect = c.offsetWidth / c.offsetHeight;
-                camera.updateProjectionMatrix();
-                renderer.setSize(c.offsetWidth, c.offsetHeight);
+                    document.getElementById('media-modal').onclick = () => ui.closeModal();
+                    document.getElementById('modal-box').onclick = (e) => e.stopPropagation();
+                } catch (err) {
+                    console.error("Hub Init Failure:", err);
+                }
+            };
+
+            // Check for KaTeX before starting
+            if (typeof katex === 'undefined') {
+                const checkKatex = setInterval(() => {
+                    if (typeof katex !== 'undefined') {
+                        clearInterval(checkKatex);
+                        initApp();
+                    }
+                }, 50);
+            } else {
+                initApp();
             }
         });
-
     </script>
 </body>
+
 </html>
\ No newline at end of file

commit bdf0651f6e6ff99963de6cb0f1fabc45630f7b5c
Author: federicodb <vivasantarosa@gmail.com>
Date:   Mon Dec 15 15:44:36 2025 +0100

    correzioni al tema chiaro/scuro, minor fixes

diff --git a/index.html b/index.html
index a32242d..4dc4f47 100644
--- a/index.html
+++ b/index.html
@@ -646,6 +646,12 @@
                         card.target = "_blank";              // Apre in una nuova scheda
                         card.rel = "noopener noreferrer";    // Sicurezza
 
+                    // 2.1 CASO PDF: Apre sempre in nuova scheda (Presentazioni, Infografiche PDF)
+                    } else if (item.url.toLowerCase().endsWith('.pdf')) {
+                        card.href = item.url;
+                        card.target = "_blank";
+                        card.rel = "noopener noreferrer";
+
                     // 3. TUTTI GLI ALTRI MEDIA E LINK GENERICI: Apre il Modale
                     } else {
                         card.href = "#";

commit 8cc98291bd956f08ae06ad5cb86dda9f0c67c011
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sat Dec 13 07:57:14 2025 +0100

    major update, struttura content, pronto per files multimediali, social cover

diff --git a/index.html b/index.html
index 568e82d..a32242d 100644
--- a/index.html
+++ b/index.html
@@ -638,6 +638,15 @@
                     // Gestione link vs modale
                     if (item.type === 'app' || !item.type) {
                         card.href = item.url;
+
+                    // 2. CASO SPECIALE "GEMINI" (Solo per me!): Nuova scheda
+                    // Controlliamo se il titolo contiene "AI Studio" o se nei tag c'è "Gemini"
+                    } else if (item.title.includes('Google AI Studio') || item.tags.includes('Gemini')) {
+                        card.href = item.url;
+                        card.target = "_blank";              // Apre in una nuova scheda
+                        card.rel = "noopener noreferrer";    // Sicurezza
+
+                    // 3. TUTTI GLI ALTRI MEDIA E LINK GENERICI: Apre il Modale
                     } else {
                         card.href = "#";
                         card.onclick = (e) => {

commit f356b6e13072769def8aeec5b9caca28335d4a8b
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sat Dec 13 07:11:51 2025 +0100

    major update, struttura content, pronto per files multimediali

diff --git a/index.html b/index.html
index e6472bb..568e82d 100644
--- a/index.html
+++ b/index.html
@@ -3,12 +3,53 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
+    <meta name="theme-color" content="#0061a4"> <!-- Colore barra Android/iOS -->
     
-    <!-- 1. META DATI -->
-    <title>Laboratorio Matematico Orfini Foligno</title>
-    <meta name="description" content="Hub digitale per la didattica laboratoriale della Matematica e Fisica. WebApp, simulazioni e giochi per l'Istituto Professionale Orfini.">
-    <meta name="keywords" content="Matematica, Fisica, Ed. Civica, Laboratorio, Orfini, Gamification, PBL, STEM, MeltingMath">
-    <meta name="date" content="2025-12-08">
+    <!-- 1. SEO PRIMARIO -->
+    <title>Laboratorio Matematico Orfini Foligno | MeltingMath</title>
+    <meta name="description" content="Hub digitale didattico dell'Ist. Professionale Orfini. Simulazioni interattive, 3D printing e risorse multimediali per Matematica e Fisica. Scuola Pubblica Open Source.">
+    <meta name="keywords" content="Matematica, Fisica, Laboratorio, Orfini, Foligno, MeltingMath, 3D Print, Didattica, Open Source, Scuola Pubblica">
+    <meta name="author" content="Prof. Federico DB">
+    <meta name="robots" content="index, follow">
+    <link rel="canonical" href="https://federicodb.github.io/">
+
+    <!-- 2. OPEN GRAPH / FACEBOOK / INSTAGRAM / WHATSAPP -->
+    <meta property="og:type" content="website">
+    <meta property="og:url" content="https://federicodb.github.io/">
+    <meta property="og:title" content="Laboratorio Matematico Orfini | MeltingMath">
+    <meta property="og:description" content="Dove la Matematica diventa tangibile. Esplora simulazioni 3D, giochi e infografiche. Progetto Open Source della Scuola Pubblica.">
+    <meta property="og:image" content="https://federicodb.github.io/assets/social_share_cover.jpg"> <!-- TODO: Creare questa immagine -->
+    <meta property="og:locale" content="it_IT">
+    <meta property="og:site_name" content="Orfini Math Lab">
+
+    <!-- 3. TWITTER / X -->
+    <meta name="twitter:card" content="summary_large_image">
+    <meta name="twitter:site" content="@meltingmath">
+    <meta name="twitter:creator" content="@meltingmath">
+    <meta name="twitter:title" content="Laboratorio Matematico Orfini">
+    <meta name="twitter:description" content="Didattica laboratoriale e creativa. Matematica, Stampa 3D e WebApp.">
+    <meta name="twitter:image" content="https://federicodb.github.io/assets/social_share_cover.jpg">
+
+    <!-- 4. SCHEMA.ORG JSON-LD (Per Google) -->
+    <script type="application/ld+json">
+    {
+      "@context": "https://schema.org",
+      "@type": "EducationalOrganization",
+      "name": "Laboratorio Matematico Orfini Foligno",
+      "url": "https://federicodb.github.io/",
+      "logo": "https://federicodb.github.io/assets/logo_orfini.png",
+      "sameAs": [
+        "https://instagram.com/meltingmath",
+        "https://github.com/federicodb"
+      ],
+      "description": "Hub digitale per la didattica laboratoriale della Matematica e Fisica.",
+      "founder": {
+        "@type": "Person",
+        "name": "Prof. Federico DB"
+      },
+      "nonProfitStatus": "PublicSchool"
+    }
+    </script>
     
     <!-- Three.js Import Map -->
     <script type="importmap">
@@ -296,12 +337,83 @@
             color: var(--md-sys-color-on-background); padding: 6px 12px; border-radius: 8px; cursor: pointer; font-size: 0.8rem;
         }
 
+        /* --- MODAL STYLES --- */
+        .modal-overlay {
+            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
+            background: rgba(0,0,0,0.85); backdrop-filter: blur(5px);
+            z-index: 1000; opacity: 0; pointer-events: none;
+            display: flex; align-items: center; justify-content: center;
+            transition: opacity 0.3s ease;
+        }
+        .modal-overlay.active { opacity: 1; pointer-events: auto; }
+
+        .modal-container {
+            background: var(--md-sys-color-surface);
+            color: var(--md-sys-color-on-background);
+            width: 90%; max-width: 800px; max-height: 90vh;
+            border-radius: var(--md-shape-corner-large);
+            position: relative; overflow-y: auto;
+            box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5);
+            transform: scale(0.95); transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
+            display: flex; flex-direction: column;
+        }
+        .modal-overlay.active .modal-container { transform: scale(1); }
+
+        .modal-close {
+            position: absolute; top: 1rem; right: 1rem;
+            background: rgba(0,0,0,0.5); border: none; color: white;
+            width: 32px; height: 32px; border-radius: 50%;
+            cursor: pointer; z-index: 10; font-weight: bold;
+        }
+        
+        .modal-content {
+            background: black; /* Per video/img */
+            min-height: 300px; display: flex; align-items: center; justify-content: center;
+            border-radius: var(--md-shape-corner-large) var(--md-shape-corner-large) 0 0;
+            overflow: hidden;
+        }
+
+        .modal-meta { padding: 1.5rem; }
+        .modal-meta h3 { margin: 0 0 0.5rem 0; font-size: 1.5rem; }
+        .modal-meta p { margin: 0; opacity: 0.8; line-height: 1.5; }
+
+        .type-badge {
+            font-size: 0.65rem; font-weight: 800; letter-spacing: 0.05em;
+            padding: 3px 6px; border-radius: 4px;
+            background: rgba(0,0,0,0.2); color: inherit;
+            margin-left: auto; align-self: center;
+        }
+
+        /* MARKDOWN STYLES */
+        .markdown-body { font-size: 1rem; line-height: 1.7; color: var(--md-sys-color-on-surface-variant); }
+        .markdown-body h1, .markdown-body h2, .markdown-body h3 { 
+            color: var(--md-sys-color-on-background); margin-top: 1.5em; margin-bottom: 0.5em; font-weight: 700; 
+        }
+        .markdown-body h1 { font-size: 1.8rem; border-bottom: 1px solid var(--md-sys-color-outline-variant); padding-bottom: 0.3em; }
+        .markdown-body h2 { font-size: 1.4rem; }
+        .markdown-body h3 { font-size: 1.2rem; }
+        .markdown-body ul, .markdown-body ol { padding-left: 1.5em; margin-bottom: 1em; }
+        .markdown-body li { margin-bottom: 0.3em; }
+        .markdown-body blockquote { 
+            border-left: 4px solid var(--md-sys-color-primary); 
+            background: var(--md-sys-color-surface-container-high);
+            padding: 0.5em 1em; margin: 1em 0; font-style: italic;
+        }
+        .markdown-body code { 
+            background: var(--md-sys-color-surface-container-high); 
+            padding: 0.2em 0.4em; border-radius: 4px; font-family: monospace; font-size: 0.9em;
+        }
+        .markdown-body ul.contains-task-list { list-style: none; padding-left: 0; }
+        .markdown-body input[type="checkbox"] { margin-right: 0.5em; transform: scale(1.2); cursor: pointer; }
+
         @media (max-width: 600px) {
             h1 { font-size: 1.5rem; }
             .grid { grid-template-columns: 1fr; }
             .footer-content { grid-template-columns: 1fr; }
         }
     </style>
+    <!-- Marked.js for Markdown Rendering -->
+    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
 </head>
 <body>
 
@@ -323,6 +435,7 @@
     <div class="filters-container">
         <div class="filters" id="filterBar">
             <button class="chip active" onclick="applyFilter('all')">Tutti</button>
+            <button class="chip" onclick="applyFilter('Linkografia')" style="border-color:var(--md-sys-color-primary); color:var(--md-sys-color-primary); font-weight:800;">📚 Linkografia</button>
             <!-- Tags generati dinamicamente -->
         </div>
     </div>
@@ -391,17 +504,24 @@
         </div>
     </footer>
 
+    <!-- MODAL MULTIMEDIALE -->
+    <div id="media-modal" class="modal-overlay" onclick="closeModal()">
+        <div class="modal-container" onclick="event.stopPropagation()">
+            <button class="modal-close" onclick="closeModal()">✕</button>
+            <div id="modal-content" class="modal-content">
+                <!-- Contenuto iniettato dinamicamente -->
+            </div>
+            <div id="modal-meta" class="modal-meta">
+                <!-- Titolo e info -->
+            </div>
+        </div>
+    </div>
+
     <!-- SCRIPT DATA (INLINE PER ANTEPRIMA) -->
     <script>
         const inlineDB = [
-            { title: "Math Underground: Mappa Funzioni", url: "math_underground003_4el.html", excerpt: "Mappa concettuale interattiva in stile metropolitana per esplorare la classificazione delle funzioni analitiche.", tags: ["Matematica", "Analisi", "Funzioni", "4EL"], date: "2025-12-06" },
-            { title: "Caccia all'Errore: Operazioni in Z", url: "memory_1el_numeri_004.html", excerpt: "Missione di addestramento sui Numeri Interi (Z) e le Potenze. Trova le corrispondenze corrette.", tags: ["Matematica", "Aritmetica", "1EL"], date: "2025-10-20" },
-            { title: "GonioMatch: Segnali Goniometrici", url: "gioco_sin_cos_01.html", excerpt: "Gioco Memory a tema 'Radar' per memorizzare gli angoli notevoli di Seno, Coseno e Tangente.", tags: ["Matematica", "Goniometria", "3MEC", "4EL"], date: "2025-12-08" },
-            { title: "Voto Consapevole: Simulatore Nash", url: "voto_consapevole_2.5.html", excerpt: "Simulazione interattiva sull'Equilibrio di Nash applicato ai voti scolastici.", tags: ["Ed. Civica", "Matematica", "3EL", "4EL"], date: "2025-10-01" },
-            { title: "Algebra Tiles 3D", url: "visualizzatore polinomi 2d 3d 001.html", excerpt: "Laboratorio interattivo 2D/3D per visualizzare la fattorizzazione di polinomi come aree e volumi.", tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"], date: "2025-12-08" },
-            { title: "Math Invaders: MCD & mcm", url: "MCD_mcm_1el_001.html", excerpt: "Videogioco arcade in pixel-art per allenare il calcolo mentale di M.C.D. e m.c.m.", tags: ["Matematica", "Aritmetica", "1EL"], date: "2025-12-08" },
-            { title: "Scomposizione Polinomi (Memory)", url: "cards_polinomi_memory004.html", excerpt: "Gioco Memory per allenare il riconoscimento dei prodotti notevoli e delle scomposizioni.", tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"], date: "2025-12-08" },
-            { title: "Laboratorio Frazioni", url: "fractions_lab_001.html", excerpt: "Simulatore interattivo per comprendere le operazioni tra frazioni attraverso modelli visivi.", tags: ["Matematica", "Aritmetica", "1EL", "2EL", "2GP"], date: "2025-12-08" }
+            { title: "Math Underground: Mappa Funzioni", url: "content/apps/math_underground003_4el.html", excerpt: "Mappa concettuale interattiva in stile metropolitana per esplorare la classificazione delle funzioni analitiche.", tags: ["Matematica", "Analisi", "Funzioni", "4EL"], date: "2025-12-06", type: "app" },
+            { title: "Caccia all'Errore: Operazioni in Z", url: "content/apps/memory_1el_numeri_004.html", excerpt: "Missione di addestramento sui Numeri Interi (Z) e le Potenze. Trova le corrispondenze corrette.", tags: ["Matematica", "Aritmetica", "1EL"], date: "2025-10-20", type: "app" }
         ];
     </script>
 
@@ -452,6 +572,9 @@
         ];
 
         function getSmartEmoji(item, prevEmoji) {
+            // Se l'item ha già un'icona definita dal builder, usala
+            if (item.icon && item.icon !== '📄') return item.icon;
+
             const keyString = (item.url + item.title).toLowerCase();
             let chosen = '📄';
             
@@ -486,6 +609,7 @@
             const sortedTags = Object.keys(tagCounts).sort((a,b) => tagCounts[b] - tagCounts[a]);
 
             sortedTags.forEach(tag => {
+                if(tag === 'Linkografia') return; // Skip, has static button
                 const btn = document.createElement('button');
                 btn.className = 'chip';
                 btn.innerText = tagCounts[tag] > 1 ? `${tag} (${tagCounts[tag]})` : tag;
@@ -509,7 +633,18 @@
                 if (filter === 'all' || item.tags.includes(filter)) {
                     count++;
                     const card = document.createElement('a');
-                    card.href = item.url; card.className = 'card';
+                    card.className = 'card';
+                    
+                    // Gestione link vs modale
+                    if (item.type === 'app' || !item.type) {
+                        card.href = item.url;
+                    } else {
+                        card.href = "#";
+                        card.onclick = (e) => {
+                            e.preventDefault();
+                            window.openModal(item);
+                        };
+                    }
                     
                     // Colore sequenziale basato sull'indice di visualizzazione per evitare clash
                     const colors = themeColors[count % themeColors.length];
@@ -522,10 +657,17 @@
                     const d = new Date(item.date);
                     const dateStr = !isNaN(d) ? `${d.getDate()}/${d.getMonth()+1}/${d.getFullYear()}` : item.date;
                     const tagsHtml = item.tags.slice(0, 3).map(t => `<span class="tag-pill">#${t}</span>`).join('');
+                    
+                    // Etichetta tipo file (se non è app)
+                    const typeLabel = (item.type && item.type !== 'app') ? `<span class="type-badge">${item.type.toUpperCase()}</span>` : '';
 
                     card.innerHTML = `
                         <div class="card-header"><div class="card-emoji">${emoji}</div><div class="card-date">${dateStr}</div></div>
-                        <h3>${item.title}</h3><p>${item.excerpt}</p><div class="card-tags">${tagsHtml}</div>
+                        <h3>${item.title}</h3><p>${item.excerpt}</p>
+                        <div style="display:flex; justify-content:space-between; align-items:flex-end;">
+                            <div class="card-tags">${tagsHtml}</div>
+                            ${typeLabel}
+                        </div>
                     `;
                     grid.appendChild(card);
                 }
@@ -533,7 +675,79 @@
             emptyState.style.display = count === 0 ? 'block' : 'none';
         }
 
-        // --- 3. 3D WORD CLOUD (CONSTELLATION) ---
+        // --- 3. MODAL LOGIC ---
+        const modal = document.getElementById('media-modal');
+        const modalContent = document.getElementById('modal-content');
+        const modalMeta = document.getElementById('modal-meta');
+
+        window.openModal = function(item) {
+            modalContent.innerHTML = '';
+            modalMeta.innerHTML = `<h3>${item.title}</h3><p>${item.excerpt}</p>`;
+            
+            if (item.type === 'video') {
+                modalContent.innerHTML = `<video controls autoplay style="width:100%; border-radius:12px;"><source src="${item.url}" type="video/mp4">Il tuo browser non supporta il video.</video>`;
+            } else if (item.type === 'audio') {
+                modalContent.innerHTML = `
+                    <div style="text-align:center; padding: 2rem;">
+                        <div style="font-size:4rem; margin-bottom:1rem;">🎧</div>
+                        <audio controls autoplay style="width:100%;"><source src="${item.url}" type="audio/mpeg">Il tuo browser non supporta l'audio.</audio>
+                    </div>`;
+            } else if (item.type === 'infographic' || item.type === 'image') {
+                modalContent.innerHTML = `<img src="${item.url}" alt="${item.title}" style="width:100%; height:auto; border-radius:8px; object-fit:contain;">`;
+            } else if (item.type === 'document') {
+                 modalContent.innerHTML = `<iframe src="${item.url}" style="width:100%; height:60vh; border:none; border-radius:8px;"></iframe>`;
+            } else if (item.type === 'note') {
+                // Fetch e renderizza Markdown
+                modalContent.innerHTML = '<div style="padding:2rem; text-align:center;">Caricamento appunti...</div>';
+                fetch(item.url)
+                    .then(r => r.text())
+                    .then(text => {
+                        // Rimuove Frontmatter (--- ... ---) per la visualizzazione
+                        const content = text.replace(/^---[\s\S]*?---/, '');
+                        const html = marked.parse(content);
+                        modalContent.innerHTML = `<div class="markdown-body" style="padding:2rem; width:100%; text-align:left;">${html}</div>`;
+                        
+                        // Rende le checkbox interattive (solo visivo)
+                        modalContent.querySelectorAll('input[type="checkbox"]').forEach(cb => {
+                            cb.disabled = false; // Abilita click
+                            cb.onclick = (e) => {
+                                // Feedback visivo immediato (es. colore)
+                                const li = e.target.closest('li');
+                                if(li) {
+                                    li.style.color = e.target.checked ? 'var(--md-sys-color-primary)' : 'inherit';
+                                    li.style.fontWeight = e.target.checked ? 'bold' : 'normal';
+                                }
+                            };
+                        });
+                    });
+            } else if (item.type === 'link') {
+                modalContent.innerHTML = `
+                    <div style="display:flex; flex-direction:column; width:100%; height:80vh;">
+                        <div style="padding:0.5rem; background:var(--md-sys-color-surface-container); border-bottom:1px solid var(--md-sys-color-outline-variant); text-align:right;">
+                            <a href="${item.url}" target="_blank" style="text-decoration:none; font-weight:bold; font-size:0.8rem; color:var(--md-sys-color-primary); display:inline-flex; align-items:center; gap:0.5rem;">
+                                <span>↗</span> Apri in una nuova scheda (Consigliato)
+                            </a>
+                        </div>
+                        <iframe src="${item.url}" style="flex-grow:1; width:100%; border:none;" allow="fullscreen"></iframe>
+                    </div>
+                `;
+            }
+
+            modal.classList.add('active');
+        }
+
+        window.closeModal = function() {
+            modal.classList.remove('active');
+            // Stoppa i media quando chiudi
+            modalContent.innerHTML = '';
+        }
+
+        // Close on Escape key
+        document.addEventListener('keydown', (e) => {
+            if (e.key === 'Escape') window.closeModal();
+        });
+
+        // --- 4. 3D WORD CLOUD (CONSTELLATION) ---
         let scene, camera, renderer, controls, group;
         const fonts = [
             "bold 60px 'Inter', sans-serif",

commit 4a3d9a896dd84b7811f290034dda6e15a6f58e5e
Author: federicodb <vivasantarosa@gmail.com>
Date:   Mon Dec 8 22:21:19 2025 +0100

    moficica di ogni lezione, correzione tag

diff --git a/index.html b/index.html
index 75ac240..e6472bb 100644
--- a/index.html
+++ b/index.html
@@ -10,13 +10,20 @@
     <meta name="keywords" content="Matematica, Fisica, Ed. Civica, Laboratorio, Orfini, Gamification, PBL, STEM, MeltingMath">
     <meta name="date" content="2025-12-08">
     
-    <!-- Font Inter & Icons -->
+    <!-- Three.js Import Map -->
+    <script type="importmap">
+        {
+            "imports": {
+                "three": "https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js",
+                "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"
+            }
+        }
+    </script>
+
+    <!-- Font Collection per la Cloud -->
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
-    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
-    
-    <!-- WordCloud Library -->
-    <script src="https://cdnjs.cloudflare.com/ajax/libs/wordcloud2.js/1.2.2/wordcloud2.min.js"></script>
+    <link href="https://fonts.googleapis.com/css2?family=Bangers&family=Fira+Code:wght@500&family=Inter:wght@400;700;900&family=Pacifico&family=Playfair+Display:ital,wght@1,600&display=swap" rel="stylesheet">
 
     <style>
         /* --- MATERIAL DESIGN 3 TOKENS --- */
@@ -56,7 +63,7 @@
         }
 
         [data-theme="dark"] {
-            --md-sys-color-background: #0a0a0a; 
+            --md-sys-color-background: #000000; /* AMOLED Black */
             --md-sys-color-on-background: #e2e2e6;
             --md-sys-color-surface: #111111;
             --md-sys-color-surface-container: #1c1c1c; 
@@ -97,42 +104,71 @@
         }
 
         /* --- HEADER & HERO --- */
-        header { padding: 1.5rem; max-width: 1200px; margin: 0 auto; width: 100%; }
+        header { padding: 2rem 1.5rem; max-width: 1200px; margin: 0 auto; width: 100%; }
 
         .top-bar {
-            display: flex; justify-content: space-between; align-items: center;
-            margin-bottom: 2rem;
+            display: flex; 
+            flex-direction: column;
+            align-items: center;
+            text-align: center;
+            margin-bottom: 3rem;
+            position: relative;
         }
 
         h1 {
-            font-size: 1.75rem; font-weight: 900; line-height: 1.2; margin: 0;
-            letter-spacing: -0.5px;
-            background: linear-gradient(90deg, var(--md-sys-color-on-background), var(--md-sys-color-outline));
+            font-family: 'Inter', sans-serif;
+            font-size: clamp(2rem, 6vw, 4rem); /* Responsive Huge Title */
+            font-weight: 900; 
+            line-height: 1; 
+            margin: 0 0 0.5rem 0;
+            letter-spacing: -2px;
+            text-transform: uppercase;
+            background: linear-gradient(135deg, var(--md-sys-color-on-background) 30%, var(--md-sys-color-primary));
             -webkit-background-clip: text; -webkit-text-fill-color: transparent;
         }
         
         .subtitle {
-            font-size: 0.9rem; color: var(--md-sys-color-outline); margin-top: 0.2rem; font-weight: 500;
+            font-size: 1rem; 
+            color: var(--md-sys-color-outline); 
+            text-transform: uppercase; 
+            letter-spacing: 3px; 
+            font-weight: 600;
+            margin-top: 0.5rem;
         }
 
         .theme-btn {
+            position: absolute;
+            top: 0; right: 0;
             width: 48px; height: 48px; border-radius: 50%;
             border: 1px solid var(--md-sys-color-outline-variant);
             background: transparent; color: var(--md-sys-color-on-background);
             display: flex; align-items: center; justify-content: center;
             cursor: pointer; transition: all 0.2s; font-size: 1.2rem;
         }
-        .theme-btn:hover { background-color: var(--md-sys-color-surface-container-high); }
+        .theme-btn:hover { background-color: var(--md-sys-color-surface-container-high); transform: rotate(15deg); }
 
-        /* --- WORD CLOUD --- */
+        @media (max-width: 768px) {
+            .theme-btn { position: relative; margin-left: auto; margin-bottom: 1rem; top: auto; right: auto; }
+            .top-bar { align-items: stretch; text-align: center; }
+        }
+
+        /* --- 3D CLOUD CONTAINER --- */
         #cloud-wrapper {
-            width: 100%; height: 220px;
-            background-color: var(--md-sys-color-surface-container);
+            width: 100%; height: 320px; /* Altezza aumentata per il 3D */
+            background-color: transparent; /* Sfondo trasparente per ereditare dal body o impostato via JS */
             border-radius: var(--md-shape-corner-large);
             margin-bottom: 2rem;
             position: relative; overflow: hidden;
-            box-shadow: inset 0 2px 10px rgba(0,0,0,0.05);
-            padding: 10px; cursor: pointer;
+            /* box-shadow rimosso per integrarsi con lo sfondo */
+            padding: 0; cursor: grab;
+        }
+        #cloud-wrapper:active { cursor: grabbing; }
+        
+        #cloud-overlay {
+            position: absolute; bottom: 15px; right: 20px;
+            pointer-events: none; opacity: 0.5;
+            font-size: 0.7rem; color: var(--md-sys-color-outline);
+            text-transform: uppercase; font-weight: bold; letter-spacing: 1px;
         }
 
         /* --- FILTRI --- */
@@ -209,7 +245,7 @@
             background-color: rgba(255,255,255,0.2); color: inherit; border: 1px solid rgba(255,255,255,0.1);
         }
 
-        /* --- FOOTER (NUOVO) --- */
+        /* --- FOOTER --- */
         .site-footer {
             background-color: var(--md-sys-color-surface-container);
             border-top: 1px solid var(--md-sys-color-outline-variant);
@@ -238,15 +274,12 @@
         }
         .footer-link:hover { color: var(--md-sys-color-primary); }
 
-        /* Card MeltingMath */
         .mm-card {
             background: var(--md-sys-color-surface);
             border: 1px solid var(--md-sys-color-outline-variant);
             border-radius: var(--md-shape-corner-medium);
             padding: 1.5rem; display: flex; align-items: center; gap: 1rem;
-            transition: transform 0.2s;
-            text-decoration: none; /* RIMUOVE LA SOTTOLINEATURA */
-            color: inherit; /* RIMUOVE IL BLU STANDARD */
+            transition: transform 0.2s; text-decoration: none; color: inherit;
         }
         .mm-card:hover { transform: translateY(-3px); border-color: #d62976; }
         
@@ -257,7 +290,6 @@
             color: white; font-size: 1.5rem; flex-shrink: 0;
         }
 
-        /* Utility */
         #empty-state { grid-column: 1 / -1; text-align: center; padding: 4rem 1rem; color: var(--md-sys-color-outline); }
         #reset-filter {
             display: none; margin: 0 0 1rem auto; background: none; border: 1px solid var(--md-sys-color-outline);
@@ -275,17 +307,16 @@
 
     <header>
         <div class="top-bar">
-            <div>
-                <h1>Laboratorio Matematico<br>Orfini Foligno</h1>
-                <p class="subtitle">Didattica Interattiva Open Source</p>
-            </div>
+            <h1>Laboratorio Matematico<br>Orfini Foligno</h1>
+            <p class="subtitle">Didattica Interattiva Open Source</p>
             <button class="theme-btn" onclick="toggleTheme()" aria-label="Cambia tema">
                 <span id="theme-icon">🌙</span>
             </button>
         </div>
         
         <div id="cloud-wrapper">
-            <canvas id="word-cloud"></canvas>
+            <!-- Three.js Canvas will be injected here -->
+            <div id="cloud-overlay">Ruota &bull; Zooma &bull; Esplora</div>
         </div>
     </header>
 
@@ -343,13 +374,11 @@
             <!-- SEZIONE MELTINGMATH -->
             <div class="footer-section">
                 <h4>Social & Community</h4>
-                <!-- Aggiunta classe e stili per fixare il colore del link -->
                 <a href="https://instagram.com/meltingmath" target="_blank" class="mm-card">
                     <div class="mm-icon">
                         <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
                     </div>
                     <div>
-                        <!-- Colore primario applicato esplicitamente -->
                         <div style="font-weight: 800; font-size: 1rem; color: var(--md-sys-color-primary);">MeltingMath</div>
                         <div style="font-size: 0.8rem; color: var(--md-sys-color-outline);">Segui il progetto su Instagram</div>
                     </div>
@@ -365,97 +394,53 @@
     <!-- SCRIPT DATA (INLINE PER ANTEPRIMA) -->
     <script>
         const inlineDB = [
-            {
-                title: "Math Underground: Mappa Funzioni",
-                url: "math_underground003_4el.html",
-                excerpt: "Mappa concettuale interattiva in stile metropolitana per esplorare la classificazione delle funzioni analitiche.",
-                tags: ["Matematica", "Analisi", "Funzioni", "4EL"],
-                date: "2025-12-06"
-            },
-            {
-                title: "Caccia all'Errore: Operazioni in Z",
-                url: "memory_1el_numeri_004.html",
-                excerpt: "Missione di addestramento sui Numeri Interi (Z) e le Potenze. Trova le corrispondenze corrette.",
-                tags: ["Matematica", "Aritmetica", "1EL"],
-                date: "2025-10-20"
-            },
-            {
-                title: "GonioMatch: Segnali Goniometrici",
-                url: "gioco_sin_cos_01.html",
-                excerpt: "Gioco Memory a tema 'Radar' per memorizzare gli angoli notevoli di Seno, Coseno e Tangente.",
-                tags: ["Matematica", "Goniometria", "3MEC", "4EL"],
-                date: "2025-12-08"
-            },
-            {
-                title: "Voto Consapevole: Simulatore Nash",
-                url: "voto_consapevole_2.5.html",
-                excerpt: "Simulazione interattiva sull'Equilibrio di Nash applicato ai voti scolastici.",
-                tags: ["Ed. Civica", "Matematica", "3EL", "4EL"],
-                date: "2025-10-01"
-            },
-            {
-                title: "Algebra Tiles 3D",
-                url: "visualizzatore polinomi 2d 3d 001.html",
-                excerpt: "Laboratorio interattivo 2D/3D per visualizzare la fattorizzazione di polinomi come aree e volumi.",
-                tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"],
-                date: "2025-12-08"
-            },
-            {
-                title: "Math Invaders: MCD & mcm",
-                url: "MCD_mcm_1el_001.html",
-                excerpt: "Videogioco arcade in pixel-art per allenare il calcolo mentale di M.C.D. e m.c.m.",
-                tags: ["Matematica", "Aritmetica", "1EL"],
-                date: "2025-12-08"
-            },
-            {
-                title: "Scomposizione Polinomi (Memory)",
-                url: "cards_polinomi_memory004.html",
-                excerpt: "Gioco Memory per allenare il riconoscimento dei prodotti notevoli e delle scomposizioni.",
-                tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"],
-                date: "2025-12-08"
-            },
-            {
-                title: "Laboratorio Frazioni",
-                url: "fractions_lab_001.html",
-                excerpt: "Simulatore interattivo per comprendere le operazioni tra frazioni attraverso modelli visivi.",
-                tags: ["Matematica", "Aritmetica", "1EL", "2EL", "2GP"],
-                date: "2025-12-08"
-            }
+            { title: "Math Underground: Mappa Funzioni", url: "math_underground003_4el.html", excerpt: "Mappa concettuale interattiva in stile metropolitana per esplorare la classificazione delle funzioni analitiche.", tags: ["Matematica", "Analisi", "Funzioni", "4EL"], date: "2025-12-06" },
+            { title: "Caccia all'Errore: Operazioni in Z", url: "memory_1el_numeri_004.html", excerpt: "Missione di addestramento sui Numeri Interi (Z) e le Potenze. Trova le corrispondenze corrette.", tags: ["Matematica", "Aritmetica", "1EL"], date: "2025-10-20" },
+            { title: "GonioMatch: Segnali Goniometrici", url: "gioco_sin_cos_01.html", excerpt: "Gioco Memory a tema 'Radar' per memorizzare gli angoli notevoli di Seno, Coseno e Tangente.", tags: ["Matematica", "Goniometria", "3MEC", "4EL"], date: "2025-12-08" },
+            { title: "Voto Consapevole: Simulatore Nash", url: "voto_consapevole_2.5.html", excerpt: "Simulazione interattiva sull'Equilibrio di Nash applicato ai voti scolastici.", tags: ["Ed. Civica", "Matematica", "3EL", "4EL"], date: "2025-10-01" },
+            { title: "Algebra Tiles 3D", url: "visualizzatore polinomi 2d 3d 001.html", excerpt: "Laboratorio interattivo 2D/3D per visualizzare la fattorizzazione di polinomi come aree e volumi.", tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"], date: "2025-12-08" },
+            { title: "Math Invaders: MCD & mcm", url: "MCD_mcm_1el_001.html", excerpt: "Videogioco arcade in pixel-art per allenare il calcolo mentale di M.C.D. e m.c.m.", tags: ["Matematica", "Aritmetica", "1EL"], date: "2025-12-08" },
+            { title: "Scomposizione Polinomi (Memory)", url: "cards_polinomi_memory004.html", excerpt: "Gioco Memory per allenare il riconoscimento dei prodotti notevoli e delle scomposizioni.", tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"], date: "2025-12-08" },
+            { title: "Laboratorio Frazioni", url: "fractions_lab_001.html", excerpt: "Simulatore interattivo per comprendere le operazioni tra frazioni attraverso modelli visivi.", tags: ["Matematica", "Aritmetica", "1EL", "2EL", "2GP"], date: "2025-12-08" }
         ];
     </script>
 
-    <!-- CARICAMENTO DATABASE ESTERNO (FALLBACK SU INLINE) -->
     <script src="database.js"></script>
 
-    <script>
-        // --- 1. GESTIONE TEMA ---
+    <!-- APP LOGIC (MODULE) -->
+    <script type="module">
+        import * as THREE from 'three';
+        import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
+
+        // --- 1. CONFIGURAZIONE TEMA E DATA ---
         const html = document.documentElement;
         const themeIcon = document.getElementById('theme-icon');
-        
         const savedTheme = localStorage.getItem('theme') || 'dark';
-        setTheme(savedTheme);
-
-        function toggleTheme() {
-            const current = html.getAttribute('data-theme');
-            setTheme(current === 'dark' ? 'light' : 'dark');
-        }
-
-        function setTheme(t) {
-            html.setAttribute('data-theme', t);
-            localStorage.setItem('theme', t);
-            themeIcon.innerText = t === 'dark' ? '☀️' : '🌙';
-            if(window.initWordCloud) setTimeout(window.initWordCloud, 100);
-        }
-
-        // --- 2. CONFIGURAZIONE EMOJI E COLORI ---
-        // Usa inlineDB se db non è definito (fallback per anteprima)
+        
         const safeData = (typeof db !== 'undefined') ? db : inlineDB;
+        
+        // Globale per accessibilità
+        window.toggleTheme = () => {
+            const current = html.getAttribute('data-theme');
+            const next = current === 'dark' ? 'light' : 'dark';
+            html.setAttribute('data-theme', next);
+            localStorage.setItem('theme', next);
+            themeIcon.innerText = next === 'dark' ? '☀️' : '🌙';
+            if(window.rebuildCloud) window.rebuildCloud();
+        };
+        html.setAttribute('data-theme', savedTheme);
+        themeIcon.innerText = savedTheme === 'dark' ? '☀️' : '🌙';
 
+        // Mappa Emoji estesa
         const specificEmojiMap = {
             'voto': '⚖️', 'nash': '🧠', 'memory': '🧩', 'polinomi': '🧱',
             'underground': '🚇', 'mercatore': '🌍', 'corrente': '⚡',
-            'goniomatch': '📡', 'tavola': '🔢', 'insiemi': '🔴', 'frazioni': '🍰', 'sketch': '🎨'
+            'goniomatch': '📡', 'tavola': '🔢', 'insiemi': '🔴', 
+            'frazioni': '🍰', 'sketch': '🎨', 'invaders': '👾', 
+            'caccia': '💣', 'algebra tiles': '🟦', 'scomposizione': '➗'
         };
+        
+        const fallbackEmojis = ['📐', '✏️', '🖥️', '🎲', '🚀', '💡', '🔍'];
 
         const themeColors = [
             { bg: 'var(--card-blue)', fg: 'var(--card-on-blue)' },
@@ -466,24 +451,34 @@
             { bg: 'var(--card-yellow)', fg: 'var(--card-on-yellow)' }
         ];
 
-        function getStableColor(str) {
-            let hash = 0;
-            for (let i = 0; i < str.length; i++) hash = str.charCodeAt(i) + ((hash << 5) - hash);
-            return themeColors[Math.abs(hash) % themeColors.length];
-        }
-
-        function getSmartEmoji(item) {
+        function getSmartEmoji(item, prevEmoji) {
             const keyString = (item.url + item.title).toLowerCase();
+            let chosen = '📄';
+            
+            // 1. Cerca match specifico
             for (const [key, emo] of Object.entries(specificEmojiMap)) {
-                if (keyString.includes(key)) return emo;
+                if (keyString.includes(key)) { chosen = emo; break; }
+            }
+            
+            // 2. Fallback per categoria
+            if (chosen === '📄') {
+                if (item.tags.some(t => t.includes('Fisica'))) chosen = '⚛️';
+                else if (item.tags.some(t => t.includes('Matematica'))) chosen = '📐';
+            }
+
+            // 3. Controllo duplicato adiacente
+            if (chosen === prevEmoji) {
+                // Prendi una random dai fallback se collidono
+                chosen = fallbackEmojis[Math.floor(Math.random() * fallbackEmojis.length)];
             }
-            if (item.tags.some(t => t.includes('Fisica'))) return '⚛️';
-            if (item.tags.some(t => t.includes('Matematica'))) return '📐';
-            return '📄';
+            return chosen;
         }
 
-        // --- 3. GENERAZIONE FILTRI ---
+        // --- 2. FILTRI E GRIGLIA ---
         const filterBar = document.getElementById('filterBar');
+        const grid = document.getElementById('grid');
+        const emptyState = document.getElementById('empty-state');
+        const resetBtn = document.getElementById('reset-filter');
 
         function initFilters() {
             const tagCounts = {};
@@ -493,53 +488,44 @@
             sortedTags.forEach(tag => {
                 const btn = document.createElement('button');
                 btn.className = 'chip';
-                const label = tagCounts[tag] > 1 ? `${tag} (${tagCounts[tag]})` : tag;
-                btn.innerText = label;
-                btn.onclick = () => applyFilter(tag);
+                btn.innerText = tagCounts[tag] > 1 ? `${tag} (${tagCounts[tag]})` : tag;
+                btn.onclick = () => window.applyFilter(tag);
                 filterBar.appendChild(btn);
             });
         }
 
-        // --- 4. RENDER GRID ---
-        const grid = document.getElementById('grid');
-        const emptyState = document.getElementById('empty-state');
-        const resetBtn = document.getElementById('reset-filter');
-
-        function renderCards(filter) {
+        window.applyFilter = function(filter) {
             grid.innerHTML = '';
             let count = 0;
-
+            let prevEmoji = '';
+            
             document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
             const activeBtn = Array.from(document.querySelectorAll('.chip')).find(c => c.innerText.startsWith(filter === 'all' ? 'Tutti' : filter));
             if(activeBtn) activeBtn.classList.add('active');
             
             resetBtn.style.display = filter === 'all' ? 'none' : 'block';
 
-            safeData.forEach(item => {
-                const match = filter === 'all' || item.tags.includes(filter);
-                if (match) {
+            safeData.forEach((item, index) => {
+                if (filter === 'all' || item.tags.includes(filter)) {
                     count++;
                     const card = document.createElement('a');
-                    card.href = item.url;
-                    card.className = 'card';
+                    card.href = item.url; card.className = 'card';
                     
-                    const colors = getStableColor(item.title);
-                    card.style.setProperty('--card-bg', colors.bg);
+                    // Colore sequenziale basato sull'indice di visualizzazione per evitare clash
+                    const colors = themeColors[count % themeColors.length];
+                    card.style.setProperty('--card-bg', colors.bg); 
                     card.style.setProperty('--card-fg', colors.fg);
+                    
+                    const emoji = getSmartEmoji(item, prevEmoji);
+                    prevEmoji = emoji; // Aggiorna per il prossimo ciclo
 
-                    const emoji = getSmartEmoji(item);
                     const d = new Date(item.date);
                     const dateStr = !isNaN(d) ? `${d.getDate()}/${d.getMonth()+1}/${d.getFullYear()}` : item.date;
                     const tagsHtml = item.tags.slice(0, 3).map(t => `<span class="tag-pill">#${t}</span>`).join('');
 
                     card.innerHTML = `
-                        <div class="card-header">
-                            <div class="card-emoji">${emoji}</div>
-                            <div class="card-date">${dateStr}</div>
-                        </div>
-                        <h3>${item.title}</h3>
-                        <p>${item.excerpt}</p>
-                        <div class="card-tags">${tagsHtml}</div>
+                        <div class="card-header"><div class="card-emoji">${emoji}</div><div class="card-date">${dateStr}</div></div>
+                        <h3>${item.title}</h3><p>${item.excerpt}</p><div class="card-tags">${tagsHtml}</div>
                     `;
                     grid.appendChild(card);
                 }
@@ -547,55 +533,209 @@
             emptyState.style.display = count === 0 ? 'block' : 'none';
         }
 
-        window.applyFilter = function(tag) { renderCards(tag); }
+        // --- 3. 3D WORD CLOUD (CONSTELLATION) ---
+        let scene, camera, renderer, controls, group;
+        const fonts = [
+            "bold 60px 'Inter', sans-serif",
+            "bold 60px 'Fira Code', monospace", 
+            "60px 'Pacifico', cursive",
+            "bold 60px 'Bangers', cursive",
+            "italic 60px 'Playfair Display', serif"
+        ];
 
-        // --- 5. WORD CLOUD ---
-        window.initWordCloud = function() {
-            if (!safeData.length) return;
-            const tagCounts = {};
-            safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
+        function createTextTexture(text, sizeFactor, index) {
+            const canvas = document.createElement('canvas');
+            const ctx = canvas.getContext('2d');
+            const font = fonts[Math.floor(Math.random() * fonts.length)];
             
-            const list = Object.keys(tagCounts).map(k => [k, 20 + tagCounts[k] * 10]);
-            const canvas = document.getElementById('word-cloud');
-            const wrapper = document.getElementById('cloud-wrapper');
+            ctx.font = font;
+            const metrics = ctx.measureText(text);
+            const w = metrics.width + 40;
+            const h = 100;
             
-            canvas.width = wrapper.offsetWidth;
-            canvas.height = wrapper.offsetHeight;
+            canvas.width = w; canvas.height = h;
+            
+            // Random Color vivid - High Contrast for Dark Mode
             const isDark = html.getAttribute('data-theme') === 'dark';
             
-            WordCloud(canvas, {
-                list: list,
-                gridSize: 15,
-                weightFactor: 1.3,
-                fontFamily: 'Inter, sans-serif',
-                color: (word, weight) => {
-                    const hue = Math.floor(Math.random() * 360);
-                    return `hsl(${hue}, ${isDark ? '80%' : '60%'}, ${isDark ? '70%' : '40%'})`;
-                },
-                backgroundColor: 'transparent',
-                rotateRatio: 0,
-                shrinkToFit: true,
-                drawOutOfBound: false,
-                click: (item) => {
-                    if(item) {
-                        applyFilter(item[0]);
-                        window.scrollTo({ top: filterBar.offsetTop - 80, behavior: 'smooth' });
-                    }
-                },
-                hover: (item) => { wrapper.style.cursor = item ? 'pointer' : 'default'; }
+            // Use index to influence hue for nearby color grouping, plus some randomness
+            const baseHue = (index * 137.5) % 360; // Golden angle distribution for hues
+            const hue = (baseHue + Math.random() * 30 - 15) % 360; 
+            
+            // High Lightness and Saturation for contrast against dark background
+            const lightness = isDark ? '75%' : '40%'; 
+            const saturation = isDark ? '85%' : '70%';
+            const color = `hsl(${hue}, ${saturation}, ${lightness})`;
+
+            ctx.font = font;
+            ctx.fillStyle = color;
+            ctx.textAlign = 'center';
+            ctx.textBaseline = 'middle';
+            ctx.fillText(text, w/2, h/2);
+            
+            const tex = new THREE.CanvasTexture(canvas);
+            tex.minFilter = THREE.LinearFilter;
+            return { tex, ratio: w/h };
+        }
+
+        function init3DCloud() {
+            const container = document.getElementById('cloud-wrapper');
+            if(!container) return;
+            container.innerHTML = '<div id="cloud-overlay">Ruota &bull; Zooma &bull; Esplora</div>';
+
+            const w = container.offsetWidth;
+            const h = container.offsetHeight;
+
+            scene = new THREE.Scene();
+            // Colore sfondo dinamico dalla pagina (CSS variable)
+            const style = getComputedStyle(document.body);
+            const bgColor = style.getPropertyValue('--md-sys-color-surface-container').trim();
+            scene.background = new THREE.Color(bgColor || (html.getAttribute('data-theme') === 'dark' ? 0x000000 : 0xf0f4f8));
+            scene.fog = new THREE.FogExp2(scene.background, 0.002);
+
+            camera = new THREE.PerspectiveCamera(50, w/h, 1, 1000);
+            
+            // IMMERSIVE VIEW: Camera at CENTER, Orbit Enabled
+            camera.position.set(0, 0, 0.1); 
+
+            renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
+            renderer.setSize(w, h);
+            container.appendChild(renderer.domElement);
+
+            controls = new OrbitControls(camera, renderer.domElement);
+            controls.enableDamping = true;
+            controls.autoRotate = true;
+            controls.autoRotateSpeed = 0.5;
+            controls.enableZoom = true;
+            controls.enablePan = false; 
+            
+            // Invert controls for "looking around" feel from inside
+            controls.rotateSpeed = -0.5;
+
+            group = new THREE.Group();
+            scene.add(group);
+
+            // Analisi Dati
+            const tagCounts = {};
+            safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
+            const tags = Object.keys(tagCounts);
+            const maxCount = Math.max(...Object.values(tagCounts));
+            const minCount = Math.min(...Object.values(tagCounts));
+
+            // Fibonacci Sphere Algorithm per distribuzione uniforme
+            const phi = Math.PI * (3 - Math.sqrt(5)); // Golden Angle
+            const n = tags.length;
+            const radius = 90; // Raggio della sfera
+
+            tags.forEach((tag, i) => {
+                const y = 1 - (i / (n - 1)) * 2; // y va da 1 a -1
+                const radiusAtY = Math.sqrt(1 - y * y); // raggio al livello y
+                const theta = phi * i; // Golden angle increment
+
+                const x = Math.cos(theta) * radiusAtY;
+                const z = Math.sin(theta) * radiusAtY;
+
+                // Calcolo Dimensione
+                const count = tagCounts[tag];
+                const normalized = (count - minCount) / (maxCount - minCount || 1); 
+                const scale = 20 + (normalized * 25); 
+
+                const { tex, ratio } = createTextTexture(tag, scale, i);
+                const mat = new THREE.SpriteMaterial({ map: tex, transparent: true, depthTest: false });
+                const sprite = new THREE.Sprite(mat);
+                
+                sprite.scale.set(scale * ratio, scale, 1);
+                sprite.position.set(x * radius, y * radius, z * radius);
+                
+                // Salva dati per interazione
+                sprite.userData = { tag: tag };
+                group.add(sprite);
+            });
+
+            // Costellazione (Linee)
+            const lineMat = new THREE.LineBasicMaterial({ 
+                color: html.getAttribute('data-theme') === 'dark' ? 0xffffff : 0x000000, 
+                transparent: true, opacity: 0.1 
+            });
+            
+            const lineGeo = new THREE.BufferGeometry();
+            const linePos = [];
+
+            // Collega ogni nodo ai 2 vicini più prossimi
+            for(let i=0; i<group.children.length; i++) {
+                const posA = group.children[i].position;
+                let neighbors = [];
+                
+                for(let j=0; j<group.children.length; j++) {
+                    if(i===j) continue;
+                    const dist = posA.distanceTo(group.children[j].position);
+                    neighbors.push({ idx: j, dist: dist });
+                }
+                neighbors.sort((a,b) => a.dist - b.dist);
+                
+                // Prendi i primi 2 vicini
+                for(let k=0; k<Math.min(2, neighbors.length); k++) {
+                    const posB = group.children[neighbors[k].idx].position;
+                    linePos.push(posA.x, posA.y, posA.z);
+                    linePos.push(posB.x, posB.y, posB.z);
+                }
+            }
+            lineGeo.setAttribute('position', new THREE.Float32BufferAttribute(linePos, 3));
+            const lines = new THREE.LineSegments(lineGeo, lineMat);
+            group.add(lines);
+
+            // Raycaster per click
+            const raycaster = new THREE.Raycaster();
+            const mouse = new THREE.Vector2();
+
+            renderer.domElement.addEventListener('click', (event) => {
+                const rect = renderer.domElement.getBoundingClientRect();
+                mouse.x = ((event.clientX - rect.left) / rect.width) * 2 - 1;
+                mouse.y = -((event.clientY - rect.top) / rect.height) * 2 + 1;
+                
+                raycaster.setFromCamera(mouse, camera);
+                const intersects = raycaster.intersectObjects(group.children.filter(c => c.isSprite));
+                
+                if(intersects.length > 0) {
+                    const tag = intersects[0].object.userData.tag;
+                    window.applyFilter(tag);
+                    window.scrollTo({ top: filterBar.offsetTop - 80, behavior: 'smooth' });
+                }
             });
+
+            animate();
+        }
+
+        function animate() {
+            requestAnimationFrame(animate);
+            controls.update();
+            renderer.render(scene, camera);
+        }
+
+        window.rebuildCloud = () => {
+            const el = document.getElementById('cloud-wrapper');
+            if(el) { 
+                el.innerHTML = ''; 
+                init3DCloud(); 
+            }
         };
 
-        window.onload = function() {
+        // --- INIT ---
+        window.addEventListener('load', () => {
             initFilters();
-            renderCards('all');
-            setTimeout(initWordCloud, 100);
-        };
+            window.applyFilter('all');
+            setTimeout(init3DCloud, 100);
+        });
+
+        window.addEventListener('resize', () => {
+            if(camera && renderer) {
+                const c = document.getElementById('cloud-wrapper');
+                camera.aspect = c.offsetWidth / c.offsetHeight;
+                camera.updateProjectionMatrix();
+                renderer.setSize(c.offsetWidth, c.offsetHeight);
+            }
+        });
 
-        window.onresize = () => {
-            clearTimeout(window.resizeTimer);
-            window.resizeTimer = setTimeout(window.initWordCloud, 200);
-        };
     </script>
 </body>
 </html>
\ No newline at end of file

commit 2fcbb9e80ad55d27f47d4b03ab777407ad287e9e
Author: federicodb <vivasantarosa@gmail.com>
Date:   Mon Dec 8 17:40:27 2025 +0100

    moficica di ogni lezione, correzione tag

diff --git a/index.html b/index.html
index 3b6c3e3..75ac240 100644
--- a/index.html
+++ b/index.html
@@ -3,12 +3,17 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
+    
+    <!-- 1. META DATI -->
     <title>Laboratorio Matematico Orfini Foligno</title>
+    <meta name="description" content="Hub digitale per la didattica laboratoriale della Matematica e Fisica. WebApp, simulazioni e giochi per l'Istituto Professionale Orfini.">
+    <meta name="keywords" content="Matematica, Fisica, Ed. Civica, Laboratorio, Orfini, Gamification, PBL, STEM, MeltingMath">
+    <meta name="date" content="2025-12-08">
     
-    <!-- Font Inter: Lo standard per UI moderne e leggibili -->
+    <!-- Font Inter & Icons -->
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
-    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
+    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
     
     <!-- WordCloud Library -->
     <script src="https://cdnjs.cloudflare.com/ajax/libs/wordcloud2.js/1.2.2/wordcloud2.min.js"></script>
@@ -20,8 +25,10 @@
             --md-sys-color-background: #fdfcff;
             --md-sys-color-on-background: #1a1c1e;
             --md-sys-color-surface: #fdfcff;
-            --md-sys-color-surface-container: #f0f4f8; /* Grigio/Bluastro chiarissimo */
+            --md-sys-color-surface-container: #f0f4f8; 
             --md-sys-color-surface-container-high: #e2e8f0;
+            --md-sys-color-surface-variant: #e1e2ec;
+            --md-sys-color-on-surface-variant: #44474f;
             
             --md-sys-color-primary: #0061a4;
             --md-sys-color-on-primary: #ffffff;
@@ -34,23 +41,28 @@
             /* Spaziature e Forme */
             --md-shape-corner-large: 24px;
             --md-shape-corner-medium: 16px;
-            --md-shape-corner-full: 999px; /* Pillole */
+            --md-shape-corner-full: 999px;
             
-            /* Palette Dinamica Card (Tone 80/40) */
+            /* Palette Dinamica Card */
             --card-blue: #d3e3fd; --card-on-blue: #041e49;
             --card-green: #c4eed0; --card-on-green: #07210e;
             --card-red: #f9dedc; --card-on-red: #410e0b;
             --card-yellow: #fdf2b4; --card-on-yellow: #261d00;
             --card-purple: #e9ddff; --card-on-purple: #21005d;
             --card-teal: #bce9eb; --card-on-teal: #001f20;
+
+            /* Brand Colors */
+            --instagram-grad: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
         }
 
         [data-theme="dark"] {
-            --md-sys-color-background: #0a0a0a; /* Nero AMOLED Profondo */
+            --md-sys-color-background: #0a0a0a; 
             --md-sys-color-on-background: #e2e2e6;
             --md-sys-color-surface: #111111;
-            --md-sys-color-surface-container: #1c1c1c; /* Grigio scuro per le card */
+            --md-sys-color-surface-container: #1c1c1c; 
             --md-sys-color-surface-container-high: #2d2d2d;
+            --md-sys-color-surface-variant: #44474f;
+            --md-sys-color-on-surface-variant: #c4c7c5;
             
             --md-sys-color-primary: #9ecaff;
             --md-sys-color-on-primary: #003258;
@@ -60,7 +72,7 @@
             --md-sys-color-outline: #8e918f;
             --md-sys-color-outline-variant: #444746;
 
-            /* Palette Dark (Tone 30/90) - Colori più scuri e saturi per sfondo */
+            /* Palette Dark */
             --card-blue: #0842a0; --card-on-blue: #d3e3fd;
             --card-green: #0f5223; --card-on-green: #c4eed0;
             --card-red: #93000a; --card-on-red: #ffdad6;
@@ -70,6 +82,7 @@
         }
 
         /* --- RESET & TYPOGRAPHY --- */
+        html { scroll-behavior: smooth; }
         * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
 
         body {
@@ -78,15 +91,13 @@
             color: var(--md-sys-color-on-background);
             font-family: 'Inter', sans-serif;
             min-height: 100dvh;
+            display: flex; flex-direction: column;
             transition: background-color 0.4s cubic-bezier(0.2, 0, 0, 1), color 0.4s;
             overflow-x: hidden;
         }
 
         /* --- HEADER & HERO --- */
-        header {
-            padding: 1.5rem;
-            max-width: 1200px; margin: 0 auto;
-        }
+        header { padding: 1.5rem; max-width: 1200px; margin: 0 auto; width: 100%; }
 
         .top-bar {
             display: flex; justify-content: space-between; align-items: center;
@@ -94,9 +105,8 @@
         }
 
         h1 {
-            font-size: 1.75rem; font-weight: 700; line-height: 1.2; margin: 0;
+            font-size: 1.75rem; font-weight: 900; line-height: 1.2; margin: 0;
             letter-spacing: -0.5px;
-            /* Gradiente sottile sul testo */
             background: linear-gradient(90deg, var(--md-sys-color-on-background), var(--md-sys-color-outline));
             -webkit-background-clip: text; -webkit-text-fill-color: transparent;
         }
@@ -105,7 +115,6 @@
             font-size: 0.9rem; color: var(--md-sys-color-outline); margin-top: 0.2rem; font-weight: 500;
         }
 
-        /* Theme Switch (Material Icon Button) */
         .theme-btn {
             width: 48px; height: 48px; border-radius: 50%;
             border: 1px solid var(--md-sys-color-outline-variant);
@@ -115,24 +124,21 @@
         }
         .theme-btn:hover { background-color: var(--md-sys-color-surface-container-high); }
 
-        /* --- WORD CLOUD CONTAINER --- */
+        /* --- WORD CLOUD --- */
         #cloud-wrapper {
             width: 100%; height: 220px;
             background-color: var(--md-sys-color-surface-container);
             border-radius: var(--md-shape-corner-large);
             margin-bottom: 2rem;
             position: relative; overflow: hidden;
-            /* Ombra interna per profondità */
             box-shadow: inset 0 2px 10px rgba(0,0,0,0.05);
-            padding: 10px; /* Padding richiesto */
-            cursor: pointer;
+            padding: 10px; cursor: pointer;
         }
 
-        /* --- FILTRI (CHIPS) --- */
+        /* --- FILTRI --- */
         .filters-container {
             position: sticky; top: 0; z-index: 50;
-            background: rgba(var(--md-sys-color-background), 0.95); /* Fallback non standard ma utile */
-            background: var(--md-sys-color-background); /* Solido per coprire scroll */
+            background: var(--md-sys-color-background);
             padding: 0.5rem 0 1rem;
             border-bottom: 1px solid var(--md-sys-color-outline-variant);
             margin-bottom: 1.5rem;
@@ -150,17 +156,12 @@
             background-color: var(--md-sys-color-surface);
             border: 1px solid var(--md-sys-color-outline-variant);
             color: var(--md-sys-color-on-background);
-            padding: 0.5rem 1rem;
-            border-radius: 8px; /* Formella leggermente squadrata stile Material 3 */
+            padding: 0.5rem 1rem; border-radius: 8px;
             font-size: 0.85rem; font-weight: 500;
             white-space: nowrap; cursor: pointer; user-select: none;
-            transition: all 0.2s cubic-bezier(0.2, 0, 0, 1);
-        }
-        
-        .chip:hover {
-            background-color: var(--md-sys-color-surface-container-high);
+            transition: all 0.2s;
         }
-
+        .chip:hover { background-color: var(--md-sys-color-surface-container-high); }
         .chip.active {
             background-color: var(--md-sys-color-primary);
             color: var(--md-sys-color-on-primary);
@@ -169,96 +170,104 @@
         }
 
         /* --- GRIGLIA CARD --- */
-        main {
-            padding: 0 1.5rem 4rem; max-width: 1200px; margin: 0 auto;
-        }
+        main { padding: 0 1.5rem 4rem; max-width: 1200px; margin: 0 auto; flex: 1; width: 100%; }
 
         .grid {
-            display: grid;
-            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
-            gap: 1.5rem;
+            display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem;
         }
 
         .card {
             background-color: var(--card-bg, var(--md-sys-color-surface-container));
             color: var(--card-fg, var(--md-sys-color-on-background));
             border-radius: var(--md-shape-corner-large);
-            padding: 1.5rem;
-            text-decoration: none;
+            padding: 1.5rem; text-decoration: none;
             display: flex; flex-direction: column;
             transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s;
             position: relative; overflow: hidden;
             border: 1px solid transparent;
         }
-
-        /* Effetto Hover "Lift" */
         @media (hover: hover) {
-            .card:hover {
-                transform: translateY(-6px) scale(1.01);
-                box-shadow: 0 12px 24px -8px rgba(0,0,0,0.3);
-                z-index: 10;
-            }
+            .card:hover { transform: translateY(-6px) scale(1.01); box-shadow: 0 12px 24px -8px rgba(0,0,0,0.3); z-index: 10; }
         }
 
-        /* Testata Card */
-        .card-header {
-            display: flex; justify-content: space-between; align-items: flex-start;
-            margin-bottom: 1rem;
-        }
-
-        .card-emoji {
-            font-size: 3rem; 
-            filter: drop-shadow(0 4px 6px rgba(0,0,0,0.1));
-            transition: transform 0.3s;
-        }
+        .card-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 1rem; }
+        .card-emoji { font-size: 3rem; filter: drop-shadow(0 4px 6px rgba(0,0,0,0.1)); transition: transform 0.3s; }
         .card:hover .card-emoji { transform: scale(1.1) rotate(5deg); }
-
+        
         .card-date {
             font-size: 0.75rem; font-weight: 600; opacity: 0.6;
             background: rgba(0,0,0,0.1); padding: 4px 8px; border-radius: 6px;
             font-family: monospace;
-            [data-theme="dark"] & { background: rgba(255,255,255,0.1); }
         }
+        [data-theme="dark"] .card-date { background: rgba(255,255,255,0.1); }
 
-        .card h3 {
-            font-size: 1.4rem; font-weight: 700; margin: 0 0 0.5rem 0;
-            line-height: 1.2; letter-spacing: -0.01em;
+        .card h3 { font-size: 1.4rem; font-weight: 700; margin: 0 0 0.5rem 0; line-height: 1.2; letter-spacing: -0.01em; }
+        .card p { font-size: 0.95rem; opacity: 0.8; margin: 0 0 1.5rem 0; line-height: 1.5; flex-grow: 1; font-weight: 400; }
+        .card-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: auto; }
+        .tag-pill {
+            font-size: 0.7rem; font-weight: 600; padding: 4px 10px; border-radius: 50px;
+            background-color: rgba(255,255,255,0.2); color: inherit; border: 1px solid rgba(255,255,255,0.1);
         }
 
-        .card p {
-            font-size: 0.95rem; opacity: 0.8; margin: 0 0 1.5rem 0;
-            line-height: 1.5; flex-grow: 1; font-weight: 400;
+        /* --- FOOTER (NUOVO) --- */
+        .site-footer {
+            background-color: var(--md-sys-color-surface-container);
+            border-top: 1px solid var(--md-sys-color-outline-variant);
+            padding: 3rem 1.5rem; margin-top: auto;
         }
-
-        /* Tags Footer */
-        .card-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: auto; }
         
-        .tag-pill {
-            font-size: 0.7rem; font-weight: 600;
-            padding: 4px 10px; border-radius: 50px;
-            background-color: rgba(255,255,255,0.2); /* Semicorpo per adattarsi al colore card */
-            color: inherit;
-            border: 1px solid rgba(255,255,255,0.1);
+        .footer-content {
+            max-width: 1200px; margin: 0 auto;
+            display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
+            gap: 2rem;
         }
 
-        /* --- STATI VUOTI E UTILITY --- */
-        #empty-state {
-            grid-column: 1 / -1; text-align: center; padding: 4rem 1rem;
-            color: var(--md-sys-color-outline);
+        .footer-section h4 {
+            font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.1em;
+            color: var(--md-sys-color-primary); margin-bottom: 1rem; font-weight: 800;
+        }
+
+        .footer-section p {
+            font-size: 0.95rem; line-height: 1.6; color: var(--md-sys-color-on-surface-variant); margin-bottom: 1rem;
+        }
+
+        .footer-link {
+            display: inline-flex; items-center; gap: 0.5rem;
+            color: var(--md-sys-color-on-background); text-decoration: none;
+            font-weight: 600; font-size: 0.95rem; transition: color 0.2s;
+        }
+        .footer-link:hover { color: var(--md-sys-color-primary); }
+
+        /* Card MeltingMath */
+        .mm-card {
+            background: var(--md-sys-color-surface);
+            border: 1px solid var(--md-sys-color-outline-variant);
+            border-radius: var(--md-shape-corner-medium);
+            padding: 1.5rem; display: flex; align-items: center; gap: 1rem;
+            transition: transform 0.2s;
+            text-decoration: none; /* RIMUOVE LA SOTTOLINEATURA */
+            color: inherit; /* RIMUOVE IL BLU STANDARD */
+        }
+        .mm-card:hover { transform: translateY(-3px); border-color: #d62976; }
+        
+        .mm-icon {
+            width: 48px; height: 48px; border-radius: 12px;
+            background: var(--instagram-grad);
+            display: flex; align-items: center; justify-content: center;
+            color: white; font-size: 1.5rem; flex-shrink: 0;
         }
 
+        /* Utility */
+        #empty-state { grid-column: 1 / -1; text-align: center; padding: 4rem 1rem; color: var(--md-sys-color-outline); }
         #reset-filter {
-            display: none; margin: 0 0 1rem auto;
-            background: none; border: 1px solid var(--md-sys-color-outline);
-            color: var(--md-sys-color-on-background); padding: 6px 12px;
-            border-radius: 8px; cursor: pointer; font-size: 0.8rem;
+            display: none; margin: 0 0 1rem auto; background: none; border: 1px solid var(--md-sys-color-outline);
+            color: var(--md-sys-color-on-background); padding: 6px 12px; border-radius: 8px; cursor: pointer; font-size: 0.8rem;
         }
 
-        /* Mobile Fixes */
         @media (max-width: 600px) {
             h1 { font-size: 1.5rem; }
             .grid { grid-template-columns: 1fr; }
-            .card { padding: 1.25rem; border-radius: 20px; }
+            .footer-content { grid-template-columns: 1fr; }
         }
     </style>
 </head>
@@ -283,7 +292,7 @@
     <div class="filters-container">
         <div class="filters" id="filterBar">
             <button class="chip active" onclick="applyFilter('all')">Tutti</button>
-            <!-- I bottoni verranno generati qui SOLO per i tag esistenti -->
+            <!-- Tags generati dinamicamente -->
         </div>
     </div>
 
@@ -293,7 +302,7 @@
         </div>
 
         <div class="grid" id="grid">
-            <!-- Le card verranno inserite qui -->
+            <!-- Cards generate dinamicamente -->
         </div>
         
         <div id="empty-state">
@@ -302,7 +311,120 @@
         </div>
     </main>
 
-    <!-- CARICAMENTO DATABASE -->
+    <!-- FOOTER INFORMATIVO -->
+    <footer class="site-footer">
+        <div class="footer-content">
+            
+            <!-- SEZIONE CHI SONO -->
+            <div class="footer-section">
+                <h4>Chi Sono</h4>
+                <p>
+                    Sono il <strong>Prof. Federico DB</strong>, docente di Matematica presso l'Istituto Professionale Orfini di Foligno. 
+                    Il mio obiettivo è trasformare l'astrazione della Matematica in esperienza tangibile.
+                </p>
+                <p>
+                    Ogni studente deve poter "toccare" i concetti scientifici per comprenderli davvero.
+                </p>
+            </div>
+
+            <!-- SEZIONE METODOLOGIA -->
+            <div class="footer-section">
+                <h4>Note Metodologiche</h4>
+                <p>
+                    Questo laboratorio segue le linee guida della <strong>Riforma degli Istituti Professionali (D.Lgs 61/2017)</strong>.
+                </p>
+                <ul style="padding-left: 1rem; color: var(--md-sys-color-on-surface-variant); font-size: 0.9rem; line-height: 1.6;">
+                    <li><strong>PBL:</strong> Apprendimento basato su progetti reali.</li>
+                    <li><strong>Gamification:</strong> L'errore è parte del gioco, non un fallimento.</li>
+                    <li><strong>Open Source:</strong> Codice accessibile e modificabile.</li>
+                </ul>
+            </div>
+
+            <!-- SEZIONE MELTINGMATH -->
+            <div class="footer-section">
+                <h4>Social & Community</h4>
+                <!-- Aggiunta classe e stili per fixare il colore del link -->
+                <a href="https://instagram.com/meltingmath" target="_blank" class="mm-card">
+                    <div class="mm-icon">
+                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
+                    </div>
+                    <div>
+                        <!-- Colore primario applicato esplicitamente -->
+                        <div style="font-weight: 800; font-size: 1rem; color: var(--md-sys-color-primary);">MeltingMath</div>
+                        <div style="font-size: 0.8rem; color: var(--md-sys-color-outline);">Segui il progetto su Instagram</div>
+                    </div>
+                </a>
+            </div>
+
+        </div>
+        <div style="text-align: center; margin-top: 2rem; font-size: 0.75rem; color: var(--md-sys-color-outline);">
+            &copy; 2025 Laboratorio Matematico Orfini Foligno - Engineered by Orfini Math Lab Architect
+        </div>
+    </footer>
+
+    <!-- SCRIPT DATA (INLINE PER ANTEPRIMA) -->
+    <script>
+        const inlineDB = [
+            {
+                title: "Math Underground: Mappa Funzioni",
+                url: "math_underground003_4el.html",
+                excerpt: "Mappa concettuale interattiva in stile metropolitana per esplorare la classificazione delle funzioni analitiche.",
+                tags: ["Matematica", "Analisi", "Funzioni", "4EL"],
+                date: "2025-12-06"
+            },
+            {
+                title: "Caccia all'Errore: Operazioni in Z",
+                url: "memory_1el_numeri_004.html",
+                excerpt: "Missione di addestramento sui Numeri Interi (Z) e le Potenze. Trova le corrispondenze corrette.",
+                tags: ["Matematica", "Aritmetica", "1EL"],
+                date: "2025-10-20"
+            },
+            {
+                title: "GonioMatch: Segnali Goniometrici",
+                url: "gioco_sin_cos_01.html",
+                excerpt: "Gioco Memory a tema 'Radar' per memorizzare gli angoli notevoli di Seno, Coseno e Tangente.",
+                tags: ["Matematica", "Goniometria", "3MEC", "4EL"],
+                date: "2025-12-08"
+            },
+            {
+                title: "Voto Consapevole: Simulatore Nash",
+                url: "voto_consapevole_2.5.html",
+                excerpt: "Simulazione interattiva sull'Equilibrio di Nash applicato ai voti scolastici.",
+                tags: ["Ed. Civica", "Matematica", "3EL", "4EL"],
+                date: "2025-10-01"
+            },
+            {
+                title: "Algebra Tiles 3D",
+                url: "visualizzatore polinomi 2d 3d 001.html",
+                excerpt: "Laboratorio interattivo 2D/3D per visualizzare la fattorizzazione di polinomi come aree e volumi.",
+                tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"],
+                date: "2025-12-08"
+            },
+            {
+                title: "Math Invaders: MCD & mcm",
+                url: "MCD_mcm_1el_001.html",
+                excerpt: "Videogioco arcade in pixel-art per allenare il calcolo mentale di M.C.D. e m.c.m.",
+                tags: ["Matematica", "Aritmetica", "1EL"],
+                date: "2025-12-08"
+            },
+            {
+                title: "Scomposizione Polinomi (Memory)",
+                url: "cards_polinomi_memory004.html",
+                excerpt: "Gioco Memory per allenare il riconoscimento dei prodotti notevoli e delle scomposizioni.",
+                tags: ["Matematica", "Algebra", "1EL", "2EL", "2GP"],
+                date: "2025-12-08"
+            },
+            {
+                title: "Laboratorio Frazioni",
+                url: "fractions_lab_001.html",
+                excerpt: "Simulatore interattivo per comprendere le operazioni tra frazioni attraverso modelli visivi.",
+                tags: ["Matematica", "Aritmetica", "1EL", "2EL", "2GP"],
+                date: "2025-12-08"
+            }
+        ];
+    </script>
+
+    <!-- CARICAMENTO DATABASE ESTERNO (FALLBACK SU INLINE) -->
     <script src="database.js"></script>
 
     <script>
@@ -322,30 +444,19 @@
             html.setAttribute('data-theme', t);
             localStorage.setItem('theme', t);
             themeIcon.innerText = t === 'dark' ? '☀️' : '🌙';
-            // Ridisegna la cloud per aggiornare i colori
             if(window.initWordCloud) setTimeout(window.initWordCloud, 100);
         }
 
         // --- 2. CONFIGURAZIONE EMOJI E COLORI ---
-        const safeData = (typeof db !== 'undefined') ? db : [];
+        // Usa inlineDB se db non è definito (fallback per anteprima)
+        const safeData = (typeof db !== 'undefined') ? db : inlineDB;
 
-        // Mappa Emoji Specifica per File/Contenuto
         const specificEmojiMap = {
-            'voto': '⚖️',      // Teoria Giochi / Equilibrio
-            'nash': '🧠',
-            'memory': '🧩',    // Memory generico
-            'polinomi': '🧱',  // Algebra tiles
-            'underground': '🚇', // Mappa metro
-            'mercatore': '🌍', // Mappe geo
-            'corrente': '⚡',   // Elettrotecnica
-            'goniomatch': '📡', // Radar
-            'tavola': '🔢',    // Pitagora/Tartaglia
-            'insiemi': '🔴',   // Venn
-            'frazioni': '🍰',  // Torte
-            'sketch': '🎨'     // Arte
+            'voto': '⚖️', 'nash': '🧠', 'memory': '🧩', 'polinomi': '🧱',
+            'underground': '🚇', 'mercatore': '🌍', 'corrente': '⚡',
+            'goniomatch': '📡', 'tavola': '🔢', 'insiemi': '🔴', 'frazioni': '🍰', 'sketch': '🎨'
         };
 
-        // Palette Colori (CSS Variables definite sopra)
         const themeColors = [
             { bg: 'var(--card-blue)', fg: 'var(--card-on-blue)' },
             { bg: 'var(--card-green)', fg: 'var(--card-on-green)' },
@@ -355,46 +466,33 @@
             { bg: 'var(--card-yellow)', fg: 'var(--card-on-yellow)' }
         ];
 
-        // Funzione Hash per colore deterministico (stesso titolo = stesso colore sempre)
         function getStableColor(str) {
             let hash = 0;
-            for (let i = 0; i < str.length; i++) {
-                hash = str.charCodeAt(i) + ((hash << 5) - hash);
-            }
-            const index = Math.abs(hash) % themeColors.length;
-            return themeColors[index];
+            for (let i = 0; i < str.length; i++) hash = str.charCodeAt(i) + ((hash << 5) - hash);
+            return themeColors[Math.abs(hash) % themeColors.length];
         }
 
         function getSmartEmoji(item) {
-            // 1. Cerca nel nome file o titolo una parola chiave specifica
             const keyString = (item.url + item.title).toLowerCase();
             for (const [key, emo] of Object.entries(specificEmojiMap)) {
                 if (keyString.includes(key)) return emo;
             }
-            // 2. Fallback sui Tag
             if (item.tags.some(t => t.includes('Fisica'))) return '⚛️';
             if (item.tags.some(t => t.includes('Matematica'))) return '📐';
-            
-            return '📄'; // Default assoluto
+            return '📄';
         }
 
-        // --- 3. GENERAZIONE FILTRI CONTESTUALI ---
+        // --- 3. GENERAZIONE FILTRI ---
         const filterBar = document.getElementById('filterBar');
 
         function initFilters() {
-            // Raccogli solo i tag veramente usati
             const tagCounts = {};
-            safeData.forEach(item => {
-                item.tags.forEach(t => { tagCounts[t] = (tagCounts[t] || 0) + 1; });
-            });
-
-            // Ordina per frequenza (i più popolari prima)
+            safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
             const sortedTags = Object.keys(tagCounts).sort((a,b) => tagCounts[b] - tagCounts[a]);
 
             sortedTags.forEach(tag => {
                 const btn = document.createElement('button');
                 btn.className = 'chip';
-                // Mostra il numero solo se > 1 per pulizia
                 const label = tagCounts[tag] > 1 ? `${tag} (${tagCounts[tag]})` : tag;
                 btn.innerText = label;
                 btn.onclick = () => applyFilter(tag);
@@ -411,7 +509,6 @@
             grid.innerHTML = '';
             let count = 0;
 
-            // UI Filtri
             document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
             const activeBtn = Array.from(document.querySelectorAll('.chip')).find(c => c.innerText.startsWith(filter === 'all' ? 'Tutti' : filter));
             if(activeBtn) activeBtn.classList.add('active');
@@ -420,25 +517,19 @@
 
             safeData.forEach(item => {
                 const match = filter === 'all' || item.tags.includes(filter);
-                
                 if (match) {
                     count++;
                     const card = document.createElement('a');
                     card.href = item.url;
                     card.className = 'card';
                     
-                    // Applica Colore Deterministico
                     const colors = getStableColor(item.title);
                     card.style.setProperty('--card-bg', colors.bg);
                     card.style.setProperty('--card-fg', colors.fg);
 
                     const emoji = getSmartEmoji(item);
-                    
-                    // Formattazione data
                     const d = new Date(item.date);
                     const dateStr = !isNaN(d) ? `${d.getDate()}/${d.getMonth()+1}/${d.getFullYear()}` : item.date;
-                    
-                    // Tag HTML (limitati a 3 per spazio)
                     const tagsHtml = item.tags.slice(0, 3).map(t => `<span class="tag-pill">#${t}</span>`).join('');
 
                     card.innerHTML = `
@@ -453,13 +544,10 @@
                     grid.appendChild(card);
                 }
             });
-
             emptyState.style.display = count === 0 ? 'block' : 'none';
         }
 
-        window.applyFilter = function(tag) {
-            renderCards(tag);
-        }
+        window.applyFilter = function(tag) { renderCards(tag); }
 
         // --- 5. WORD CLOUD ---
         window.initWordCloud = function() {
@@ -467,14 +555,12 @@
             const tagCounts = {};
             safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
             
-            const list = Object.keys(tagCounts).map(k => [k, 20 + tagCounts[k] * 10]); // Peso base 20
+            const list = Object.keys(tagCounts).map(k => [k, 20 + tagCounts[k] * 10]);
             const canvas = document.getElementById('word-cloud');
             const wrapper = document.getElementById('cloud-wrapper');
             
             canvas.width = wrapper.offsetWidth;
             canvas.height = wrapper.offsetHeight;
-
-            // Colore testo adattivo al tema
             const isDark = html.getAttribute('data-theme') === 'dark';
             
             WordCloud(canvas, {
@@ -483,34 +569,26 @@
                 weightFactor: 1.3,
                 fontFamily: 'Inter, sans-serif',
                 color: (word, weight) => {
-                    // Colori più o meno saturi in base al tema
                     const hue = Math.floor(Math.random() * 360);
-                    const sat = isDark ? '80%' : '60%';
-                    const light = isDark ? '70%' : '40%';
-                    return `hsl(${hue}, ${sat}, ${light})`;
+                    return `hsl(${hue}, ${isDark ? '80%' : '60%'}, ${isDark ? '70%' : '40%'})`;
                 },
                 backgroundColor: 'transparent',
-                rotateRatio: 0, // Solo orizzontale per leggibilità
+                rotateRatio: 0,
                 shrinkToFit: true,
                 drawOutOfBound: false,
-                
                 click: (item) => {
                     if(item) {
                         applyFilter(item[0]);
                         window.scrollTo({ top: filterBar.offsetTop - 80, behavior: 'smooth' });
                     }
                 },
-                hover: (item) => {
-                    wrapper.style.cursor = item ? 'pointer' : 'default';
-                }
+                hover: (item) => { wrapper.style.cursor = item ? 'pointer' : 'default'; }
             });
         };
 
-        // --- BOOTSTRAP ---
         window.onload = function() {
             initFilters();
             renderCards('all');
-            // Ritardo per assicurare che il font sia caricato per la wordcloud
             setTimeout(initWordCloud, 100);
         };
 

commit dd062963592f7ef8d52bf2791c771ff4e9e30ebb
Author: federicodb <vivasantarosa@gmail.com>
Date:   Mon Dec 8 14:42:38 2025 +0100

    moficica di ogni lezione, correzione tag

diff --git a/index.html b/index.html
index cd3205a..3b6c3e3 100644
--- a/index.html
+++ b/index.html
@@ -1,250 +1,425 @@
 <!DOCTYPE html>
-<html lang="it">
+<html lang="it" data-theme="dark">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
-    <title>FedericoDB Lab</title>
+    <title>Laboratorio Matematico Orfini Foligno</title>
     
-    <!-- Google Font: Roboto (Lo standard del Material Design) -->
+    <!-- Font Inter: Lo standard per UI moderne e leggibili -->
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
-    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
+    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
     
+    <!-- WordCloud Library -->
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/wordcloud2.js/1.2.2/wordcloud2.min.js"></script>
+
     <style>
-        /* --- MATERIAL DESIGN 3 (Dark AMOLED Theme) --- */
+        /* --- MATERIAL DESIGN 3 TOKENS --- */
         :root {
-            --md-sys-color-background: #000000;       /* Nero AMOLED */
-            --md-sys-color-surface: #141414;          /* Grigio scurissimo per le card */
-            --md-sys-color-surface-variant: #282828;  /* Grigio per hover/bordi */
+            /* Palette Material 3 (Base) */
+            --md-sys-color-background: #fdfcff;
+            --md-sys-color-on-background: #1a1c1e;
+            --md-sys-color-surface: #fdfcff;
+            --md-sys-color-surface-container: #f0f4f8; /* Grigio/Bluastro chiarissimo */
+            --md-sys-color-surface-container-high: #e2e8f0;
+            
+            --md-sys-color-primary: #0061a4;
+            --md-sys-color-on-primary: #ffffff;
+            --md-sys-color-secondary-container: #dbe4f9;
+            --md-sys-color-on-secondary-container: #171c22;
+            
+            --md-sys-color-outline: #74777f;
+            --md-sys-color-outline-variant: #c4c7c5;
+
+            /* Spaziature e Forme */
+            --md-shape-corner-large: 24px;
+            --md-shape-corner-medium: 16px;
+            --md-shape-corner-full: 999px; /* Pillole */
+            
+            /* Palette Dinamica Card (Tone 80/40) */
+            --card-blue: #d3e3fd; --card-on-blue: #041e49;
+            --card-green: #c4eed0; --card-on-green: #07210e;
+            --card-red: #f9dedc; --card-on-red: #410e0b;
+            --card-yellow: #fdf2b4; --card-on-yellow: #261d00;
+            --card-purple: #e9ddff; --card-on-purple: #21005d;
+            --card-teal: #bce9eb; --card-on-teal: #001f20;
+        }
+
+        [data-theme="dark"] {
+            --md-sys-color-background: #0a0a0a; /* Nero AMOLED Profondo */
+            --md-sys-color-on-background: #e2e2e6;
+            --md-sys-color-surface: #111111;
+            --md-sys-color-surface-container: #1c1c1c; /* Grigio scuro per le card */
+            --md-sys-color-surface-container-high: #2d2d2d;
             
-            --md-sys-color-on-background: #E6E1E5;    /* Testo principale (bianco sporco) */
-            --md-sys-color-on-surface-variant: #CAC4D0; /* Testo secondario */
-            --md-sys-color-outline: #49454F;          /* Bordi sottili */
+            --md-sys-color-primary: #9ecaff;
+            --md-sys-color-on-primary: #003258;
+            --md-sys-color-secondary-container: #33445a;
+            --md-sys-color-on-secondary-container: #dbe4f9;
             
-            --md-elevation-1: 0px 1px 2px rgba(0,0,0,0.3);
-            --md-shape-corner: 16px;                  /* Raggio stondatura schede */
-            --md-shape-pill: 100px;                   /* Raggio filtri */
+            --md-sys-color-outline: #8e918f;
+            --md-sys-color-outline-variant: #444746;
+
+            /* Palette Dark (Tone 30/90) - Colori più scuri e saturi per sfondo */
+            --card-blue: #0842a0; --card-on-blue: #d3e3fd;
+            --card-green: #0f5223; --card-on-green: #c4eed0;
+            --card-red: #93000a; --card-on-red: #ffdad6;
+            --card-yellow: #5d4200; --card-on-yellow: #fff392;
+            --card-purple: #55339e; --card-on-purple: #e9ddff;
+            --card-teal: #004f50; --card-on-teal: #bce9eb;
         }
 
-        /* RESET & BASE */
+        /* --- RESET & TYPOGRAPHY --- */
         * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
-        
+
         body {
             margin: 0; padding: 0;
             background-color: var(--md-sys-color-background);
             color: var(--md-sys-color-on-background);
-            font-family: 'Roboto', sans-serif;
+            font-family: 'Inter', sans-serif;
             min-height: 100dvh;
-            /* Typography */
-            letter-spacing: 0.01em;
-            line-height: 1.5;
+            transition: background-color 0.4s cubic-bezier(0.2, 0, 0, 1), color 0.4s;
+            overflow-x: hidden;
         }
 
-        /* --- HEADER --- */
+        /* --- HEADER & HERO --- */
         header {
-            padding: 3rem 1.5rem 1rem;
-            max-width: 1200px;
-            margin: 0 auto;
+            padding: 1.5rem;
+            max-width: 1200px; margin: 0 auto;
+        }
+
+        .top-bar {
+            display: flex; justify-content: space-between; align-items: center;
+            margin-bottom: 2rem;
         }
 
         h1 {
-            font-size: 2.5rem;
-            font-weight: 400; /* Material Design usa pesi leggeri per titoli grandi */
-            margin: 0 0 0.5rem 0;
-            color: #ffffff;
-            letter-spacing: -0.02em;
+            font-size: 1.75rem; font-weight: 700; line-height: 1.2; margin: 0;
+            letter-spacing: -0.5px;
+            /* Gradiente sottile sul testo */
+            background: linear-gradient(90deg, var(--md-sys-color-on-background), var(--md-sys-color-outline));
+            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
         }
         
-        header p {
-            font-size: 1rem;
-            color: var(--md-sys-color-on-surface-variant);
-            font-weight: 300;
-            margin: 0;
-            max-width: 600px;
+        .subtitle {
+            font-size: 0.9rem; color: var(--md-sys-color-outline); margin-top: 0.2rem; font-weight: 500;
+        }
+
+        /* Theme Switch (Material Icon Button) */
+        .theme-btn {
+            width: 48px; height: 48px; border-radius: 50%;
+            border: 1px solid var(--md-sys-color-outline-variant);
+            background: transparent; color: var(--md-sys-color-on-background);
+            display: flex; align-items: center; justify-content: center;
+            cursor: pointer; transition: all 0.2s; font-size: 1.2rem;
+        }
+        .theme-btn:hover { background-color: var(--md-sys-color-surface-container-high); }
+
+        /* --- WORD CLOUD CONTAINER --- */
+        #cloud-wrapper {
+            width: 100%; height: 220px;
+            background-color: var(--md-sys-color-surface-container);
+            border-radius: var(--md-shape-corner-large);
+            margin-bottom: 2rem;
+            position: relative; overflow: hidden;
+            /* Ombra interna per profondità */
+            box-shadow: inset 0 2px 10px rgba(0,0,0,0.05);
+            padding: 10px; /* Padding richiesto */
+            cursor: pointer;
         }
 
         /* --- FILTRI (CHIPS) --- */
         .filters-container {
-            position: sticky; top: 0; z-index: 100;
-            background: rgba(0,0,0,0.85); /* Sfondo semi-trasparente */
-            backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
-            border-bottom: 1px solid #1f1f1f;
-            padding: 1rem 0;
-            margin-bottom: 2rem;
+            position: sticky; top: 0; z-index: 50;
+            background: rgba(var(--md-sys-color-background), 0.95); /* Fallback non standard ma utile */
+            background: var(--md-sys-color-background); /* Solido per coprire scroll */
+            padding: 0.5rem 0 1rem;
+            border-bottom: 1px solid var(--md-sys-color-outline-variant);
+            margin-bottom: 1.5rem;
+            opacity: 0.98;
         }
 
         .filters {
-            display: flex; gap: 0.75rem; overflow-x: auto; 
+            display: flex; gap: 0.5rem; overflow-x: auto;
             padding: 0 1.5rem; max-width: 1200px; margin: 0 auto;
-            -webkit-overflow-scrolling: touch; 
-            scrollbar-width: none;
+            scrollbar-width: none; -webkit-overflow-scrolling: touch;
         }
         .filters::-webkit-scrollbar { display: none; }
 
-        /* Stile "Filter Chip" Material Design */
         .chip {
-            background: transparent;
-            border: 1px solid var(--md-sys-color-outline);
-            color: var(--md-sys-color-on-surface-variant);
-            padding: 0.5rem 1.25rem;
-            border-radius: 8px; /* Look leggermente più tech/squadrato rispetto al pill */
-            font-size: 0.875rem;
-            font-weight: 500;
-            white-space: nowrap;
-            cursor: pointer;
-            transition: all 0.2s ease;
-            user-select: none;
+            background-color: var(--md-sys-color-surface);
+            border: 1px solid var(--md-sys-color-outline-variant);
+            color: var(--md-sys-color-on-background);
+            padding: 0.5rem 1rem;
+            border-radius: 8px; /* Formella leggermente squadrata stile Material 3 */
+            font-size: 0.85rem; font-weight: 500;
+            white-space: nowrap; cursor: pointer; user-select: none;
+            transition: all 0.2s cubic-bezier(0.2, 0, 0, 1);
         }
-
+        
         .chip:hover {
-            background-color: rgba(255,255,255,0.05);
-            border-color: #79747E;
+            background-color: var(--md-sys-color-surface-container-high);
         }
 
-        /* Stato Attivo (Selected) */
         .chip.active {
-            background-color: #333333; /* Grigio medio */
-            color: #ffffff;
-            border-color: #ffffff;
-            /* Nessun colore "accento", solo scala di grigi */
+            background-color: var(--md-sys-color-primary);
+            color: var(--md-sys-color-on-primary);
+            border-color: var(--md-sys-color-primary);
+            box-shadow: 0 2px 6px rgba(0,0,0,0.2);
         }
 
-        /* --- GRIGLIA & CARDS --- */
-        main { padding: 0 1.5rem 6rem; max-width: 1200px; margin: 0 auto; }
+        /* --- GRIGLIA CARD --- */
+        main {
+            padding: 0 1.5rem 4rem; max-width: 1200px; margin: 0 auto;
+        }
 
         .grid {
             display: grid;
-            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
+            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
             gap: 1.5rem;
         }
 
         .card {
-            background-color: var(--md-sys-color-surface);
-            border-radius: var(--md-shape-corner);
+            background-color: var(--card-bg, var(--md-sys-color-surface-container));
+            color: var(--card-fg, var(--md-sys-color-on-background));
+            border-radius: var(--md-shape-corner-large);
             padding: 1.5rem;
-            text-decoration: none; color: inherit;
+            text-decoration: none;
             display: flex; flex-direction: column;
+            transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s;
+            position: relative; overflow: hidden;
             border: 1px solid transparent;
-            transition: transform 0.2s, background-color 0.2s;
-            position: relative;
         }
 
-        .card:active { transform: scale(0.98); background-color: #1f1f1f; }
-        
-        /* Hover su Desktop */
+        /* Effetto Hover "Lift" */
         @media (hover: hover) {
             .card:hover {
-                background-color: #1a1a1a;
-                border-color: #333;
-                transform: translateY(-4px);
+                transform: translateY(-6px) scale(1.01);
+                box-shadow: 0 12px 24px -8px rgba(0,0,0,0.3);
+                z-index: 10;
             }
         }
 
-        /* Layout Card: Emoji a sinistra, testo a destra (o sopra/sotto) */
+        /* Testata Card */
         .card-header {
             display: flex; justify-content: space-between; align-items: flex-start;
             margin-bottom: 1rem;
         }
 
         .card-emoji {
-            font-size: 3rem; /* Emoji Grande come accento */
-            line-height: 1;
-            filter: drop-shadow(0 0 15px rgba(255,255,255,0.1)); /* Leggero glow */
+            font-size: 3rem; 
+            filter: drop-shadow(0 4px 6px rgba(0,0,0,0.1));
+            transition: transform 0.3s;
         }
-        
+        .card:hover .card-emoji { transform: scale(1.1) rotate(5deg); }
+
         .card-date {
-            font-size: 0.75rem;
-            color: #666;
+            font-size: 0.75rem; font-weight: 600; opacity: 0.6;
+            background: rgba(0,0,0,0.1); padding: 4px 8px; border-radius: 6px;
             font-family: monospace;
-            border: 1px solid #333;
-            padding: 2px 6px;
-            border-radius: 4px;
+            [data-theme="dark"] & { background: rgba(255,255,255,0.1); }
         }
 
         .card h3 {
-            font-size: 1.25rem; font-weight: 500; color: #fff;
-            margin: 0 0 0.5rem 0; line-height: 1.3;
+            font-size: 1.4rem; font-weight: 700; margin: 0 0 0.5rem 0;
+            line-height: 1.2; letter-spacing: -0.01em;
         }
 
         .card p {
-            color: #aaa; font-size: 0.9rem; line-height: 1.5;
-            margin: 0 0 1.5rem 0; flex-grow: 1; font-weight: 300;
+            font-size: 0.95rem; opacity: 0.8; margin: 0 0 1.5rem 0;
+            line-height: 1.5; flex-grow: 1; font-weight: 400;
         }
 
-        .card-footer {
-            display: flex; gap: 0.5rem; flex-wrap: wrap; margin-top: auto;
+        /* Tags Footer */
+        .card-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: auto; }
+        
+        .tag-pill {
+            font-size: 0.7rem; font-weight: 600;
+            padding: 4px 10px; border-radius: 50px;
+            background-color: rgba(255,255,255,0.2); /* Semicorpo per adattarsi al colore card */
+            color: inherit;
+            border: 1px solid rgba(255,255,255,0.1);
         }
 
-        .tag-badge {
-            font-size: 0.7rem;
-            background: #252525;
-            color: #ccc;
-            padding: 4px 8px;
-            border-radius: 4px;
-            font-weight: 500;
-            letter-spacing: 0.5px;
+        /* --- STATI VUOTI E UTILITY --- */
+        #empty-state {
+            grid-column: 1 / -1; text-align: center; padding: 4rem 1rem;
+            color: var(--md-sys-color-outline);
         }
 
-        /* --- STATO VUOTO --- */
-        #empty-state {
-            grid-column: 1 / -1;
-            text-align: center; padding: 4rem;
-            color: #444; font-size: 1.5rem;
-            display: none;
+        #reset-filter {
+            display: none; margin: 0 0 1rem auto;
+            background: none; border: 1px solid var(--md-sys-color-outline);
+            color: var(--md-sys-color-on-background); padding: 6px 12px;
+            border-radius: 8px; cursor: pointer; font-size: 0.8rem;
         }
 
-        /* MOBILE ADJUSTMENTS */
+        /* Mobile Fixes */
         @media (max-width: 600px) {
-            h1 { font-size: 2rem; }
-            .grid { grid-template-columns: 1fr; } /* 1 colonna secca su mobile */
+            h1 { font-size: 1.5rem; }
+            .grid { grid-template-columns: 1fr; }
+            .card { padding: 1.25rem; border-radius: 20px; }
         }
     </style>
 </head>
 <body>
 
     <header>
-        <h1>FedericoDB Lab</h1>
-        <p>Risorse Didattiche Open Source. Semplice, essenziale, accessibile.</p>
+        <div class="top-bar">
+            <div>
+                <h1>Laboratorio Matematico<br>Orfini Foligno</h1>
+                <p class="subtitle">Didattica Interattiva Open Source</p>
+            </div>
+            <button class="theme-btn" onclick="toggleTheme()" aria-label="Cambia tema">
+                <span id="theme-icon">🌙</span>
+            </button>
+        </div>
+        
+        <div id="cloud-wrapper">
+            <canvas id="word-cloud"></canvas>
+        </div>
     </header>
 
     <div class="filters-container">
         <div class="filters" id="filterBar">
-            <button class="chip active" data-filter="all">Tutti</button>
-            <button class="chip" data-filter="Matematica">Matematica</button>
-            <button class="chip" data-filter="Fisica">Fisica</button>
-            <button class="chip" data-filter="Lab">Lab</button>
-            <button class="chip" data-filter="Gioco">Games</button>
-            <button class="chip" data-filter="1EL">1EL</button>
-            <button class="chip" data-filter="4EL">4EL</button>
+            <button class="chip active" onclick="applyFilter('all')">Tutti</button>
+            <!-- I bottoni verranno generati qui SOLO per i tag esistenti -->
         </div>
     </div>
 
     <main>
+        <div style="display: flex; justify-content: flex-end;">
+            <button id="reset-filter" onclick="applyFilter('all')">✕ Rimuovi Filtri</button>
+        </div>
+
         <div class="grid" id="grid">
-            <!-- Card iniettate via JS -->
+            <!-- Le card verranno inserite qui -->
         </div>
+        
         <div id="empty-state">
-            ( ˘_˘ )<br><br>Nessuna attività trovata.
+            <div style="font-size:3rem; margin-bottom:1rem; opacity:0.5;">🔭</div>
+            <p>Nessuna attività trovata per questo filtro.</p>
         </div>
     </main>
 
-    <!-- DATABASE -->
+    <!-- CARICAMENTO DATABASE -->
     <script src="database.js"></script>
 
     <script>
-        const grid = document.getElementById('grid');
-        const emptyState = document.getElementById('empty-state');
-        const chips = document.querySelectorAll('.chip');
+        // --- 1. GESTIONE TEMA ---
+        const html = document.documentElement;
+        const themeIcon = document.getElementById('theme-icon');
         
-        // Fallback Database
+        const savedTheme = localStorage.getItem('theme') || 'dark';
+        setTheme(savedTheme);
+
+        function toggleTheme() {
+            const current = html.getAttribute('data-theme');
+            setTheme(current === 'dark' ? 'light' : 'dark');
+        }
+
+        function setTheme(t) {
+            html.setAttribute('data-theme', t);
+            localStorage.setItem('theme', t);
+            themeIcon.innerText = t === 'dark' ? '☀️' : '🌙';
+            // Ridisegna la cloud per aggiornare i colori
+            if(window.initWordCloud) setTimeout(window.initWordCloud, 100);
+        }
+
+        // --- 2. CONFIGURAZIONE EMOJI E COLORI ---
         const safeData = (typeof db !== 'undefined') ? db : [];
 
-        function render(filter) {
+        // Mappa Emoji Specifica per File/Contenuto
+        const specificEmojiMap = {
+            'voto': '⚖️',      // Teoria Giochi / Equilibrio
+            'nash': '🧠',
+            'memory': '🧩',    // Memory generico
+            'polinomi': '🧱',  // Algebra tiles
+            'underground': '🚇', // Mappa metro
+            'mercatore': '🌍', // Mappe geo
+            'corrente': '⚡',   // Elettrotecnica
+            'goniomatch': '📡', // Radar
+            'tavola': '🔢',    // Pitagora/Tartaglia
+            'insiemi': '🔴',   // Venn
+            'frazioni': '🍰',  // Torte
+            'sketch': '🎨'     // Arte
+        };
+
+        // Palette Colori (CSS Variables definite sopra)
+        const themeColors = [
+            { bg: 'var(--card-blue)', fg: 'var(--card-on-blue)' },
+            { bg: 'var(--card-green)', fg: 'var(--card-on-green)' },
+            { bg: 'var(--card-purple)', fg: 'var(--card-on-purple)' },
+            { bg: 'var(--card-red)', fg: 'var(--card-on-red)' },
+            { bg: 'var(--card-teal)', fg: 'var(--card-on-teal)' },
+            { bg: 'var(--card-yellow)', fg: 'var(--card-on-yellow)' }
+        ];
+
+        // Funzione Hash per colore deterministico (stesso titolo = stesso colore sempre)
+        function getStableColor(str) {
+            let hash = 0;
+            for (let i = 0; i < str.length; i++) {
+                hash = str.charCodeAt(i) + ((hash << 5) - hash);
+            }
+            const index = Math.abs(hash) % themeColors.length;
+            return themeColors[index];
+        }
+
+        function getSmartEmoji(item) {
+            // 1. Cerca nel nome file o titolo una parola chiave specifica
+            const keyString = (item.url + item.title).toLowerCase();
+            for (const [key, emo] of Object.entries(specificEmojiMap)) {
+                if (keyString.includes(key)) return emo;
+            }
+            // 2. Fallback sui Tag
+            if (item.tags.some(t => t.includes('Fisica'))) return '⚛️';
+            if (item.tags.some(t => t.includes('Matematica'))) return '📐';
+            
+            return '📄'; // Default assoluto
+        }
+
+        // --- 3. GENERAZIONE FILTRI CONTESTUALI ---
+        const filterBar = document.getElementById('filterBar');
+
+        function initFilters() {
+            // Raccogli solo i tag veramente usati
+            const tagCounts = {};
+            safeData.forEach(item => {
+                item.tags.forEach(t => { tagCounts[t] = (tagCounts[t] || 0) + 1; });
+            });
+
+            // Ordina per frequenza (i più popolari prima)
+            const sortedTags = Object.keys(tagCounts).sort((a,b) => tagCounts[b] - tagCounts[a]);
+
+            sortedTags.forEach(tag => {
+                const btn = document.createElement('button');
+                btn.className = 'chip';
+                // Mostra il numero solo se > 1 per pulizia
+                const label = tagCounts[tag] > 1 ? `${tag} (${tagCounts[tag]})` : tag;
+                btn.innerText = label;
+                btn.onclick = () => applyFilter(tag);
+                filterBar.appendChild(btn);
+            });
+        }
+
+        // --- 4. RENDER GRID ---
+        const grid = document.getElementById('grid');
+        const emptyState = document.getElementById('empty-state');
+        const resetBtn = document.getElementById('reset-filter');
+
+        function renderCards(filter) {
             grid.innerHTML = '';
             let count = 0;
 
+            // UI Filtri
+            document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
+            const activeBtn = Array.from(document.querySelectorAll('.chip')).find(c => c.innerText.startsWith(filter === 'all' ? 'Tutti' : filter));
+            if(activeBtn) activeBtn.classList.add('active');
+            
+            resetBtn.style.display = filter === 'all' ? 'none' : 'block';
+
             safeData.forEach(item => {
-                const match = filter === 'all' || item.tags.some(t => t.includes(filter));
+                const match = filter === 'all' || item.tags.includes(filter);
                 
                 if (match) {
                     count++;
@@ -252,16 +427,19 @@
                     card.href = item.url;
                     card.className = 'card';
                     
-                    const tagsHtml = item.tags.map(t => `<span class="tag-badge">#${t}</span>`).join('');
-                    const dateObj = new Date(item.date);
-                    // Formato data breve tecnico: DD.MM.YY
-                    const dateStr = !isNaN(dateObj) ? 
-                        `${String(dateObj.getDate()).padStart(2,'0')}.${String(dateObj.getMonth()+1).padStart(2,'0')}.${String(dateObj.getFullYear()).slice(-2)}` : 
-                        item.date;
+                    // Applica Colore Deterministico
+                    const colors = getStableColor(item.title);
+                    card.style.setProperty('--card-bg', colors.bg);
+                    card.style.setProperty('--card-fg', colors.fg);
 
-                    // NOTA: item.icon viene generato automaticamente da build.py in base ai tag
-                    // Se build.py non ha assegnato un'icona, usiamo un fallback generico
-                    const emoji = item.icon || '📄';
+                    const emoji = getSmartEmoji(item);
+                    
+                    // Formattazione data
+                    const d = new Date(item.date);
+                    const dateStr = !isNaN(d) ? `${d.getDate()}/${d.getMonth()+1}/${d.getFullYear()}` : item.date;
+                    
+                    // Tag HTML (limitati a 3 per spazio)
+                    const tagsHtml = item.tags.slice(0, 3).map(t => `<span class="tag-pill">#${t}</span>`).join('');
 
                     card.innerHTML = `
                         <div class="card-header">
@@ -270,7 +448,7 @@
                         </div>
                         <h3>${item.title}</h3>
                         <p>${item.excerpt}</p>
-                        <div class="card-footer">${tagsHtml}</div>
+                        <div class="card-tags">${tagsHtml}</div>
                     `;
                     grid.appendChild(card);
                 }
@@ -279,22 +457,67 @@
             emptyState.style.display = count === 0 ? 'block' : 'none';
         }
 
-        // Logic Chips
-        chips.forEach(btn => {
-            btn.addEventListener('click', () => {
-                // Rimuovi active da tutti
-                document.querySelectorAll('.chip.active').forEach(c => c.classList.remove('active'));
-                // Aggiungi active al cliccato
-                btn.classList.add('active');
-                // Scroll automatico per centrare il chip cliccato (UX Mobile)
-                btn.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
+        window.applyFilter = function(tag) {
+            renderCards(tag);
+        }
+
+        // --- 5. WORD CLOUD ---
+        window.initWordCloud = function() {
+            if (!safeData.length) return;
+            const tagCounts = {};
+            safeData.forEach(item => item.tags.forEach(t => tagCounts[t] = (tagCounts[t] || 0) + 1));
+            
+            const list = Object.keys(tagCounts).map(k => [k, 20 + tagCounts[k] * 10]); // Peso base 20
+            const canvas = document.getElementById('word-cloud');
+            const wrapper = document.getElementById('cloud-wrapper');
+            
+            canvas.width = wrapper.offsetWidth;
+            canvas.height = wrapper.offsetHeight;
+
+            // Colore testo adattivo al tema
+            const isDark = html.getAttribute('data-theme') === 'dark';
+            
+            WordCloud(canvas, {
+                list: list,
+                gridSize: 15,
+                weightFactor: 1.3,
+                fontFamily: 'Inter, sans-serif',
+                color: (word, weight) => {
+                    // Colori più o meno saturi in base al tema
+                    const hue = Math.floor(Math.random() * 360);
+                    const sat = isDark ? '80%' : '60%';
+                    const light = isDark ? '70%' : '40%';
+                    return `hsl(${hue}, ${sat}, ${light})`;
+                },
+                backgroundColor: 'transparent',
+                rotateRatio: 0, // Solo orizzontale per leggibilità
+                shrinkToFit: true,
+                drawOutOfBound: false,
                 
-                render(btn.dataset.filter);
+                click: (item) => {
+                    if(item) {
+                        applyFilter(item[0]);
+                        window.scrollTo({ top: filterBar.offsetTop - 80, behavior: 'smooth' });
+                    }
+                },
+                hover: (item) => {
+                    wrapper.style.cursor = item ? 'pointer' : 'default';
+                }
             });
-        });
-
-        // Avvio
-        render('all');
+        };
+
+        // --- BOOTSTRAP ---
+        window.onload = function() {
+            initFilters();
+            renderCards('all');
+            // Ritardo per assicurare che il font sia caricato per la wordcloud
+            setTimeout(initWordCloud, 100);
+        };
+
+        window.onresize = () => {
+            clearTimeout(window.resizeTimer);
+            window.resizeTimer = setTimeout(window.initWordCloud, 200);
+        };
     </script>
 </body>
-</html>
+</html>
\ No newline at end of file

commit c46ce29d7156b959ae66e0552b744d2e2b164c54
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sun Dec 7 19:23:55 2025 +0100

    Aggiunta nuova lezione e fix mobile

diff --git a/index.html b/index.html
index 6e51286..cd3205a 100644
--- a/index.html
+++ b/index.html
@@ -2,128 +2,248 @@
 <html lang="it">
 <head>
     <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
-    <title>ORFINI MATH LAB</title>
-    <!-- PicoCSS minimalista -->
-    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
+    <title>FedericoDB Lab</title>
+    
+    <!-- Google Font: Roboto (Lo standard del Material Design) -->
+    <link rel="preconnect" href="https://fonts.googleapis.com">
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
     
     <style>
+        /* --- MATERIAL DESIGN 3 (Dark AMOLED Theme) --- */
         :root {
-            --primary: #4f46e5;
-            --bg-body: #0f172a;
-            --bg-card: #1e293b;
-            --text-main: #f8fafc;
-            --text-muted: #94a3b8;
+            --md-sys-color-background: #000000;       /* Nero AMOLED */
+            --md-sys-color-surface: #141414;          /* Grigio scurissimo per le card */
+            --md-sys-color-surface-variant: #282828;  /* Grigio per hover/bordi */
+            
+            --md-sys-color-on-background: #E6E1E5;    /* Testo principale (bianco sporco) */
+            --md-sys-color-on-surface-variant: #CAC4D0; /* Testo secondario */
+            --md-sys-color-outline: #49454F;          /* Bordi sottili */
+            
+            --md-elevation-1: 0px 1px 2px rgba(0,0,0,0.3);
+            --md-shape-corner: 16px;                  /* Raggio stondatura schede */
+            --md-shape-pill: 100px;                   /* Raggio filtri */
         }
 
+        /* RESET & BASE */
+        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
+        
         body {
-            background-color: var(--bg-body);
-            color: var(--text-main);
-            padding-bottom: 3rem;
-            font-family: system-ui, -apple-system, sans-serif;
+            margin: 0; padding: 0;
+            background-color: var(--md-sys-color-background);
+            color: var(--md-sys-color-on-background);
+            font-family: 'Roboto', sans-serif;
+            min-height: 100dvh;
+            /* Typography */
+            letter-spacing: 0.01em;
+            line-height: 1.5;
+        }
+
+        /* --- HEADER --- */
+        header {
+            padding: 3rem 1.5rem 1rem;
+            max-width: 1200px;
+            margin: 0 auto;
+        }
+
+        h1 {
+            font-size: 2.5rem;
+            font-weight: 400; /* Material Design usa pesi leggeri per titoli grandi */
+            margin: 0 0 0.5rem 0;
+            color: #ffffff;
+            letter-spacing: -0.02em;
+        }
+        
+        header p {
+            font-size: 1rem;
+            color: var(--md-sys-color-on-surface-variant);
+            font-weight: 300;
+            margin: 0;
+            max-width: 600px;
         }
 
-        /* Header */
-        header { padding: 3rem 1rem 2rem; text-align: center; }
-        header h1 { margin-bottom: 0.5rem; color: #38bdf8; text-transform: uppercase; letter-spacing: 2px; font-weight: 800; }
-        header p { color: var(--text-muted); font-size: 0.9rem; }
+        /* --- FILTRI (CHIPS) --- */
+        .filters-container {
+            position: sticky; top: 0; z-index: 100;
+            background: rgba(0,0,0,0.85); /* Sfondo semi-trasparente */
+            backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
+            border-bottom: 1px solid #1f1f1f;
+            padding: 1rem 0;
+            margin-bottom: 2rem;
+        }
 
-        /* Filtri */
         .filters {
-            display: flex; gap: 0.5rem; overflow-x: auto; padding: 0.5rem 1rem 1.5rem; 
-            justify-content: center; -webkit-overflow-scrolling: touch;
+            display: flex; gap: 0.75rem; overflow-x: auto; 
+            padding: 0 1.5rem; max-width: 1200px; margin: 0 auto;
+            -webkit-overflow-scrolling: touch; 
+            scrollbar-width: none;
+        }
+        .filters::-webkit-scrollbar { display: none; }
+
+        /* Stile "Filter Chip" Material Design */
+        .chip {
+            background: transparent;
+            border: 1px solid var(--md-sys-color-outline);
+            color: var(--md-sys-color-on-surface-variant);
+            padding: 0.5rem 1.25rem;
+            border-radius: 8px; /* Look leggermente più tech/squadrato rispetto al pill */
+            font-size: 0.875rem;
+            font-weight: 500;
+            white-space: nowrap;
+            cursor: pointer;
+            transition: all 0.2s ease;
+            user-select: none;
+        }
+
+        .chip:hover {
+            background-color: rgba(255,255,255,0.05);
+            border-color: #79747E;
         }
-        .filter-btn {
-            background: #334155; border: 1px solid #475569; color: #cbd5e1;
-            padding: 0.4rem 1.2rem; border-radius: 50px; font-size: 0.85rem; cursor: pointer; white-space: nowrap;
-            transition: all 0.2s ease; font-weight: 600;
+
+        /* Stato Attivo (Selected) */
+        .chip.active {
+            background-color: #333333; /* Grigio medio */
+            color: #ffffff;
+            border-color: #ffffff;
+            /* Nessun colore "accento", solo scala di grigi */
         }
-        .filter-btn:hover { background: #475569; }
-        .filter-btn.active { background: var(--primary); color: white; border-color: var(--primary); box-shadow: 0 0 15px rgba(79, 70, 229, 0.4); }
 
-        /* Griglia */
-        .grid-container {
+        /* --- GRIGLIA & CARDS --- */
+        main { padding: 0 1.5rem 6rem; max-width: 1200px; margin: 0 auto; }
+
+        .grid {
             display: grid;
-            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
+            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
             gap: 1.5rem;
-            padding: 0 1rem;
-            max-width: 1400px;
-            margin: 0 auto;
         }
 
-        /* Card */
         .card {
-            background: var(--bg-card);
-            border: 1px solid #334155;
-            border-radius: 16px;
+            background-color: var(--md-sys-color-surface);
+            border-radius: var(--md-shape-corner);
             padding: 1.5rem;
-            transition: transform 0.2s, box-shadow 0.2s, border-color 0.2s;
-            cursor: pointer;
-            position: relative;
-            display: flex; flex-direction: column;
             text-decoration: none; color: inherit;
-            height: 100%;
+            display: flex; flex-direction: column;
+            border: 1px solid transparent;
+            transition: transform 0.2s, background-color 0.2s;
+            position: relative;
         }
-        .card:hover { transform: translateY(-4px); box-shadow: 0 10px 30px rgba(0,0,0,0.3); border-color: #38bdf8; }
+
+        .card:active { transform: scale(0.98); background-color: #1f1f1f; }
         
-        .card-icon { position: absolute; top: 1.2rem; right: 1.2rem; font-size: 1.8rem; opacity: 0.8; }
-        .card-date { font-size: 0.75rem; color: var(--text-muted); margin-bottom: 0.5rem; font-family: monospace; }
-        .card h3 { font-size: 1.3rem; margin-bottom: 0.5rem; color: white; font-weight: 700; padding-right: 2rem; }
-        .card p { font-size: 0.9rem; color: #cbd5e1; margin-bottom: 1.5rem; flex-grow: 1; line-height: 1.5; }
+        /* Hover su Desktop */
+        @media (hover: hover) {
+            .card:hover {
+                background-color: #1a1a1a;
+                border-color: #333;
+                transform: translateY(-4px);
+            }
+        }
+
+        /* Layout Card: Emoji a sinistra, testo a destra (o sopra/sotto) */
+        .card-header {
+            display: flex; justify-content: space-between; align-items: flex-start;
+            margin-bottom: 1rem;
+        }
+
+        .card-emoji {
+            font-size: 3rem; /* Emoji Grande come accento */
+            line-height: 1;
+            filter: drop-shadow(0 0 15px rgba(255,255,255,0.1)); /* Leggero glow */
+        }
         
-        .card-tags { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: auto; }
-        .tag { font-size: 0.7rem; padding: 3px 8px; border-radius: 6px; background: #334155; color: #94a3b8; font-weight: 500; }
+        .card-date {
+            font-size: 0.75rem;
+            color: #666;
+            font-family: monospace;
+            border: 1px solid #333;
+            padding: 2px 6px;
+            border-radius: 4px;
+        }
+
+        .card h3 {
+            font-size: 1.25rem; font-weight: 500; color: #fff;
+            margin: 0 0 0.5rem 0; line-height: 1.3;
+        }
+
+        .card p {
+            color: #aaa; font-size: 0.9rem; line-height: 1.5;
+            margin: 0 0 1.5rem 0; flex-grow: 1; font-weight: 300;
+        }
+
+        .card-footer {
+            display: flex; gap: 0.5rem; flex-wrap: wrap; margin-top: auto;
+        }
 
-        /* Stato vuoto */
-        #empty-state { text-align: center; padding: 4rem; color: var(--text-muted); grid-column: 1 / -1; display: none; }
+        .tag-badge {
+            font-size: 0.7rem;
+            background: #252525;
+            color: #ccc;
+            padding: 4px 8px;
+            border-radius: 4px;
+            font-weight: 500;
+            letter-spacing: 0.5px;
+        }
+
+        /* --- STATO VUOTO --- */
+        #empty-state {
+            grid-column: 1 / -1;
+            text-align: center; padding: 4rem;
+            color: #444; font-size: 1.5rem;
+            display: none;
+        }
+
+        /* MOBILE ADJUSTMENTS */
+        @media (max-width: 600px) {
+            h1 { font-size: 2rem; }
+            .grid { grid-template-columns: 1fr; } /* 1 colonna secca su mobile */
+        }
     </style>
 </head>
 <body>
 
     <header>
-        <h1>ORFINI MATH LAB</h1>
-        <p>Risorse Didattiche Open Source</p>
+        <h1>FedericoDB Lab</h1>
+        <p>Risorse Didattiche Open Source. Semplice, essenziale, accessibile.</p>
     </header>
 
-    <div class="filters" id="filterBar">
-        <button class="filter-btn active" data-filter="all">Tutti</button>
-        <!-- I filtri verranno popolati dinamicamente o puoi lasciarli statici se preferisci -->
-        <button class="filter-btn" data-filter="Matematica">Matematica</button>
-        <button class="filter-btn" data-filter="Algebra">Fisica</button>
-        <button class="filter-btn" data-filter="Lab">Lab</button>
-        <button class="filter-btn" data-filter="Gioco">Gamification</button>
-        <button class="filter-btn" data-filter="1EL">1EL</button>
-        <button class="filter-btn" data-filter="2EL">2EL</button>
-        <button class="filter-btn" data-filter="2GR">2GR</button>
-        <button class="filter-btn" data-filter="3MEC">3MEC</button>
-        <button class="filter-btn" data-filter="4EL">4EL</button>
+    <div class="filters-container">
+        <div class="filters" id="filterBar">
+            <button class="chip active" data-filter="all">Tutti</button>
+            <button class="chip" data-filter="Matematica">Matematica</button>
+            <button class="chip" data-filter="Fisica">Fisica</button>
+            <button class="chip" data-filter="Lab">Lab</button>
+            <button class="chip" data-filter="Gioco">Games</button>
+            <button class="chip" data-filter="1EL">1EL</button>
+            <button class="chip" data-filter="4EL">4EL</button>
+        </div>
     </div>
 
     <main>
-        <div class="grid-container" id="grid">
-            <!-- LE CARD VERRANNO INSERITE QUI DA JS -->
+        <div class="grid" id="grid">
+            <!-- Card iniettate via JS -->
+        </div>
+        <div id="empty-state">
+            ( ˘_˘ )<br><br>Nessuna attività trovata.
         </div>
-        <div id="empty-state">Nessuna lezione trovata con questo filtro.</div>
     </main>
 
-    <!-- 1. Carichiamo il database generato da Python -->
+    <!-- DATABASE -->
     <script src="database.js"></script>
 
-    <!-- 2. Logica di Rendering -->
     <script>
         const grid = document.getElementById('grid');
         const emptyState = document.getElementById('empty-state');
-        const buttons = document.querySelectorAll('.filter-btn');
-
-        // Se database.js non esiste o è vuoto (primo avvio senza build), usiamo un array vuoto per non rompere il sito
-        const data = (typeof db !== 'undefined') ? db : [];
+        const chips = document.querySelectorAll('.chip');
+        
+        // Fallback Database
+        const safeData = (typeof db !== 'undefined') ? db : [];
 
         function render(filter) {
             grid.innerHTML = '';
             let count = 0;
 
-            data.forEach(item => {
-                // Logica di filtro inclusiva: se filtro="all" o se uno dei tag dell'item contiene la stringa del filtro
+            safeData.forEach(item => {
                 const match = filter === 'all' || item.tags.some(t => t.includes(filter));
                 
                 if (match) {
@@ -132,18 +252,25 @@
                     card.href = item.url;
                     card.className = 'card';
                     
-                    const tagsHtml = item.tags.map(t => `<span class="tag">#${t}</span>`).join('');
-                    
-                    // Formatta la data (opzionale)
+                    const tagsHtml = item.tags.map(t => `<span class="tag-badge">#${t}</span>`).join('');
                     const dateObj = new Date(item.date);
-                    const dateStr = !isNaN(dateObj) ? dateObj.toLocaleDateString('it-IT') : item.date;
+                    // Formato data breve tecnico: DD.MM.YY
+                    const dateStr = !isNaN(dateObj) ? 
+                        `${String(dateObj.getDate()).padStart(2,'0')}.${String(dateObj.getMonth()+1).padStart(2,'0')}.${String(dateObj.getFullYear()).slice(-2)}` : 
+                        item.date;
+
+                    // NOTA: item.icon viene generato automaticamente da build.py in base ai tag
+                    // Se build.py non ha assegnato un'icona, usiamo un fallback generico
+                    const emoji = item.icon || '📄';
 
                     card.innerHTML = `
-                        <div class="card-icon">${item.icon}</div>
-                        <div class="card-date">${dateStr}</div>
+                        <div class="card-header">
+                            <div class="card-emoji">${emoji}</div>
+                            <div class="card-date">${dateStr}</div>
+                        </div>
                         <h3>${item.title}</h3>
                         <p>${item.excerpt}</p>
-                        <div class="card-tags">${tagsHtml}</div>
+                        <div class="card-footer">${tagsHtml}</div>
                     `;
                     grid.appendChild(card);
                 }
@@ -152,11 +279,16 @@
             emptyState.style.display = count === 0 ? 'block' : 'none';
         }
 
-        // Setup Filtri
-        buttons.forEach(btn => {
+        // Logic Chips
+        chips.forEach(btn => {
             btn.addEventListener('click', () => {
-                document.querySelector('.filter-btn.active').classList.remove('active');
+                // Rimuovi active da tutti
+                document.querySelectorAll('.chip.active').forEach(c => c.classList.remove('active'));
+                // Aggiungi active al cliccato
                 btn.classList.add('active');
+                // Scroll automatico per centrare il chip cliccato (UX Mobile)
+                btn.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
+                
                 render(btn.dataset.filter);
             });
         });

commit 99837e5c0777a53096911d0e6816ab4a48316b78
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sun Dec 7 19:16:23 2025 +0100

    trasferito su antigravity, prima prova nero su tutte le pagine

diff --git a/index.html b/index.html
index 0510d78..6e51286 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
-    <title>FedericoDB Lab</title>
+    <title>ORFINI MATH LAB</title>
     <!-- PicoCSS minimalista -->
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
     
@@ -81,7 +81,7 @@
 <body>
 
     <header>
-        <h1>FedericoDB Lab</h1>
+        <h1>ORFINI MATH LAB</h1>
         <p>Risorse Didattiche Open Source</p>
     </header>
 

commit 4eb6baaa48fefd53e9148bba311c3b644d61b7e4
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sun Dec 7 11:54:35 2025 +0100

    Aggiunta nuova lezione e fix mobile

diff --git a/index.html b/index.html
index 9764863..0510d78 100644
--- a/index.html
+++ b/index.html
@@ -89,11 +89,14 @@
         <button class="filter-btn active" data-filter="all">Tutti</button>
         <!-- I filtri verranno popolati dinamicamente o puoi lasciarli statici se preferisci -->
         <button class="filter-btn" data-filter="Matematica">Matematica</button>
-        <button class="filter-btn" data-filter="Fisica">Fisica</button>
+        <button class="filter-btn" data-filter="Algebra">Fisica</button>
         <button class="filter-btn" data-filter="Lab">Lab</button>
         <button class="filter-btn" data-filter="Gioco">Gamification</button>
-        <button class="filter-btn" data-filter="1EL">Classe 1ª</button>
-        <button class="filter-btn" data-filter="4EL">Classe 4ª</button>
+        <button class="filter-btn" data-filter="1EL">1EL</button>
+        <button class="filter-btn" data-filter="2EL">2EL</button>
+        <button class="filter-btn" data-filter="2GR">2GR</button>
+        <button class="filter-btn" data-filter="3MEC">3MEC</button>
+        <button class="filter-btn" data-filter="4EL">4EL</button>
     </div>
 
     <main>

commit 6f2d8909d56bf5ad4abd9709feb65a18276077f3
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sun Dec 7 10:37:38 2025 +0100

    Aggiunta nuova lezione e fix mobile

diff --git a/index.html b/index.html
index 002c480..9764863 100644
--- a/index.html
+++ b/index.html
@@ -4,165 +4,162 @@
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
     <title>FedericoDB Lab</title>
+    <!-- PicoCSS minimalista -->
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
     
     <style>
         :root {
-            --primary: #4f46e5; /* Indaco professionale */
+            --primary: #4f46e5;
+            --bg-body: #0f172a;
             --bg-card: #1e293b;
             --text-main: #f8fafc;
+            --text-muted: #94a3b8;
         }
 
         body {
-            background-color: #0f172a; /* Slate 900 (Scuro elegante) */
+            background-color: var(--bg-body);
             color: var(--text-main);
             padding-bottom: 3rem;
+            font-family: system-ui, -apple-system, sans-serif;
         }
 
-        /* Intestazione */
-        header { padding: 2rem 0; text-align: center; }
-        header h1 { margin-bottom: 0.5rem; color: #38bdf8; text-transform: uppercase; letter-spacing: 2px; }
-        
-        /* Filtri (bottoni stile chip) */
+        /* Header */
+        header { padding: 3rem 1rem 2rem; text-align: center; }
+        header h1 { margin-bottom: 0.5rem; color: #38bdf8; text-transform: uppercase; letter-spacing: 2px; font-weight: 800; }
+        header p { color: var(--text-muted); font-size: 0.9rem; }
+
+        /* Filtri */
         .filters {
-            display: flex; gap: 0.5rem; overflow-x: auto; padding-bottom: 1rem; margin-bottom: 2rem;
+            display: flex; gap: 0.5rem; overflow-x: auto; padding: 0.5rem 1rem 1.5rem; 
             justify-content: center; -webkit-overflow-scrolling: touch;
         }
         .filter-btn {
             background: #334155; border: 1px solid #475569; color: #cbd5e1;
-            padding: 0.4rem 1rem; border-radius: 50px; font-size: 0.8rem; cursor: pointer; white-space: nowrap;
-            transition: all 0.2s;
+            padding: 0.4rem 1.2rem; border-radius: 50px; font-size: 0.85rem; cursor: pointer; white-space: nowrap;
+            transition: all 0.2s ease; font-weight: 600;
         }
-        .filter-btn.active { background: var(--primary); color: white; border-color: var(--primary); transform: scale(1.05); }
+        .filter-btn:hover { background: #475569; }
+        .filter-btn.active { background: var(--primary); color: white; border-color: var(--primary); box-shadow: 0 0 15px rgba(79, 70, 229, 0.4); }
 
-        /* Griglia Responsive */
+        /* Griglia */
         .grid-container {
             display: grid;
-            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
+            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
             gap: 1.5rem;
+            padding: 0 1rem;
+            max-width: 1400px;
+            margin: 0 auto;
         }
 
-        /* Card della Lezione */
+        /* Card */
         .card {
             background: var(--bg-card);
             border: 1px solid #334155;
             border-radius: 16px;
             padding: 1.5rem;
-            transition: transform 0.2s, box-shadow 0.2s;
+            transition: transform 0.2s, box-shadow 0.2s, border-color 0.2s;
             cursor: pointer;
             position: relative;
-            overflow: hidden;
             display: flex; flex-direction: column;
-            text-decoration: none; /* Rimuove sottolineatura link */
-            color: inherit;
+            text-decoration: none; color: inherit;
+            height: 100%;
         }
-        .card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(0,0,0,0.5); border-color: #38bdf8; }
-
-        /* Elementi della Card */
-        .card-meta { display: flex; justify-content: space-between; font-size: 0.7rem; color: #94a3b8; margin-bottom: 0.5rem; uppercase; letter-spacing: 1px; }
-        .card h3 { font-size: 1.25rem; margin-bottom: 0.5rem; color: white; }
-        .card p { font-size: 0.9rem; color: #cbd5e1; flex-grow: 1; margin-bottom: 1rem; }
+        .card:hover { transform: translateY(-4px); box-shadow: 0 10px 30px rgba(0,0,0,0.3); border-color: #38bdf8; }
         
-        /* Tags visivi */
-        .card-tags { display: flex; flex-wrap: wrap; gap: 0.3rem; }
-        .tag { font-size: 0.7rem; padding: 2px 8px; border-radius: 4px; background: #334155; color: #94a3b8; }
+        .card-icon { position: absolute; top: 1.2rem; right: 1.2rem; font-size: 1.8rem; opacity: 0.8; }
+        .card-date { font-size: 0.75rem; color: var(--text-muted); margin-bottom: 0.5rem; font-family: monospace; }
+        .card h3 { font-size: 1.3rem; margin-bottom: 0.5rem; color: white; font-weight: 700; padding-right: 2rem; }
+        .card p { font-size: 0.9rem; color: #cbd5e1; margin-bottom: 1.5rem; flex-grow: 1; line-height: 1.5; }
         
-        /* Icona Tipo Lezione (Angolo in alto a destra) */
-        .card-icon { position: absolute; top: 1rem; right: 1rem; font-size: 1.5rem; opacity: 0.5; }
+        .card-tags { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: auto; }
+        .tag { font-size: 0.7rem; padding: 3px 8px; border-radius: 6px; background: #334155; color: #94a3b8; font-weight: 500; }
 
+        /* Stato vuoto */
+        #empty-state { text-align: center; padding: 4rem; color: var(--text-muted); grid-column: 1 / -1; display: none; }
     </style>
 </head>
 <body>
 
-    <main class="container">
-        <header>
-            <h1>Laboratorio Digitale</h1>
-            <p>Matematica • Fisica • Coding • Creatività</p>
-        </header>
-
-        <div class="filters" id="filterBar">
-            <button class="filter-btn active" data-filter="all">Tutti</button>
-            <button class="filter-btn" data-filter="1EL">1ª Elettronica</button>
-            <button class="filter-btn" data-filter="4EL">4ª Elettronica</button>
-            <button class="filter-btn" data-filter="Matematica">Matematica</button>
-            <button class="filter-btn" data-filter="Lab">Laboratorio</button>
-            <button class="filter-btn" data-filter="Giochi">Giochi</button>
+    <header>
+        <h1>FedericoDB Lab</h1>
+        <p>Risorse Didattiche Open Source</p>
+    </header>
+
+    <div class="filters" id="filterBar">
+        <button class="filter-btn active" data-filter="all">Tutti</button>
+        <!-- I filtri verranno popolati dinamicamente o puoi lasciarli statici se preferisci -->
+        <button class="filter-btn" data-filter="Matematica">Matematica</button>
+        <button class="filter-btn" data-filter="Fisica">Fisica</button>
+        <button class="filter-btn" data-filter="Lab">Lab</button>
+        <button class="filter-btn" data-filter="Gioco">Gamification</button>
+        <button class="filter-btn" data-filter="1EL">Classe 1ª</button>
+        <button class="filter-btn" data-filter="4EL">Classe 4ª</button>
+    </div>
+
+    <main>
+        <div class="grid-container" id="grid">
+            <!-- LE CARD VERRANNO INSERITE QUI DA JS -->
         </div>
-
-        <div class="grid-container">
-
-            <a href="lezioni/cards_polinomi_memory004.html" class="card" data-tags="Matematica Giochi 1EL">
-                <div class="card-icon">🧩</div> <div class="card-meta">
-                    <span>15 Nov 2025</span>
-                    <span style="color: #fbbf24;">★ New</span>
-                </div>
-                <h3>FactorMatch Memory</h3>
-                <p>Sfida a tempo sulle scomposizioni di polinomi. Trova le coppie tra polinomio e fattorizzazione.</p>
-                <div class="card-tags">
-                    <span class="tag">#Algebra</span>
-                    <span class="tag">#Gamification</span>
-                </div>
-            </a>
-
-            <a href="lezioni/math_underground003_4el.html" class="card" data-tags="Matematica 4EL Lab">
-                <div class="card-icon">🚇</div>
-                <div class="card-meta">
-                    <span>06 Dic 2025</span>
-                </div>
-                <h3>Math Underground</h3>
-                <p>Mappa interattiva stile metropolitana per visualizzare il dominio e la classificazione delle funzioni.</p>
-                <div class="card-tags">
-                    <span class="tag">#Analisi</span>
-                    <span class="tag">#Mappe</span>
-                </div>
-            </a>
-
-            <a href="lezioni/voto_consapevole_2.5.html" class="card" data-tags="Lab Civica 4EL">
-                <div class="card-icon">🎲</div>
-                <div class="card-meta">
-                    <span>01 Ott 2025</span>
-                </div>
-                <h3>Voto Consapevole</h3>
-                <p>Simulazione interattiva sull'Equilibrio di Nash applicato alla media dei voti scolastici.</p>
-                <div class="card-tags">
-                    <span class="tag">#EdCivica</span>
-                    <span class="tag">#Simulazione</span>
-                </div>
-            </a>
-
-            </div>
+        <div id="empty-state">Nessuna lezione trovata con questo filtro.</div>
     </main>
 
+    <!-- 1. Carichiamo il database generato da Python -->
+    <script src="database.js"></script>
+
+    <!-- 2. Logica di Rendering -->
     <script>
-        // Logica semplice per i filtri (Vanilla JS - 0 dipendenze)
+        const grid = document.getElementById('grid');
+        const emptyState = document.getElementById('empty-state');
         const buttons = document.querySelectorAll('.filter-btn');
-        const cards = document.querySelectorAll('.card');
 
+        // Se database.js non esiste o è vuoto (primo avvio senza build), usiamo un array vuoto per non rompere il sito
+        const data = (typeof db !== 'undefined') ? db : [];
+
+        function render(filter) {
+            grid.innerHTML = '';
+            let count = 0;
+
+            data.forEach(item => {
+                // Logica di filtro inclusiva: se filtro="all" o se uno dei tag dell'item contiene la stringa del filtro
+                const match = filter === 'all' || item.tags.some(t => t.includes(filter));
+                
+                if (match) {
+                    count++;
+                    const card = document.createElement('a');
+                    card.href = item.url;
+                    card.className = 'card';
+                    
+                    const tagsHtml = item.tags.map(t => `<span class="tag">#${t}</span>`).join('');
+                    
+                    // Formatta la data (opzionale)
+                    const dateObj = new Date(item.date);
+                    const dateStr = !isNaN(dateObj) ? dateObj.toLocaleDateString('it-IT') : item.date;
+
+                    card.innerHTML = `
+                        <div class="card-icon">${item.icon}</div>
+                        <div class="card-date">${dateStr}</div>
+                        <h3>${item.title}</h3>
+                        <p>${item.excerpt}</p>
+                        <div class="card-tags">${tagsHtml}</div>
+                    `;
+                    grid.appendChild(card);
+                }
+            });
+
+            emptyState.style.display = count === 0 ? 'block' : 'none';
+        }
+
+        // Setup Filtri
         buttons.forEach(btn => {
             btn.addEventListener('click', () => {
-                // 1. Gestione stile bottone attivo
-                buttons.forEach(b => b.classList.remove('active'));
+                document.querySelector('.filter-btn.active').classList.remove('active');
                 btn.classList.add('active');
-
-                const filterValue = btn.getAttribute('data-filter');
-
-                // 2. Logica Filtro
-                cards.forEach(card => {
-                    const tags = card.getAttribute('data-tags'); // Legge i tag dalla card
-                    
-                    if (filterValue === 'all') {
-                        card.style.display = 'flex';
-                    } else {
-                        // Controlla se il tag è presente nella stringa data-tags
-                        if (tags.includes(filterValue)) {
-                            card.style.display = 'flex';
-                        } else {
-                            card.style.display = 'none';
-                        }
-                    }
-                });
+                render(btn.dataset.filter);
             });
         });
+
+        // Avvio
+        render('all');
     </script>
 </body>
-</html>
\ No newline at end of file
+</html>

commit ab5cca59c89d51b52d6bb88803646b4dd36d8188
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sun Dec 7 10:02:32 2025 +0100

    index nuovo 001

diff --git a/index.html b/index.html
index f2d3891..002c480 100644
--- a/index.html
+++ b/index.html
@@ -1,176 +1,168 @@
 <!DOCTYPE html>
-<html lang="it" data-theme="dark">
+<html lang="it">
 <head>
     <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Archivio Didattico</title>
+    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
+    <title>FedericoDB Lab</title>
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
     
     <style>
-        /* Layout */
-        .header-section { margin-bottom: 2rem; border-bottom: 1px solid var(--muted-border-color); padding-bottom: 1rem; }
+        :root {
+            --primary: #4f46e5; /* Indaco professionale */
+            --bg-card: #1e293b;
+            --text-main: #f8fafc;
+        }
+
+        body {
+            background-color: #0f172a; /* Slate 900 (Scuro elegante) */
+            color: var(--text-main);
+            padding-bottom: 3rem;
+        }
+
+        /* Intestazione */
+        header { padding: 2rem 0; text-align: center; }
+        header h1 { margin-bottom: 0.5rem; color: #38bdf8; text-transform: uppercase; letter-spacing: 2px; }
         
-        /* Wordcloud Container */
-        #cloud-container {
-            width: 100%;
-            height: 300px;
-            background: rgba(255,255,255,0.05);
-            border-radius: 8px;
-            margin-bottom: 2rem;
+        /* Filtri (bottoni stile chip) */
+        .filters {
+            display: flex; gap: 0.5rem; overflow-x: auto; padding-bottom: 1rem; margin-bottom: 2rem;
+            justify-content: center; -webkit-overflow-scrolling: touch;
+        }
+        .filter-btn {
+            background: #334155; border: 1px solid #475569; color: #cbd5e1;
+            padding: 0.4rem 1rem; border-radius: 50px; font-size: 0.8rem; cursor: pointer; white-space: nowrap;
+            transition: all 0.2s;
+        }
+        .filter-btn.active { background: var(--primary); color: white; border-color: var(--primary); transform: scale(1.05); }
+
+        /* Griglia Responsive */
+        .grid-container {
+            display: grid;
+            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
+            gap: 1.5rem;
+        }
+
+        /* Card della Lezione */
+        .card {
+            background: var(--bg-card);
+            border: 1px solid #334155;
+            border-radius: 16px;
+            padding: 1.5rem;
+            transition: transform 0.2s, box-shadow 0.2s;
             cursor: pointer;
             position: relative;
+            overflow: hidden;
+            display: flex; flex-direction: column;
+            text-decoration: none; /* Rimuove sottolineatura link */
+            color: inherit;
         }
-        /* Messaggio sovrapposto alla cloud se vuota */
-        #cloud-overlay {
-            position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
-            color: var(--muted-color); pointer-events: none;
-        }
+        .card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(0,0,0,0.5); border-color: #38bdf8; }
 
-        /* Card Articoli */
-        .grid-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.5rem; }
-        article { cursor: pointer; transition: transform 0.2s, opacity 0.3s; }
-        article:hover { transform: translateY(-5px); border-color: var(--primary); }
+        /* Elementi della Card */
+        .card-meta { display: flex; justify-content: space-between; font-size: 0.7rem; color: #94a3b8; margin-bottom: 0.5rem; uppercase; letter-spacing: 1px; }
+        .card h3 { font-size: 1.25rem; margin-bottom: 0.5rem; color: white; }
+        .card p { font-size: 0.9rem; color: #cbd5e1; flex-grow: 1; margin-bottom: 1rem; }
         
-        /* Tag Styles */
-        .tag-pill { 
-            font-size: 0.75rem; 
-            padding: 2px 8px; 
-            border-radius: 12px; 
-            background: var(--card-background-color); 
-            border: 1px solid var(--muted-border-color);
-            margin-right: 5px;
-            display: inline-block;
-        }
-        .active-filter { background: var(--primary); color: white; border-color: var(--primary); }
+        /* Tags visivi */
+        .card-tags { display: flex; flex-wrap: wrap; gap: 0.3rem; }
+        .tag { font-size: 0.7rem; padding: 2px 8px; border-radius: 4px; background: #334155; color: #94a3b8; }
         
-        /* Control Bar */
-        .controls { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; }
+        /* Icona Tipo Lezione (Angolo in alto a destra) */
+        .card-icon { position: absolute; top: 1rem; right: 1rem; font-size: 1.5rem; opacity: 0.5; }
+
     </style>
 </head>
 <body>
 
     <main class="container">
-        
-        <header class="header-section">
-            <hgroup>
-                <h1>Diario di Laboratorio</h1>
-                <h2>Esplora le attività per tema, classe o anno</h2>
-            </hgroup>
+        <header>
+            <h1>Laboratorio Digitale</h1>
+            <p>Matematica • Fisica • Coding • Creatività</p>
         </header>
 
-        <section>
-            <small>Filtra cliccando sulle parole chiave o esplora la nuvola:</small>
-            <div id="cloud-container">
-                <canvas id="word-cloud" width="1000" height="300"></canvas>
-                <div id="cloud-overlay">Caricamento Tags...</div>
-            </div>
-            
-            <div class="controls">
-                <strong id="results-count">Mostrando tutti i post</strong>
-                <button class="outline secondary" onclick="resetFilters()" style="width: auto; font-size: 0.8rem;">Resetta Filtri ✕</button>
-            </div>
-        </section>
+        <div class="filters" id="filterBar">
+            <button class="filter-btn active" data-filter="all">Tutti</button>
+            <button class="filter-btn" data-filter="1EL">1ª Elettronica</button>
+            <button class="filter-btn" data-filter="4EL">4ª Elettronica</button>
+            <button class="filter-btn" data-filter="Matematica">Matematica</button>
+            <button class="filter-btn" data-filter="Lab">Laboratorio</button>
+            <button class="filter-btn" data-filter="Giochi">Giochi</button>
+        </div>
+
+        <div class="grid-container">
+
+            <a href="lezioni/cards_polinomi_memory004.html" class="card" data-tags="Matematica Giochi 1EL">
+                <div class="card-icon">🧩</div> <div class="card-meta">
+                    <span>15 Nov 2025</span>
+                    <span style="color: #fbbf24;">★ New</span>
+                </div>
+                <h3>FactorMatch Memory</h3>
+                <p>Sfida a tempo sulle scomposizioni di polinomi. Trova le coppie tra polinomio e fattorizzazione.</p>
+                <div class="card-tags">
+                    <span class="tag">#Algebra</span>
+                    <span class="tag">#Gamification</span>
+                </div>
+            </a>
+
+            <a href="lezioni/math_underground003_4el.html" class="card" data-tags="Matematica 4EL Lab">
+                <div class="card-icon">🚇</div>
+                <div class="card-meta">
+                    <span>06 Dic 2025</span>
+                </div>
+                <h3>Math Underground</h3>
+                <p>Mappa interattiva stile metropolitana per visualizzare il dominio e la classificazione delle funzioni.</p>
+                <div class="card-tags">
+                    <span class="tag">#Analisi</span>
+                    <span class="tag">#Mappe</span>
+                </div>
+            </a>
+
+            <a href="lezioni/voto_consapevole_2.5.html" class="card" data-tags="Lab Civica 4EL">
+                <div class="card-icon">🎲</div>
+                <div class="card-meta">
+                    <span>01 Ott 2025</span>
+                </div>
+                <h3>Voto Consapevole</h3>
+                <p>Simulazione interattiva sull'Equilibrio di Nash applicato alla media dei voti scolastici.</p>
+                <div class="card-tags">
+                    <span class="tag">#EdCivica</span>
+                    <span class="tag">#Simulazione</span>
+                </div>
+            </a>
 
-        <div class="grid-cards" id="posts-grid">
             </div>
-
     </main>
 
-    <script src="database.js"></script>
-    <script src="https://cdnjs.cloudflare.com/ajax/libs/wordcloud2.js/1.2.2/wordcloud2.min.js"></script>
-
     <script>
-        const grid = document.getElementById('posts-grid');
-        const countLabel = document.getElementById('results-count');
-        let activeTag = null;
-
-        // --- FUNZIONE: Inizializzazione ---
-        window.onload = function() {
-            renderPosts(db);
-            generateCloud(db);
-        };
-
-        // --- FUNZIONE: Disegna i Post ---
-        function renderPosts(data) {
-            grid.innerHTML = '';
-            
-            // Ordina per data (dal più recente)
-            data.sort((a, b) => new Date(b.date) - new Date(a.date));
-
-            data.forEach(post => {
-                const article = document.createElement('article');
-                article.onclick = () => window.location.href = post.url;
-                
-                // Crea HTML dei tag
-                const tagsHtml = post.tags.map(t => `<span class="tag-pill">${t}</span>`).join('');
-                const dateObj = new Date(post.date).toLocaleDateString('it-IT', { year: 'numeric', month: 'long', day: 'numeric' });
-
-                article.innerHTML = `
-                    <header><small>${dateObj}</small></header>
-                    <h4>${post.title}</h4>
-                    <p>${post.excerpt}</p>
-                    <footer>${tagsHtml}</footer>
-                `;
-                grid.appendChild(article);
-            });
-            
-            countLabel.innerText = `Visualizzati ${data.length} post`;
-        }
-
-        // --- FUNZIONE: Genera Wordcloud ---
-        function generateCloud(data) {
-            // Estrae e conta tutti i tag
-            const tagCounts = {};
-            data.forEach(post => {
-                post.tags.forEach(tag => {
-                    tagCounts[tag] = (tagCounts[tag] || 0) + 1;
+        // Logica semplice per i filtri (Vanilla JS - 0 dipendenze)
+        const buttons = document.querySelectorAll('.filter-btn');
+        const cards = document.querySelectorAll('.card');
+
+        buttons.forEach(btn => {
+            btn.addEventListener('click', () => {
+                // 1. Gestione stile bottone attivo
+                buttons.forEach(b => b.classList.remove('active'));
+                btn.classList.add('active');
+
+                const filterValue = btn.getAttribute('data-filter');
+
+                // 2. Logica Filtro
+                cards.forEach(card => {
+                    const tags = card.getAttribute('data-tags'); // Legge i tag dalla card
+                    
+                    if (filterValue === 'all') {
+                        card.style.display = 'flex';
+                    } else {
+                        // Controlla se il tag è presente nella stringa data-tags
+                        if (tags.includes(filterValue)) {
+                            card.style.display = 'flex';
+                        } else {
+                            card.style.display = 'none';
+                        }
+                    }
                 });
             });
-
-            // Converte in formato array per wordcloud2: [['Tag', peso], ...]
-            // Aumentiamo il peso per renderli più leggibili
-            const list = Object.keys(tagCounts).map(key => [key, tagCounts[key] * 15 + 10]);
-
-            const canvas = document.getElementById('word-cloud');
-            // Adatta il canvas alla larghezza del contenitore
-            canvas.width = document.getElementById('cloud-container').offsetWidth;
-            
-            document.getElementById('cloud-overlay').style.display = 'none';
-
-            WordCloud(canvas, {
-                list: list,
-                gridSize: 8,
-                weightFactor: 1.2, // Scala la dimensione
-                fontFamily: 'system-ui, sans-serif',
-                color: 'random-light', // Colori chiari per tema scuro
-                backgroundColor: 'transparent',
-                rotateRatio: 0, // 0 = tutto orizzontale (più leggibile)
-                click: function(item) {
-                    filterByTag(item[0]);
-                },
-                hover: function(item, dimension, event) {
-                    canvas.style.cursor = item ? 'pointer' : 'default';
-                }
-            });
-        }
-
-        // --- FUNZIONE: Filtra ---
-        function filterByTag(tag) {
-            activeTag = tag;
-            const filtered = db.filter(post => post.tags.includes(tag));
-            renderPosts(filtered);
-            countLabel.innerHTML = `Filtro: <strong>${tag}</strong> (${filtered.length})`;
-            window.scrollTo({ top: document.getElementById('posts-grid').offsetTop - 100, behavior: 'smooth' });
-        }
-
-        function resetFilters() {
-            activeTag = null;
-            renderPosts(db);
-        }
-        
-        // Ridisegna la cloud se si ridimensiona la finestra
-        window.onresize = () => generateCloud(db);
-
+        });
     </script>
 </body>
 </html>
\ No newline at end of file

commit 405ce9bd0b99508c15c1cf5051083cf7512124aa
Author: federicodb <vivasantarosa@gmail.com>
Date:   Thu Dec 4 23:34:47 2025 +0100

    pulizia vecchio sito, landing page di prova

diff --git a/index.html b/index.html
index f37372c..f2d3891 100644
--- a/index.html
+++ b/index.html
@@ -1,5 +1,176 @@
----
-layout: home
-title: Melting Math
-subtitle: A space for melting math
----
+<!DOCTYPE html>
+<html lang="it" data-theme="dark">
+<head>
+    <meta charset="UTF-8">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0">
+    <title>Archivio Didattico</title>
+    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
+    
+    <style>
+        /* Layout */
+        .header-section { margin-bottom: 2rem; border-bottom: 1px solid var(--muted-border-color); padding-bottom: 1rem; }
+        
+        /* Wordcloud Container */
+        #cloud-container {
+            width: 100%;
+            height: 300px;
+            background: rgba(255,255,255,0.05);
+            border-radius: 8px;
+            margin-bottom: 2rem;
+            cursor: pointer;
+            position: relative;
+        }
+        /* Messaggio sovrapposto alla cloud se vuota */
+        #cloud-overlay {
+            position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
+            color: var(--muted-color); pointer-events: none;
+        }
+
+        /* Card Articoli */
+        .grid-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.5rem; }
+        article { cursor: pointer; transition: transform 0.2s, opacity 0.3s; }
+        article:hover { transform: translateY(-5px); border-color: var(--primary); }
+        
+        /* Tag Styles */
+        .tag-pill { 
+            font-size: 0.75rem; 
+            padding: 2px 8px; 
+            border-radius: 12px; 
+            background: var(--card-background-color); 
+            border: 1px solid var(--muted-border-color);
+            margin-right: 5px;
+            display: inline-block;
+        }
+        .active-filter { background: var(--primary); color: white; border-color: var(--primary); }
+        
+        /* Control Bar */
+        .controls { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; }
+    </style>
+</head>
+<body>
+
+    <main class="container">
+        
+        <header class="header-section">
+            <hgroup>
+                <h1>Diario di Laboratorio</h1>
+                <h2>Esplora le attività per tema, classe o anno</h2>
+            </hgroup>
+        </header>
+
+        <section>
+            <small>Filtra cliccando sulle parole chiave o esplora la nuvola:</small>
+            <div id="cloud-container">
+                <canvas id="word-cloud" width="1000" height="300"></canvas>
+                <div id="cloud-overlay">Caricamento Tags...</div>
+            </div>
+            
+            <div class="controls">
+                <strong id="results-count">Mostrando tutti i post</strong>
+                <button class="outline secondary" onclick="resetFilters()" style="width: auto; font-size: 0.8rem;">Resetta Filtri ✕</button>
+            </div>
+        </section>
+
+        <div class="grid-cards" id="posts-grid">
+            </div>
+
+    </main>
+
+    <script src="database.js"></script>
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/wordcloud2.js/1.2.2/wordcloud2.min.js"></script>
+
+    <script>
+        const grid = document.getElementById('posts-grid');
+        const countLabel = document.getElementById('results-count');
+        let activeTag = null;
+
+        // --- FUNZIONE: Inizializzazione ---
+        window.onload = function() {
+            renderPosts(db);
+            generateCloud(db);
+        };
+
+        // --- FUNZIONE: Disegna i Post ---
+        function renderPosts(data) {
+            grid.innerHTML = '';
+            
+            // Ordina per data (dal più recente)
+            data.sort((a, b) => new Date(b.date) - new Date(a.date));
+
+            data.forEach(post => {
+                const article = document.createElement('article');
+                article.onclick = () => window.location.href = post.url;
+                
+                // Crea HTML dei tag
+                const tagsHtml = post.tags.map(t => `<span class="tag-pill">${t}</span>`).join('');
+                const dateObj = new Date(post.date).toLocaleDateString('it-IT', { year: 'numeric', month: 'long', day: 'numeric' });
+
+                article.innerHTML = `
+                    <header><small>${dateObj}</small></header>
+                    <h4>${post.title}</h4>
+                    <p>${post.excerpt}</p>
+                    <footer>${tagsHtml}</footer>
+                `;
+                grid.appendChild(article);
+            });
+            
+            countLabel.innerText = `Visualizzati ${data.length} post`;
+        }
+
+        // --- FUNZIONE: Genera Wordcloud ---
+        function generateCloud(data) {
+            // Estrae e conta tutti i tag
+            const tagCounts = {};
+            data.forEach(post => {
+                post.tags.forEach(tag => {
+                    tagCounts[tag] = (tagCounts[tag] || 0) + 1;
+                });
+            });
+
+            // Converte in formato array per wordcloud2: [['Tag', peso], ...]
+            // Aumentiamo il peso per renderli più leggibili
+            const list = Object.keys(tagCounts).map(key => [key, tagCounts[key] * 15 + 10]);
+
+            const canvas = document.getElementById('word-cloud');
+            // Adatta il canvas alla larghezza del contenitore
+            canvas.width = document.getElementById('cloud-container').offsetWidth;
+            
+            document.getElementById('cloud-overlay').style.display = 'none';
+
+            WordCloud(canvas, {
+                list: list,
+                gridSize: 8,
+                weightFactor: 1.2, // Scala la dimensione
+                fontFamily: 'system-ui, sans-serif',
+                color: 'random-light', // Colori chiari per tema scuro
+                backgroundColor: 'transparent',
+                rotateRatio: 0, // 0 = tutto orizzontale (più leggibile)
+                click: function(item) {
+                    filterByTag(item[0]);
+                },
+                hover: function(item, dimension, event) {
+                    canvas.style.cursor = item ? 'pointer' : 'default';
+                }
+            });
+        }
+
+        // --- FUNZIONE: Filtra ---
+        function filterByTag(tag) {
+            activeTag = tag;
+            const filtered = db.filter(post => post.tags.includes(tag));
+            renderPosts(filtered);
+            countLabel.innerHTML = `Filtro: <strong>${tag}</strong> (${filtered.length})`;
+            window.scrollTo({ top: document.getElementById('posts-grid').offsetTop - 100, behavior: 'smooth' });
+        }
+
+        function resetFilters() {
+            activeTag = null;
+            renderPosts(db);
+        }
+        
+        // Ridisegna la cloud se si ridimensiona la finestra
+        window.onresize = () => generateCloud(db);
+
+    </script>
+</body>
+</html>
\ No newline at end of file

commit 16b69a3bac190dc081f4c93622441a9e177d02bb
Author: federicodb <vivasantarosa@gmail.com>
Date:   Sat Dec 31 18:33:18 2022 +0100

    Update index.html

diff --git a/index.html b/index.html
index c72dfaa..f37372c 100644
--- a/index.html
+++ b/index.html
@@ -1,5 +1,5 @@
 ---
 layout: home
-title: My website
-subtitle: This is where I will tell my friends way too much about me
+title: Melting Math
+subtitle: A space for melting math
 ---

commit f65e439025a7890b8b5f2ed2a77dd13490d381e9
Author: Dean Attali <daattali@gmail.com>
Date:   Sun Aug 23 19:23:14 2020 +0000

    add full-width YAML option

diff --git a/index.html b/index.html
index 2f8866e..c72dfaa 100644
--- a/index.html
+++ b/index.html
@@ -3,4 +3,3 @@ layout: home
 title: My website
 subtitle: This is where I will tell my friends way too much about me
 ---
-

commit c2e655f7e8c90b82c224dbf7ad4dab1235f625be
Author: Dean Attali <daattali@gmail.com>
Date:   Sun Apr 26 06:57:50 2020 +0000

    add 'home' layout and use it in index file

diff --git a/index.html b/index.html
index 554133a..2f8866e 100644
--- a/index.html
+++ b/index.html
@@ -1,74 +1,6 @@
 ---
-layout: page
+layout: home
 title: My website
 subtitle: This is where I will tell my friends way too much about me
-use-site-title: true
 ---
 
-<div class="posts-list">
-  {% for post in paginator.posts %}
-  <article class="post-preview">
-    <a href="{{ post.url | relative_url }}">
-	  <h2 class="post-title">{{ post.title }}</h2>
-
-	  {% if post.subtitle %}
-	  <h3 class="post-subtitle">
-	    {{ post.subtitle }}
-	  </h3>
-	  {% endif %}
-    </a>
-
-    <p class="post-meta">
-        {% assign date_format = site.date_format | default: "%B %-d, %Y" %}
-        Posted on {{ post.date | date: date_format }}
-    </p>
-
-    <div class="post-entry-container">
-      {% if post.image %}
-      <div class="post-image">
-        <a href="{{ post.url | relative_url }}">
-          <img src="{{ post.image | relative_url }}">
-        </a>
-      </div>
-      {% endif %}
-      <div class="post-entry">
-        {% assign excerpt_length = site.excerpt_length | default: 50 %}
-	{{ post.excerpt | strip_html | xml_escape | truncatewords: excerpt_length }}
-        {% assign excerpt_word_count = post.excerpt | number_of_words %}
-        {% if post.content != post.excerpt or excerpt_word_count > excerpt_length %}
-          <a href="{{ post.url | relative_url }}" class="post-read-more">[Read&nbsp;More]</a>
-        {% endif %}
-      </div>
-    </div>
-
-    {% if post.tags.size > 0 %}
-    <div class="blog-tags">
-      Tags:
-      {% if site.link-tags %}
-      {% for tag in post.tags %}
-      <a href="{{ '/tags' | relative_url }}#{{- tag -}}">{{- tag -}}</a>
-      {% endfor %}
-      {% else %}
-        {{ post.tags | join: ", " }}
-      {% endif %}
-    </div>
-    {% endif %}
-
-   </article>
-  {% endfor %}
-</div>
-
-{% if paginator.total_pages > 1 %}
-<ul class="pager main-pager">
-  {% if paginator.previous_page %}
-  <li class="previous">
-    <a href="{{ paginator.previous_page_path | relative_url }}">&larr; Newer Posts</a>
-  </li>
-  {% endif %}
-  {% if paginator.next_page %}
-  <li class="next">
-    <a href="{{ paginator.next_page_path | relative_url }}">Older Posts &rarr;</a>
-  </li>
-  {% endif %}
-</ul>
-{% endif %}

commit b03fb7311f1a40f7679f6f124917d972e7c3d60a
Author: Gabriel <gabriel@potter.fr>
Date:   Fri Apr 24 01:18:23 2020 +0200

    Add default values for config variables (#621)

diff --git a/index.html b/index.html
index 3c1ccd4..554133a 100644
--- a/index.html
+++ b/index.html
@@ -19,7 +19,8 @@ use-site-title: true
     </a>
 
     <p class="post-meta">
-      Posted on {{ post.date | date: site.date_format }}
+        {% assign date_format = site.date_format | default: "%B %-d, %Y" %}
+        Posted on {{ post.date | date: date_format }}
     </p>
 
     <div class="post-entry-container">
@@ -31,9 +32,10 @@ use-site-title: true
       </div>
       {% endif %}
       <div class="post-entry">
-        {{ post.excerpt | strip_html | xml_escape | truncatewords: site.excerpt_length }}
+        {% assign excerpt_length = site.excerpt_length | default: 50 %}
+	{{ post.excerpt | strip_html | xml_escape | truncatewords: excerpt_length }}
         {% assign excerpt_word_count = post.excerpt | number_of_words %}
-        {% if post.content != post.excerpt or excerpt_word_count > site.excerpt_length %}
+        {% if post.content != post.excerpt or excerpt_word_count > excerpt_length %}
           <a href="{{ post.url | relative_url }}" class="post-read-more">[Read&nbsp;More]</a>
         {% endif %}
       </div>

commit 5dca8a44ff42c8e8069a01a5364666cd2c8089c9
Author: Bang Jun-young <bang.junyoung@gmail.com>
Date:   Wed Aug 21 22:46:01 2019 +0900

    Add support for customizable date format (#536)

diff --git a/index.html b/index.html
index 5f9c764..3c1ccd4 100644
--- a/index.html
+++ b/index.html
@@ -19,7 +19,7 @@ use-site-title: true
     </a>
 
     <p class="post-meta">
-      Posted on {{ post.date | date: "%B %-d, %Y" }}
+      Posted on {{ post.date | date: site.date_format }}
     </p>
 
     <div class="post-entry-container">

commit 7061f6be17e3a577cad5dbb612c2d192148a08eb
Author: Abel Cheung <abelcheung@users.noreply.github.com>
Date:   Fri Jun 14 01:28:17 2019 +0800

    Fix post image url too (#504)

diff --git a/index.html b/index.html
index 68916b4..5f9c764 100644
--- a/index.html
+++ b/index.html
@@ -26,7 +26,7 @@ use-site-title: true
       {% if post.image %}
       <div class="post-image">
         <a href="{{ post.url | relative_url }}">
-          <img src="{{ post.image }}">
+          <img src="{{ post.image | relative_url }}">
         </a>
       </div>
       {% endif %}

commit 7b43e4061dd795cb3be91b47f828ea6d72cd7dcf
Author: Abel Cheung <abelcheung@users.noreply.github.com>
Date:   Sat Jun 8 13:34:33 2019 +0800

    Fix showing background img with non-empty baseurl (#497)
    
    * Fix showing background img with non-empty baseurl
    
    Also switch globally to relative_url for all baseurl prepends,
    which is more robust than custom URL mangling
    
    * More site.baseurl -> relative_url conversion

diff --git a/index.html b/index.html
index cfb2ebe..68916b4 100644
--- a/index.html
+++ b/index.html
@@ -8,7 +8,7 @@ use-site-title: true
 <div class="posts-list">
   {% for post in paginator.posts %}
   <article class="post-preview">
-    <a href="{{ post.url | prepend: site.baseurl }}">
+    <a href="{{ post.url | relative_url }}">
 	  <h2 class="post-title">{{ post.title }}</h2>
 
 	  {% if post.subtitle %}
@@ -25,7 +25,7 @@ use-site-title: true
     <div class="post-entry-container">
       {% if post.image %}
       <div class="post-image">
-        <a href="{{ post.url | prepend: site.baseurl }}">
+        <a href="{{ post.url | relative_url }}">
           <img src="{{ post.image }}">
         </a>
       </div>
@@ -34,7 +34,7 @@ use-site-title: true
         {{ post.excerpt | strip_html | xml_escape | truncatewords: site.excerpt_length }}
         {% assign excerpt_word_count = post.excerpt | number_of_words %}
         {% if post.content != post.excerpt or excerpt_word_count > site.excerpt_length %}
-          <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+          <a href="{{ post.url | relative_url }}" class="post-read-more">[Read&nbsp;More]</a>
         {% endif %}
       </div>
     </div>
@@ -44,7 +44,7 @@ use-site-title: true
       Tags:
       {% if site.link-tags %}
       {% for tag in post.tags %}
-      <a href="{{ site.baseurl }}/tags#{{- tag -}}">{{- tag -}}</a>
+      <a href="{{ '/tags' | relative_url }}#{{- tag -}}">{{- tag -}}</a>
       {% endfor %}
       {% else %}
         {{ post.tags | join: ", " }}
@@ -60,12 +60,12 @@ use-site-title: true
 <ul class="pager main-pager">
   {% if paginator.previous_page %}
   <li class="previous">
-    <a href="{{ paginator.previous_page_path | prepend: site.baseurl | replace: '//', '/' }}">&larr; Newer Posts</a>
+    <a href="{{ paginator.previous_page_path | relative_url }}">&larr; Newer Posts</a>
   </li>
   {% endif %}
   {% if paginator.next_page %}
   <li class="next">
-    <a href="{{ paginator.next_page_path | prepend: site.baseurl | replace: '//', '/' }}">Older Posts &rarr;</a>
+    <a href="{{ paginator.next_page_path | relative_url }}">Older Posts &rarr;</a>
   </li>
   {% endif %}
 </ul>

commit 3664e56dbda016a332377903fe4801801e27ba4f
Author: Marco Blessing <marco.blessing@googlemail.com>
Date:   Fri Jan 19 05:58:00 2018 +0100

    Replaces tag index plugin with native jekyll features (#307)
    
    * remove tag _gen plugin and its folder
    
    * remove deprecated tag index layout
    
    * add tag index page
    
    * link tag index page in nav bar, enable tag links by default
    
    * add button spacer in main css
    
    * fix tag links in post view
    
    * fix tag links on index and pagination
    
    * tag link instuctions removed. it's enabled as default

diff --git a/index.html b/index.html
index e297651..cfb2ebe 100644
--- a/index.html
+++ b/index.html
@@ -44,7 +44,7 @@ use-site-title: true
       Tags:
       {% if site.link-tags %}
       {% for tag in post.tags %}
-      <a href="{{ site.baseurl }}/tag/{{ tag }}">{{ tag }}</a>
+      <a href="{{ site.baseurl }}/tags#{{- tag -}}">{{- tag -}}</a>
       {% endfor %}
       {% else %}
         {{ post.tags | join: ", " }}

commit 75f4d8703e995ca52ab6efffd7f836d57337f41f
Author: Tony Ho <tony-ho@users.noreply.github.com>
Date:   Fri Jan 6 19:33:51 2017 +1100

    Add options to configure HTML document title (#154)
    
    * Add option to use site title as HTML document title
    
    * Add configurable title separator character
    
    * Keep old document title behaviour when not using use-site-title

diff --git a/index.html b/index.html
index 9e8da19..e297651 100644
--- a/index.html
+++ b/index.html
@@ -2,6 +2,7 @@
 layout: page
 title: My website
 subtitle: This is where I will tell my friends way too much about me
+use-site-title: true
 ---
 
 <div class="posts-list">

commit 66bd2ada990086449ebd10d4f1c97d0ebdecda12
Author: Bryan Britten <britten.bryan@gmail.com>
Date:   Sat Dec 24 17:48:55 2016 -0500

    Allow dynamic images on each blog post (#143)
    
    * Allow dynamic images on each blog post
    
    * Adding responsive CSS for blog post images
    
    * Adding image parameter to YAML front matter

diff --git a/index.html b/index.html
index d846f02..9e8da19 100644
--- a/index.html
+++ b/index.html
@@ -21,12 +21,21 @@ subtitle: This is where I will tell my friends way too much about me
       Posted on {{ post.date | date: "%B %-d, %Y" }}
     </p>
 
-    <div class="post-entry">
-      {{ post.excerpt | strip_html | xml_escape | truncatewords: site.excerpt_length }}
-      {% assign excerpt_word_count = post.excerpt | number_of_words %}
-      {% if post.content != post.excerpt or excerpt_word_count > site.excerpt_length %}
-        <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+    <div class="post-entry-container">
+      {% if post.image %}
+      <div class="post-image">
+        <a href="{{ post.url | prepend: site.baseurl }}">
+          <img src="{{ post.image }}">
+        </a>
+      </div>
       {% endif %}
+      <div class="post-entry">
+        {{ post.excerpt | strip_html | xml_escape | truncatewords: site.excerpt_length }}
+        {% assign excerpt_word_count = post.excerpt | number_of_words %}
+        {% if post.content != post.excerpt or excerpt_word_count > site.excerpt_length %}
+          <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+        {% endif %}
+      </div>
     </div>
 
     {% if post.tags.size > 0 %}

commit f4d139d2d01045bc59aa85f67758ebd8b83f8783
Author: Luke Dodge <luke.r.dodge@gmail.com>
Date:   Fri Oct 7 04:56:48 2016 -0500

    Fix show/hide of Read Me link on index page (#111)

diff --git a/index.html b/index.html
index e7ffb0d..d846f02 100644
--- a/index.html
+++ b/index.html
@@ -22,19 +22,16 @@ subtitle: This is where I will tell my friends way too much about me
     </p>
 
     <div class="post-entry">
-      {% assign cleaned_content = post.excerpt | strip_html | xml_escape %}
-      {% assign truncated_content = cleaned_content | truncatewords: 50 %}
-      {% if truncated_content == cleaned_content %}
-        {{ cleaned_content }}
-      {% else %}
-        {{ truncated_content }}
+      {{ post.excerpt | strip_html | xml_escape | truncatewords: site.excerpt_length }}
+      {% assign excerpt_word_count = post.excerpt | number_of_words %}
+      {% if post.content != post.excerpt or excerpt_word_count > site.excerpt_length %}
         <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
-      {% endif%}
+      {% endif %}
     </div>
 
     {% if post.tags.size > 0 %}
     <div class="blog-tags">
-      Tags: 
+      Tags:
       {% if site.link-tags %}
       {% for tag in post.tags %}
       <a href="{{ site.baseurl }}/tag/{{ tag }}">{{ tag }}</a>

commit dfb1c6191764af7e8d57baa141ab44f764a5923a
Author: Dean Attali <daattali@gmail.com>
Date:   Tue Oct 4 19:43:33 2016 -0700

    replace post.content with post.excerpt in index page

diff --git a/index.html b/index.html
index 53c8aaa..e7ffb0d 100644
--- a/index.html
+++ b/index.html
@@ -22,7 +22,7 @@ subtitle: This is where I will tell my friends way too much about me
     </p>
 
     <div class="post-entry">
-      {% assign cleaned_content = post.content | strip_html | xml_escape %}
+      {% assign cleaned_content = post.excerpt | strip_html | xml_escape %}
       {% assign truncated_content = cleaned_content | truncatewords: 50 %}
       {% if truncated_content == cleaned_content %}
         {{ cleaned_content }}

commit 1b550134f1a5e27b81e7b7604e2b682703f8f864
Author: Dean Attali <daattali@gmail.com>
Date:   Tue Oct 4 19:36:39 2016 -0700

    ignore excerpt_separator in index.html

diff --git a/index.html b/index.html
index 5f93984..53c8aaa 100644
--- a/index.html
+++ b/index.html
@@ -22,19 +22,14 @@ subtitle: This is where I will tell my friends way too much about me
     </p>
 
     <div class="post-entry">
-      {% if post.content contains site.excerpt_separator %}
-        {{ post.excerpt | strip_html | xml_escape }}
-        <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+      {% assign cleaned_content = post.content | strip_html | xml_escape %}
+      {% assign truncated_content = cleaned_content | truncatewords: 50 %}
+      {% if truncated_content == cleaned_content %}
+        {{ cleaned_content }}
       {% else %}
-        {% assign cleaned_content = post.content | strip_html | xml_escape %}
-        {% assign truncated_content = cleaned_content | truncatewords: 50 %}
-        {% if truncated_content == cleaned_content %}
-          {{ cleaned_content }}
-        {% else %}
-          {{ truncated_content }}
-          <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
-        {% endif%}
-      {% endif %}
+        {{ truncated_content }}
+        <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+      {% endif%}
     </div>
 
     {% if post.tags.size > 0 %}

commit 1b668a32237f6064e9e01984b54f8a93b58308d3
Author: Luke Dodge <luke.r.dodge@gmail.com>
Date:   Tue Oct 4 21:12:20 2016 -0500

    Add check to only show readme link when there is additional post content not shown on index page. Handles truncate words problems with Chinese and Japanese characters by checking for a manual break using the site excerpt separator. (#110)

diff --git a/index.html b/index.html
index 463143b..5f93984 100644
--- a/index.html
+++ b/index.html
@@ -22,8 +22,19 @@ subtitle: This is where I will tell my friends way too much about me
     </p>
 
     <div class="post-entry">
-      {{ post.excerpt | strip_html | xml_escape | truncatewords: 50 }}
-	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+      {% if post.content contains site.excerpt_separator %}
+        {{ post.excerpt | strip_html | xml_escape }}
+        <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+      {% else %}
+        {% assign cleaned_content = post.content | strip_html | xml_escape %}
+        {% assign truncated_content = cleaned_content | truncatewords: 50 %}
+        {% if truncated_content == cleaned_content %}
+          {{ cleaned_content }}
+        {% else %}
+          {{ truncated_content }}
+          <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
+        {% endif%}
+      {% endif %}
     </div>
 
     {% if post.tags.size > 0 %}

commit c2534b71ec5223a7d21543414135420b10f89dbc
Author: Liu Binsheng <liubinsheng@gmail.com>
Date:   Fri Sep 16 00:41:31 2016 +0800

    use post.excerpt instead of post.content

diff --git a/index.html b/index.html
index 325a239..463143b 100644
--- a/index.html
+++ b/index.html
@@ -22,7 +22,7 @@ subtitle: This is where I will tell my friends way too much about me
     </p>
 
     <div class="post-entry">
-      {{ post.content | strip_html | xml_escape | truncatewords: 50 }}
+      {{ post.excerpt | strip_html | xml_escape | truncatewords: 50 }}
 	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
 

commit 0eb18422ee3aaa2ccd172f0604d77f36ca20523b
Author: Pete <epwalsh10@gmail.com>
Date:   Wed Jul 27 18:20:28 2016 -0500

    minor fixes

diff --git a/index.html b/index.html
index 27a75d9..325a239 100644
--- a/index.html
+++ b/index.html
@@ -31,7 +31,7 @@ subtitle: This is where I will tell my friends way too much about me
       Tags: 
       {% if site.link-tags %}
       {% for tag in post.tags %}
-        <a href="/tag/{{ tag }}">{{ tag }}</a>
+      <a href="{{ site.baseurl }}/tag/{{ tag }}">{{ tag }}</a>
       {% endfor %}
       {% else %}
         {{ post.tags | join: ", " }}

commit 0458bf9803bcfbaaab2643bd47abff90c50aa0bc
Author: Pete <epwalsh10@gmail.com>
Date:   Wed Jul 27 17:11:58 2016 -0500

    tagging options
    
    Tags will display as plain text if a user adds tags to a post. If user
    sets 'link-tags: true' in _config.yml, then Jekyll will make a new page
    for each tag which lists all posts with given tag (provided the site is
    not deployed through GitHub pages)

diff --git a/index.html b/index.html
index 6a84221..27a75d9 100644
--- a/index.html
+++ b/index.html
@@ -26,12 +26,16 @@ subtitle: This is where I will tell my friends way too much about me
 	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
 
-    {% if site.show-tags %}
+    {% if post.tags.size > 0 %}
     <div class="blog-tags">
       Tags: 
+      {% if site.link-tags %}
       {% for tag in post.tags %}
         <a href="/tag/{{ tag }}">{{ tag }}</a>
       {% endfor %}
+      {% else %}
+        {{ post.tags | join: ", " }}
+      {% endif %}
     </div>
     {% endif %}
 

commit ce8ece5fc24774df89a47acc98dcf1bbe20e4d5b
Author: Pete <epwalsh10@gmail.com>
Date:   Wed Jul 27 16:46:42 2016 -0500

    option for tags

diff --git a/index.html b/index.html
index da5142d..6a84221 100644
--- a/index.html
+++ b/index.html
@@ -26,14 +26,15 @@ subtitle: This is where I will tell my friends way too much about me
 	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
 
-    {% if post.tags.size > 0 %}
+    {% if site.show-tags %}
     <div class="blog-tags">
       Tags: 
       {% for tag in post.tags %}
-      <a href="/tag/{{ tag }}">{{ tag }}</a>
+        <a href="/tag/{{ tag }}">{{ tag }}</a>
       {% endfor %}
     </div>
     {% endif %}
+
    </article>
   {% endfor %}
 </div>

commit b90c1c8de94d9c51be4a3d72bcb5c40ff51b2e65
Author: Pete <epwalsh10@gmail.com>
Date:   Wed Jul 27 15:19:16 2016 -0500

    add tags

diff --git a/index.html b/index.html
index 9893958..da5142d 100644
--- a/index.html
+++ b/index.html
@@ -26,6 +26,14 @@ subtitle: This is where I will tell my friends way too much about me
 	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
 
+    {% if post.tags.size > 0 %}
+    <div class="blog-tags">
+      Tags: 
+      {% for tag in post.tags %}
+      <a href="/tag/{{ tag }}">{{ tag }}</a>
+      {% endfor %}
+    </div>
+    {% endif %}
    </article>
   {% endfor %}
 </div>

commit 9ecf26af733937377b7f751c41459c8648cd81a9
Author: lrdodge <luke.r.dodge@gmail.com>
Date:   Sun Jun 19 19:09:43 2016 -0500

    Change order of post excerpt processing

diff --git a/index.html b/index.html
index 9bb92a7..9893958 100644
--- a/index.html
+++ b/index.html
@@ -9,23 +9,23 @@ subtitle: This is where I will tell my friends way too much about me
   <article class="post-preview">
     <a href="{{ post.url | prepend: site.baseurl }}">
 	  <h2 class="post-title">{{ post.title }}</h2>
-	
+
 	  {% if post.subtitle %}
 	  <h3 class="post-subtitle">
 	    {{ post.subtitle }}
 	  </h3>
-	  {% endif %}  
+	  {% endif %}
     </a>
 
     <p class="post-meta">
       Posted on {{ post.date | date: "%B %-d, %Y" }}
     </p>
-  
+
     <div class="post-entry">
-      {{ post.content | truncatewords: 50 | strip_html | xml_escape}}
+      {{ post.content | strip_html | xml_escape | truncatewords: 50 }}
 	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
-  
+
    </article>
   {% endfor %}
 </div>

commit 1884ac978f8c4a6006553cef9eadaa8ffa8f1e83
Author: Dean Attali <daattali@gmail.com>
Date:   Sat Nov 21 11:48:10 2015 -0800

    Remove the instructions in index.html because users didn't know how to remove it

diff --git a/index.html b/index.html
index e15b8fd..9bb92a7 100644
--- a/index.html
+++ b/index.html
@@ -4,12 +4,6 @@ title: My website
 subtitle: This is where I will tell my friends way too much about me
 ---
 
-<div class="main-explain-area jumbotron">
-  <p>Your website is ready!</p>
-  <p>After editing the <code>_config.yml</code> file to personalize your site's settings, you can start writing blog posts or pages.<p>
-  <p>Below are sample posts for illustration purposes.</p>
-</div>
-
 <div class="posts-list">
   {% for post in paginator.posts %}
   <article class="post-preview">

commit b115a560676dcaf9481a29790dc4713c15e21767
Author: Dean Attali <daattali@gmail.com>
Date:   Sun Nov 8 15:12:36 2015 -0800

    XML escape post blurbs

diff --git a/index.html b/index.html
index 3692574..e15b8fd 100644
--- a/index.html
+++ b/index.html
@@ -28,7 +28,7 @@ subtitle: This is where I will tell my friends way too much about me
     </p>
   
     <div class="post-entry">
-      {{ post.content | truncatewords: 50 | strip_html}}
+      {{ post.content | truncatewords: 50 | strip_html | xml_escape}}
 	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
   

commit 66df8a95ca69c12ac0e3c7f9f71cdd3b89a9b198
Author: Dean Attali <daattali@gmail.com>
Date:   Tue Mar 31 00:23:21 2015 -0700

    make sure the [Read More] in blog excerpts is not disrupted by new line

diff --git a/index.html b/index.html
index 37a9ad8..3692574 100644
--- a/index.html
+++ b/index.html
@@ -29,7 +29,7 @@ subtitle: This is where I will tell my friends way too much about me
   
     <div class="post-entry">
       {{ post.content | truncatewords: 50 | strip_html}}
-	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">Read More</a>
+	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">[Read&nbsp;More]</a>
     </div>
   
    </article>

commit 757206b08202d85fc8f450384742335ffe974111
Author: Dean Attali <daattali@gmail.com>
Date:   Tue Mar 3 01:03:01 2015 -0800

    simplify YAML params and readme

diff --git a/index.html b/index.html
index 754a5e2..37a9ad8 100644
--- a/index.html
+++ b/index.html
@@ -1,7 +1,7 @@
 ---
 layout: page
 title: My website
-description: This is where I will tell my friends way too much about me
+subtitle: This is where I will tell my friends way too much about me
 ---
 
 <div class="main-explain-area jumbotron">

commit b88715cf3ce75085e990c9340acb1b68e495f6b3
Author: Dean Attali <daattali@gmail.com>
Date:   Mon Mar 2 17:21:32 2015 -0800

    slight wording change

diff --git a/index.html b/index.html
index e3dcdc9..754a5e2 100644
--- a/index.html
+++ b/index.html
@@ -6,7 +6,7 @@ description: This is where I will tell my friends way too much about me
 
 <div class="main-explain-area jumbotron">
   <p>Your website is ready!</p>
-  <p>After editing the <code>_config.yml</code> file to personalize the settings, you can start writing blog posts or pages.<p>
+  <p>After editing the <code>_config.yml</code> file to personalize your site's settings, you can start writing blog posts or pages.<p>
   <p>Below are sample posts for illustration purposes.</p>
 </div>
 

commit 13df585003bd40609a74b85e7738e0e2b0b246af
Author: Dean Attali <daattali@gmail.com>
Date:   Mon Mar 2 16:24:07 2015 -0800

    refer to all files relative to baseurl to work for project pages

diff --git a/index.html b/index.html
index 9b93db7..e3dcdc9 100644
--- a/index.html
+++ b/index.html
@@ -1,9 +1,15 @@
 ---
 layout: page
 title: My website
-description: This is where I will tell my friends everything about me
+description: This is where I will tell my friends way too much about me
 ---
 
+<div class="main-explain-area jumbotron">
+  <p>Your website is ready!</p>
+  <p>After editing the <code>_config.yml</code> file to personalize the settings, you can start writing blog posts or pages.<p>
+  <p>Below are sample posts for illustration purposes.</p>
+</div>
+
 <div class="posts-list">
   {% for post in paginator.posts %}
   <article class="post-preview">

commit 709f591ab459c8d3fbd7a97262ebeb37429474a3
Author: Dean Attali <daattali@gmail.com>
Date:   Mon Mar 2 12:06:05 2015 -0800

    initial commit

diff --git a/index.html b/index.html
new file mode 100644
index 0000000..9b93db7
--- /dev/null
+++ b/index.html
@@ -0,0 +1,46 @@
+---
+layout: page
+title: My website
+description: This is where I will tell my friends everything about me
+---
+
+<div class="posts-list">
+  {% for post in paginator.posts %}
+  <article class="post-preview">
+    <a href="{{ post.url | prepend: site.baseurl }}">
+	  <h2 class="post-title">{{ post.title }}</h2>
+	
+	  {% if post.subtitle %}
+	  <h3 class="post-subtitle">
+	    {{ post.subtitle }}
+	  </h3>
+	  {% endif %}  
+    </a>
+
+    <p class="post-meta">
+      Posted on {{ post.date | date: "%B %-d, %Y" }}
+    </p>
+  
+    <div class="post-entry">
+      {{ post.content | truncatewords: 50 | strip_html}}
+	  <a href="{{ post.url | prepend: site.baseurl }}" class="post-read-more">Read More</a>
+    </div>
+  
+   </article>
+  {% endfor %}
+</div>
+
+{% if paginator.total_pages > 1 %}
+<ul class="pager main-pager">
+  {% if paginator.previous_page %}
+  <li class="previous">
+    <a href="{{ paginator.previous_page_path | prepend: site.baseurl | replace: '//', '/' }}">&larr; Newer Posts</a>
+  </li>
+  {% endif %}
+  {% if paginator.next_page %}
+  <li class="next">
+    <a href="{{ paginator.next_page_path | prepend: site.baseurl | replace: '//', '/' }}">Older Posts &rarr;</a>
+  </li>
+  {% endif %}
+</ul>
+{% endif %}
